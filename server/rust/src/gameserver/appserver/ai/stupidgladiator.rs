//! Вариант боевого ИИ `CStupidGladiator`.
//!
//! Его подтверждённый `OnSearchEnemy` совпадает с `CGladiator` и выполняется
//! общим владельцем семейства в `ai/gladiator.rs` для реального типа ИИ `3`.
//! Отдельного состояния или отличающихся побочных эффектов у достигнутого пути нет.
