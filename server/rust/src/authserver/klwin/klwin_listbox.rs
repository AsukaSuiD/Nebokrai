//! Win32 listbox `klwin_listbox.cpp`, подтверждённый `authserver.exe` и
//! `authserver.pdb`. Операторские записи публикуются через стандартные потоки;
//! оконный контрол не входит в серверный контракт.
