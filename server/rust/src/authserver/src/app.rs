//! Процессная оболочка `app.cpp`, подтверждённая `authserver.exe` и
//! `authserver.pdb`. MFC window и message pump заменены Linux entrypoint;
//! доменным lifecycle владеет `cgame::game_thread_func`.
