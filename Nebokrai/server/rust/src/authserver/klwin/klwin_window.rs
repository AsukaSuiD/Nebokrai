//! Win32 window `klwin_window.cpp`, подтверждённое `authserver.exe` и
//! `authserver.pdb`. Message pump и callbacks заменены process entrypoint и
//! сигналами; отдельная UI-реализация не требуется.
