# ADR-0013: async-вызов в dyn-совместимом view-трейте через boxed future

Принято в шаге переноса GM-диспетчера WorldServer в Realm. Область: узкие view-швы между библиотеками `realm`/`zone` и владельцами, остающимися в старом пакете.

## Решение и причина

View-трейты края (`WorldGameView` и последующие) потребляются как `&mut dyn Trait` и `Box<dyn Trait>` сессиями и диспетчерами и потому обязаны оставаться dyn-совместимыми: аргументы методов — конкретные типы или `&dyn`, не `impl Trait`. Когда обработчику нужен асинхронный вызов владельца, метод записывается не `async fn`, а возвращением `Pin<Box<dyn Future<Output = ...> + 'a>>` с явным временем жизни аргументов. Реализация у владельца игры делегирует существующему inherent async-методу через `Box::pin(...)`; имена намеренно совпадают — inherent priority исключает рекурсию, как и в синхронных методах того же view.

`async fn` в публичном трейте (RPITIT) и generic-параметры теряют dyn-совместимость (E0038). Разрывать существующих dyn-потребителей или заводить второй синхронный shadow API ради одной ветви дороже одной heap-аллокации на вызов: управляющие маршруты (перезагрузка из GM/сценария) редки и уже не бесплатны из-за чтения ресурсов.

## Последствия

- Первый пример — `WorldGameView::reload` в [`realm/app/world_game_view.rs`](../../server/rust/realm/src/app/world_game_view.rs): boxed future поверх inherent `CGame::reload` с теми же аргументами; порядок вызовов и контракт результата (`WorldReloadResult`) не меняются.
- Маркер `Send` boxed future не добавляется, если исходный async-контракт его не требовал: dyn-контексты (`WorldReloadContext`) не обязуются быть Send, и их потребители уже работают вне Send-ограничения.
- Для горячих путей обработчиков boxed-heap-аллокация на каждый вызов не вводится без отдельного основания; сначала ищется синхронный или заранее выделенный путь.
