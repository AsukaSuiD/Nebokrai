# Аудит готовности серверной реконструкции

## Shared resources: фильтры кодов и запрещённых слов 25 сентября 2026

Фильтр допустимых кодов `CharCodeFilter` (осмотр диапазонов байт) и фильтр запрещённых слов `CWordsFilter` (ordered filters, two Check-варианта, wire-codec) перенесены из `src/public/char_code_filter.rs` и `src/public/wordsfilter.rs` в [`shared/src/resources/char_code_filter.rs`](../../server/rust/shared/src/resources/char_code_filter.rs) и [`wordsfilter.rs`](../../server/rust/shared/src/resources/wordsfilter.rs). Обе роли используют одну реализацию через тонкие реэкспорты прежних файлов; информационная trace-строка после decode удалена (Shared без `tracing`). Установленный экземпляр дома сообщения остаётся у владельца роли; GameServer-вариант `char_code_filter::check` сохраняет прежний статус `UNKNOWN` из метаданных исследования, каким был до переноса.

В этом шаге wire wordsfilter сверен с машинным кодом читателя Game `FromByteArray@CWordsFilter` (секция 1:0x291a0, VA 0x42a1a0): signed count диапазонов с выходом JLE, за ним по 1+1 byte на диапазон — 2-байтовые элементы в vector с push_back, а затем signed count строк — NUL-строки в `std::list<std::string>`; **обе секции дописываются к текущему содержимому без free/zero/nil-shuffle — заявленное заголовком «не очищает singleton, а дописывает оба списка» подтверждено машинными инструкциями**, в отличие от большинства sibling snapshot-таблиц с очисткой. Signed JLE совпадает с `max(0)`, safe-prefix за границу legacy безразмерного overread сохраняется. У World найдены `LoadFilter` (секция 1:0x50360), two Check-варианты (1:0x4fcf0 с тремя параметрами и 1:0x4fbd0) и singleton `instance`; у Game — singleton и свой Check (1:0x28b20). Тело World writer `AddByteToArray` публичными символами не экспортируется и отдельными инструкциями в этом проходе не открывалось; парность формата подтверждается полной схемой читателя и фактической доставкой конфигурации. RVA из заголовка `char_code_filter` (World `load 0x000D47A0`, `check 0x000D4590`, `IsAllNumbers 0x000D4330`) инструкциями заново не открывались. rustfmt привёл в порядок ошибочный отступ ` ///` doc-comment и одной вложенной примечание-строки прежних файлов, текста и семантики не затронув.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 33,25 секунды без предупреждений; `rustfmt` новых Shared-файлов и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.
## Shared resources: правила вставки больших отверстий 25 сентября 2026

Правила вставки больших отверстий `CDaKongXiangQian` (info vector 8×i32, три ordered external-attribute maps, delux_modify vector, key/log_key flags, wire-codec и `GetAddType/ChoiseRandom`) перенесены из `src/public/dakongxiangqian.rs` в [`shared/src/resources/dakongxiangqian.rs`](../../server/rust/shared/src/resources/dakongxiangqian.rs). Обе роли используют одну реализацию через тонкий реэкспорт прежнего файла; информационная trace-строка после decode удалена вместе со сведением суммы external-атрибутов (Shared без `tracing`). Установленный экземпляр и его потребители (factory/session Giant Cavity на Game, DB/World-цепочки) остаются у владельцев ролей.

В этом шаге формат и главная семантическая особенность сверены с машинным кодом зафиксированной пары сборок: у World найдены статический `AddToByteArray@CDaKongXiangQian` (секция 1:0x8adc0), `LoadFile` (1:0x8bad0), `SetKey` и булевы statics `m_bDaKongKey/m_bLogKey`, строки `data\DaKongXiangQian.ini` и `Load DaKongXiangQian.ini...ok/failed`. Writer пишет count и восемь raw-4 поля на 32-байтовую info-запись, затем map: u32 key + u32 inner count + четыре raw-4 поля на запись — три такие ordered map подряд, затем delux vector. Game `DecordFromByteArray@CDaKongXiangQian` (секция 1:0xa3660) подтверждает формат на входе и показывает искомое различие владельцев: основное состояние очищается — три external map erase+reinit и info vector free+zero — а **delux_modify vector не очищается, records дописываются в накопленный вектор при каждом snapshot повторно** — заявленный заголовком «дописывающий» decoder подтверждён инструкциями. Три external read-прохода идут с аргументами индекса 0/1/2 вставки, как у Rust массива `[ExtrernalAttributeMap; 3]`. Counters — signed JLE, совпадает с `max(0)`. `ChoiseRandom` и `GetAddType` присутствуют в gameserver публичных symbols (секции 1:0xa1ab0 и 1:0xa2980), но инструкциями в этом проходе не открывались — как и INI-loader `0x2B`-ordered формат, → для них основание остаётся заголовок.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 35,87 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.
## Shared resources: комплекты TaoZhuang 25 сентября 2026

Конфигурация комплектов `CTaoZhuangSetup` (ordered skill id set и item map с nested property/skill maps, declared-vs-actual counts, token loader и wire-codec) перенесена из `src/public/taozhuangsetup.rs` в [`shared/src/resources/taozhuangsetup.rs`](../../server/rust/shared/src/resources/taozhuangsetup.rs). Обе роли используют одну реализацию через тонкий реэкспорт прежнего файла; информационная trace-строка после decode удалена (Shared без `tracing`). Установленный экземпляр и его потребители (включая gameplay `IsCollectAll/QueryTaoZhuangIdByName/AddResultToPlayer` на Game) остаются у владельцев ролей.

В этом шаге схема wire сверена с машинным кодом по читателю и символам обеих сборок: у World найдены `ReadFile@CTaoZhuangSetup` (секция 1:0x88ae0), singleton `GetSingleInstance/pInstance` и строки `Load TaoZhuang config...ok/failed`; у Game — `DeCodeFromByte@CTaoZhuangSetup` (секция 1:0xe3020) и gameplay query-family. Reader (VA 0x4e4020) сначала вызывает `Clear` (секция 1:0xe2fc0), очищающий оба owner-а, затем читает skill-set count с jle-выходом и вставками id, далее count items и на item: четыре u32 (id, color и сохранённые declared counts), три C-строки через 1024-байтовые stack-буферы (тот самый старый 1024/1028-byte pattern, который Rust заменяет безопасными byte strings) и вложенные ordered property/skill maps — в полном соответствии Rust codec. Тело writer `AddByteToArray` World (inlined за singleton-владельцем) публичными символами не экспортируется и отдельными инструкциями в этом проходе не открывалось; парность формата подтверждается читателем по полной его схеме и фактом доставки snapshot-цепочки (`Add TaoZhuangSetup...OK` в gameserver.exe). Семантика declared-vs-actual count-ов (передаются сохранённые u32, а не пересчитанные) используется Rust serializer так же, как в вышеуказанном поле-поле reader-а: поля назначаются сохранёнными, а не вычисляются по container length. Duplicate-item-first-win текстового loader-а и abort-дубли вложенных вставок в этом проходе заново не дизассемблировались.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 35,37 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.
## Shared resources: конфигурация CiQing 25 сентября 2026

Конфигурация `CCiQingSetup` (три секции make/compose/improve, token loader с goods-name resolution через closure, wire-codec и query `GetNeedNumberById/GetImproveNode/GetComputeNode/RandChoise`) перенесена из `src/public/ciqing.rs` в [`shared/src/resources/ciqing.rs`](../../server/rust/shared/src/resources/ciqing.rs). Обе роли используют одну реализацию через тонкий реэкспорт прежнего файла; информационная trace-строка после decode удалена (Shared без `tracing`). Установленный экземпляр и его потребители остаются у владельцев ролей.

В этом шаге формат и ряд точных цитат заголовка сверены с машинным кодом зафиксированной пары сборок. Символьная привязка совпадает с уже заявленными адресами: `ReadSetupFile@CCiQingSetup` — секция 1:0x867f0 (VA `0x4877F0` в заголовке файла), `DeByteFromArray@CCiQingSetup` — секция 1:0xe5110 (VA `0x4E6110`). World `AddByteToArray` (тело VA `0x486080`, без публичного символа) пишет 24-байтовые make records шестью u32 подряд в layout order, затем 60-байтовые compose records в точном порядке полей `0, 4, 8, 0x10, 0x0C, 0x14` — буквально как заявлено заголовком — и размер result-vector с 8-байтовыми парами; improve records — 12-байтовые. Game decoder (VA `0x4E6110`) очищает три вектора до чтения и сохраняет только полные records, каунт-выход по JBE для внешних секций — Rust трактует отрицательное как пустое, без legacy unbounded-read. `RandChoise` (VA `0x4E4990`) подтверждён отдельным `random(0x2711)` и кумулятивным вычитанием probability по цепочке в порядке записей до `< 1`. Строка `there something wrong of CiQing.` и `Add CiQingSetup...ok!` присутствуют в соответствующих EXE. Text-loader (`label/count` секций и goods-name resolution) инструкциями в этом проходе заново не открывался.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 35,49 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.
## Shared resources: таблицы преобразования экипировки 25 сентября 2026

Две таблицы преобразования экипировки `EquipmentComposeList` (ordered `u32 → u32` map ×2, token loader с legacy-различием доступности/полноты, wire-codec, insert first-wins и query `Get*Compose`) перенесены из `src/public/equipmentcomposelist.rs` в [`shared/src/resources/equipmentcomposelist.rs`](../../server/rust/shared/src/resources/equipmentcomposelist.rs). Обе роли используют одну реализацию через тонкий реэкспорт прежнего файла; информационная trace-строка после decode удалена (Shared без `tracing`). Установленный экземпляр и его потребители остаются у владельцев ролей.

В этом шаге формат и обе особенности сверены с машинным кодом зафиксированной пары сборок: World `AddToByteArray@EquipmentComposeList` (секция 1:0x89750) пишет u32 count и пары `u32 source + u32 target` подряд для map1 (statics `0x6bf0f8..0x6bf0fc`) и map2 (`0x6bf104..0x6bf108`); строки `data\EquipmentCompose.ini` и `Load EquipmentCompose.ini...ok/failed` присутствуют в worldserver.exe. Game decoder `DecordFromByteArray@EquipmentComposeList` (секция 1:0x1c97c0) перед чтением очищает обе таблицы erase+reinit sentinel, вставляет каждую пару через `_Tree::insert`, то есть duplicate source сохраняет первое значение — как у Rust `insert_first_wins`; **функция завершается `xor al,al`, то есть исходный owner действительно возвращал `false` даже после успешной записи — заявленная заголовком особенность подтверждена инструкциями**, а reconnect caller её игнорирует. Условия чтения count decoder-а выходят по JBE (отрицательный count оригинал принял бы за огромный unsigned loop с unbounded read; Rust трактует отрицательный как пустую секцию — легитимный writer его не создаёт; соответствующее пояснение заголовка подтверждено в том же просмотре). `GetFirstCompose/GetSecondCompose` — статические методы с возвратом нуля для отсутствующего source (секции 1:0x1c9760/0x1c9790), как у Rust.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 35,49 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.
## Shared resources: дублирующие регионы 25 сентября 2026

Список дублирующих регионов `CDupliRegionSetup` (insertion-order `Vec` вместо старого `std::list`, token loader, wire-codec и `GetRandomRegion`) перенесён из `src/public/dupliregionsetup.rs` в [`shared/src/resources/dupliregionsetup.rs`](../../server/rust/shared/src/resources/dupliregionsetup.rs). Обе роли используют одну реализацию через тонкий реэкспорт прежнего файла; `thiserror` derive заменён ручными `Display`/`Error` (Shared без `thiserror`). Установленный экземпляр и его потребители (World-загрузчик и `Option<CDupliRegionSetup>` на Game) остаются у владельцев ролей.

В этом шаге формат сверен с машинным кодом зафиксированной пары сборок: оба writer — World `AddToByteArray` (секция 1:0x4f6b0) и Game-парный `AddToByteArray` (секция 1:0x228b0) — имеют идентичную структуру: u32 count, затем обход `std::list` со stride-узлом и по 8 raw байт на запись (`region_id`, `duplicate_region_id`), что подтверждает формат и insertion-order. Game decoder `DecordFromByteArray` (секция 1:0x22c20) очищает list, читает count 8-байтовыми записями — в строгом совпадении с Rust codec. `GetRandomRegion` (секция 1:0x4fa00) проверена: исходный ID кладётся во временный vector первым, затем все duplicate ID в порядке списка, и ровно один раз вызывается `random(count)` для выбора индекса — то же поведение, что у Rust `get_random_region`. Строка `setup\DupliRegionsSetup.ini` присутствует в worldserver.exe. Text loader (`*` + formatted long pairs) самими инструкциями в этом проходе не разбирался и остаётся на прежнем основании; заявленное заголовком повреждённое-чтение-префикс сохранено без переноса дефекта неинициализированной памяти.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 36,40 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.
## Порядок переноса: TimeToReturn 25 сентября 2026

Оценена принадлежность `src/setup/timetoreturn.rs` (`TimeToReturn`): это Realm-событие (World-only по заголовку, активируется календарём `CTimer` и отправляет `0x7FA13` маршрутом `game_server_number_by_region_id → send_to_map_id`). Зависимости уже разведены и выделены: `TagTime` — в [`shared/values/date.rs`](../../server/rust/shared/src/values/date.rs), `CTimer` — в [`shared/runtime/timer.rs`](../../server/rust/shared/src/runtime/timer.rs), `read_to` — `read_to_marker` в Shared resources.

Перенос в `realm/` сейчас упирается в направление сети: граница отправки требует `CMessage` со `send_to_map_id/send_to_socket` и `ServerCommandHandle` из `nets/networld` вместе с `CBaseMessage`, RLE, клиентскими/серверными очередями и сериализационным мьютексом старого пакета. Собственный 16-байтовый header с типом в `+4` принадлежит именно этому стеку направлений, а не формату события TimeToReturn; воспроизводить его локально в Realm-активности — дублирование несогласованного знания о wire-framing. Поэтому TimeToReturn отложен не по неопределённости, а по карте зависимостей: он поедет вместе с мировыми организаторскими календарями (`organizingsystem/*`, те же пользователи `CTimer`) после выделенной сетевой направленности World→Realm (`nets/networld` → компоненты Realm app/network), где `CMessage` получит своего нового владельца. Пребывание в `src/setup/` без тонкого реэкспорта оставлено сознательно.

Соответствующие этой ссылке переносы `date.rs` и `timer.rs` зафиксированы отдельными записями аудита в тот же день; проверки для этой записи — общий уже прошедший Linux `cargo check --locked --workspace --lib --bins`; новых кусков кода эта запись не вносит.
## Shared runtime: CTimer 25 сентября 2026

Calendar/interval реестр `CTimer` (generic-пара typed periodic records с PDB-layout `tagTimer`, ordered calendar events, singleton-совместимый global uid-счётчик и оба прохода `Run` — sync и async DB-owner adapter) перенесён из `src/public/timer.rs` в новый компонент [`shared/runtime/timer.rs`](../../server/rust/shared/src/runtime/timer.rs) по правилу архитектурной карты для Shared runtime (механизмы ожиданий и источники времени; у роли остаётся его экземпляр и семантика срока). Прежний `src/public/timer.rs` стал тонким реэкспортом. На границе пакета всплыл lint `async_fn_in_trait` — он подавлен известным `#[allow]`, так как trait use only внутренний, публичного контракта auto-bounds у него нет.

src/public/timer.rs → thin реэкспорт; потребители (`TimeToReturn`, организаторские календари мира) продолжают работать без изменений.

Перенос подтверждён прямыми ссылками к заявленному контракту мира: символы `CTimer` в `Nworldserver.exe + WorldServer.pdb` присутствуют полным набором (`getInstance/instance`, свободный `GetTimer`, `SetTimeEvent@CTimer` секция 1:0x62cd0, `KillTimeEvent` 1:0x627f0, `Run` 1:0x62850 и статический local `?uID@?1??GetULONGID@CTimer@@AAEKXZ@4KA` — это и есть global uid-счётчик, который Rust воспроизводит атомиком внутри модуля). Структура `Run` (секция 1:0x62850) опрошена дизассемблированием: periodic-проход с вызовом по function pointer идёт раньше calendar-прохода, событие удаляется после callback — соответствует Rust `run`/`run_with_async_handler`. PDB-layout периодической записи (восемь полей с use_ai_tick и двумя парами elapsed/start) перенесён без изменений. Сегодняшний перенос не проверял сами async DB-owner adapter-пути поверх этого реестра — они остаются у своих доменов и проверяются при их переносе.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл без предупреждений. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 34,19 секунды без предупреждений; `rustfmt` новых Shared-файлов и реэкспорта и `git diff --check` прошли (rustfmt привёл в порядок историческое отступление doc-comment строк прежнего файла, поведения не затронув). Серверы и клиент не запускались, автоматические тесты не создавались.
## Shared values: tagTime 25 сентября 2026

Календарное значение `TagTime` (восемь `u16`-полей PDB-layout, legacy-сравнения, AddDay/AddHour/AddMinute/AddSecond, GetTimeDifference и формат `year-month-day hour:minute`) перенесено из `src/public/date.rs` в [`shared/src/values/date.rs`](../../server/rust/shared/src/values/date.rs). Тонкий реэкспорт оставлен в `src/public/date.rs` для всех текущих потребителей; `chrono` добавлен зависимостью Shared с тем же constraint, что в основном пакете (`0.4.45`, `clock` + `std`, без default features); lockfile отличается на одну запись. `TagTime` нужен и Realm (WorldLifeCycle, TimeToReturn, организаторские календари), и Zone — его место в Shared values, откуда следующим шагом выедут `CTimer` и сам TimeToReturn.

Перенос подтверждён прямыми машинными ссылками к уже заявленным RVA мира: обе `dtab`-таблицы прочитаны по VA `0x0056B640` точного `Nworldserver.exe` — `0,31,28,...,31` и `0,31,29,...,31` в точном виде; `IsLeap` (RVA `0x000A36A0`) — `year % 4 == 0`, затем `% 100 != 0`, иначе `% 400 == 0`; `operator==` (RVA `0x000A3560`) сравнивает слова по смещениям `0,2,6,8,0xA,0xC` — year/month/day/hour/minute/second, без weekday и milliseconds, что подтверждает и шестипольную семантику сравнений, и 8×`u16` layout. Остальные цитируемые заголовком RVA (`constructor 0x000A3CF0`, `AddDay 0x000A36E0`, `AddHour/AddMinute/AddSecond 0x000A3990/0x000A3A10/0x000A3A90`, `GetTimeDifference 0x000A3B10`, `GetFormatStr 0x000A41B0`) остаются на прежнем основании из заголовка и этим проходом заново не открывались, как и поведение `chrono::Local` для `local_now`.

Проверка `cargo check --locked -p nebokrai-shared --lib` (Windows, после одной разрешённой перезаписи lockfile локально) прошла. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 1 минуту 23 секунды (прогрев кеша после добавления chrono в Shared) без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.
## Shared resources: настройки журналирования 25 сентября 2026

Настройки журналирования `CLogSystem` (сырой 64-байтный `tagLogSystem`, ordered set идентификаторов предметов, text loader, wire-codec и positional query-family по байтам аудита) перенесены из `src/setup/logsystem.rs` в [`shared/src/resources/logsystem.rs`](../../server/rust/shared/src/resources/logsystem.rs). Обе роли используют одну реализацию через тонкий реэкспорт прежнего файла; информационная trace-строка после decode удалена (Shared без `tracing`). Установленный экземпляр и его потребители остаются у владельцев ролей.

Прежняя осторожная пометка «у каждой роли своя инфраструктура — без чёткой wire-пары не трогать» закрыта прямой проверкой: пара существует и заголовок файла её уже фиксировал. Символы `CLogSystem` найдены в обеих PDB: у World — text `Load` (секция 1:0x98e90) и статик `_log_settings`, у Game — `DecordFromByteArray@CLogSystem` (секция 1:0xa4ab0) и `is_log_item` (1:0xa4a80) со своим статиком. Машинным кодом подтверждено: World writer (тело VA 0x499b60, без публичного символа) пишет raw 0x40 байт `_log_settings`, затем u32 count и по i32 на node в порядке set-обхода; Game decoder (VA 0x4a5ab0) читает 0x40 байт в ABI-статик, очищает set, читает signed count (выход по JLE — совпадает с `max(0)`) и вставляет i32 в ordered set, после чего немедленно передаёт байт 56 (`+0x38`) в `CDaKongXiangQian`-путь, как записано в заголовке. Строки `setup\LogSystem.ini` и `Load LogSystem.ini...OK/FAILED` присутствуют в worldserver.exe.

Правила сохранены: снимок остаётся массивом фиксированной длины с нулевым статическим default, `BTreeSet<i32>` сохраняет знаковый порядок и уникальность; positional bytes аудита (0/1/2/3/4/5/7/8/9/10/11/12/17/18/24/26/28/30/50/51..53/54..63) не менялись; count decoder читает как signed с выходом по JLE, что совпадает с семантикой `max(0)` — отдельной пометки класса JE/JBE не требуется. Text loader повреждённого хвоста этим проходом не дизассемблировался.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 37,65 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.
## Shared resources: Gods Battle configuration 25 сентября 2026

Общий Gods Battle configuration owner `CGodsBattleConf` (NPC, ordered base-money map, revise/SZL/faction-rule векторные секции, два XYD, die-back, wire-codec и SZL/XYD/NPC query-family) перенесён из `src/setup/godsbattleconf.rs` в [`shared/src/resources/godsbattleconf.rs`](../../server/rust/shared/src/resources/godsbattleconf.rs). Обе роли используют одну реализацию через тонкий реэкспорт прежнего файла; информационная trace-строка после decode удалена (Shared без `tracing`). Установленный экземпляр и его потребители (Domain Gods Battle на Game, reload/DB-цепочки на World, RSGodsBattle) остаются у владельцев ролей.

Вопрос принадлежности решён машинным кодом, а не заголовком: в worldserver.exe присутствуют `CGodsBattleConf` singleton, `LoadFile`, `AddByteToArray@CGodsBattleConf` (секция 1:0x7d990), в gameserver.exe — `CGodsBattleMgr` с `DecordFromByteArray@CGodsBattleMgr` (секция 1:0xaa260) и domain-класс `CServerGodsBattleRegion`, то есть таблица действительно парная World↔Game и место её в Shared resources, а не в чистой World-ветке.

Проверка wire writer `AddByteToArray` (worldserver VA 0x47e990) подтвердила формат: NPC records по 60 байт (u32 faction + name C-string + monsters C-string), затем u32 count; base-money ordered map — u32 count и 12-байтовые records (key не передаётся); revise vector — u32 count и 16-байтовые records; szl-level — 12 байт; faction-rule — 12 байт; два XYD u32 подряд; die-back — u32 count и 28-байтовые records. Reader `DecordFromByteArray@CGodsBattleMgr` (gameserver VA 0x4ab260) подтверждает порядок записей и семантику очистки: NPC vector и base-money map очищаются до чтения, revise и szl-level vectors очищаются, **faction-rule vector при повторном snapshot-е не очищается — records дополняют его без free/zero** (между szl-секцией и faction-rule-секцией в decoder-е нет clear-блока, при count ≤ 0 секция просто не трогается); два u32 XYD присваиваются в [this+0x60]/[this+0x64]; die-back vector очищается до чтения. Число revise-записей, отличное от 1, вызывает отдельную ветку журнала — как в Rust предупреждении о единственной записи; при пустом die-back создаётся дефолтный region-объект, как в колбэке `MissingDieBack`. Counters decoder-а выходят по signed JLE, что совпадает с семантикой `max(0)`: отдельной пометки класса JE/JBE не требуется. Строки `Save GodsBattle...start...` и семейство NPC/Belief-журналов присутствуют в worldserver.exe.

Не дизассемблировались в этом проходе: шестифайловый text/XML loader World (`LoadFile` 1:000800d0), DB-цепочка RSGodsBattle и формулы SZL (`CalAddSZL`/`CalMinSZL`/`GetPlayerSZLLev` в gameserver) — их правила остаются на прежнем уровне из заголовка файла (`принцип inclusion level band, base-money lookup по уровню жертвы, единственная revise-запись, signed x86 wrapping clamp перед wrapping addition`).

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 37,38 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.
## Shared resources: ежедневные действия LeiTing 25 сентября 2026

Таблица ежедневных действий `CThingSetup` (deque мастер-записей TID/max/point, token loader с логами, wire-codec и daily projection) перенесена из `src/setup/leitingsetup.rs` в [`shared/src/resources/leitingsetup.rs`](../../server/rust/shared/src/resources/leitingsetup.rs). Обе роли используют одну реализацию через тонкий реэкспорт прежнего файла; логи loader-а по-прежнему передаются замыканием caller-а, внешних зависимостей у модуля нет. Установленный экземпляр и его потребители остаются у владельцев ролей, включая World-цепочки DB-проекций игроков.

В этом шаге wire-формат и projection сверены с машинным кодом зафиксированной пары сборок. World writer не экспортируется публичными символами (тело VA 0x481ae0, вызывается из reload-блока `0x36` по 0x17f99): пишет u32 count (`[0x6bf038]`) и шесть raw байт на запись — TID/max/point; Rust `add_to_byte_array` с тремя u16 даёт те же байты. Game decoder `DecordFromByteArray@CThingSetup` (decoder VA 0x4db5a0) очищает deque, читает count 6-байтовыми записями в `push_back`, собирает журнальную строку с count. Daily projection `GetDailyThingList` (worldserver VA 0x481ff0) подтверждена полностью: TID ≥ 2000 входит всегда, строго `1000 < TID < 2000` — только при совпадении `(TID % 1000) % 7` с `tm_wday` от `time()` отдельно для каждого элемента; пустой результат не замещает destination (swap выполняется лишь при непустом локальном итоге). `SetDailyUpdateStamp` (VA 0x481ac0) пишет 23:59:59 в tm hour/minute/second. Указатели потока: World reload отправляет subtype `0x36`, Game decord-handling того же потока в диспетчере начальной конфигурации (call site RVA 0x9de62).

Класс расхождения `max(0)`: outer count decoder-а выходит по JBE — отрицательный count оригинал принял бы за огромный loop с чтением за пределами буфера; Rust отвечает явной ошибкой `NegativeCount` вместо пустого входа или отказа. Уточнение записано в заголовок Shared-файла. Text-loader повреждённого хвоста (последняя полная запись и нулевые остатки полей) этим проходом не дизассемблировался — основание прежнее: заголовок.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 38,11 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.
## Shared resources: тюремная конфигурация 25 сентября 2026

Тюремная конфигурация `PrisonConf` (Option PK-порог, signed-byte map `i8 → PrisonParam`, позиционный loader и wire-codec) перенесена из `src/setup/prisonconf.rs` в [`shared/src/resources/prisonconf.rs`](../../server/rust/shared/src/resources/prisonconf.rs). Обе роли используют одну реализацию через тонкий реэкспорт прежнего файла. Установленный экземпляр и его потребители остаются у владельцев ролей.

В этом шаге формат и цитируемые заголовком адреса сверены с машинным кодом зафиксированной пары сборок: `AddToByteArray@PrisonConf` (worldserver, секция 1:0x45040) пишет u32 threshold, u32 count и records byte country + u32 region + u16 x + u16 y + byte direction — десять байт без padding; `DecordFromByteArray@PrisonConf` (gameserver, секция 1:0x1c44f0) очищает map, читает threshold до count и вставляет records в том же порядке через `map::operator[]`. Тело operator[] (gameserver VA 0x4d0b60) подтверждает оба цитируемых заголовком диапазона: 0x004D0BA1..0x004D0BC8 обнуляют три DWORD (region/x/y/direction) перед вставкой для отсутствующего signed-byte ключа, а pair-copy обработчик (0x4d0560, вызываемый из operator[]) сохраняет поля без изменения; сравнение ключей — signed char (`jge`). Строки `data\PrisonConf.ini`, `PrisonConf`, `Load PrisonConf.ini...OK/FAILED` присутствуют в worldserver.exe; класс — singleton `get_inst/_inst` в обеих сборках.

Правила сохранены: loader очищает map, но при ошибке открытия сохраняет прежний PK threshold, до первой загрузки `None`; duplicate country заменяет значение; decoder публикует threshold до count и только полные records; safe short-buffer сохраняет подтверждённый partial state; `get_param` через `entry().or_default()` воспроизводит zero-fill operator[]; внутренний NUL-режим строк не затрагивается. Счётчики чтений decoder-а используют signed JLE, что совпадает с семантикой `max(0)` — отдельной пометки класса JE/JBE здесь не требуется.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 37,32 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.
## Shared resources: LingBao 25 сентября 2026

Таблица `CLingBaoSetup` (multimap name → ticket + три секции records, позиционный loader и wire-codec) перенесена из `src/setup/lingbao.rs` в [`shared/src/resources/lingbao.rs`](../../server/rust/shared/src/resources/lingbao.rs). Обе роли используют одну реализацию через тонкий реэкспорт прежнего файла; информационная trace-строка после decode удалена вместе со счётчиком (Shared без `tracing`). Установленный экземпляр и его потребители остаются у владельцев ролей.

В этом шаге формат и спорная семантика контейнера сверены с машинным кодом пары сборок (та же зафиксированная пара, что в поверке выше: `Nworldserver.exe` ↔ `WorldServer.pdb` GUID `289F1FB3-96A0-4FF4-8B5D-1FD17B50B751` age 1; `gameserver.exe` ↔ `GameServer.pdb` GUID `5BEE6DD1-BF90-49B8-8BE9-EB25C4038D53` age 2). Подтверждено: `AddByteLingBao` (worldserver, секция 1:0x7b7c0) пишет u32 count, key C-string, u32 ticket, три signed counts и секции raw по 0x20/0x14/0xC байт; `DecodeFromArrayLingBao` (gameserver, секция 1:0x1c9240) читает их же с очисткой map и 256-байтового name buffer; строка `\data\lingbao.ini` присутствует в worldserver.exe, класс — singleton `GetInstance/pInstance` в обеих сборках.

Отдельно проверена модель хранения: в PDB обеих сборок тип контейнера назван `_Tmap_traits<basic_string, stNodeInfo>` без `_Tmultimap_traits`, однако машинный код `_Insert` (gameserver VA 0x5ca150, worldserver VA 0x47da80) при равном ключе уходит вправо и вставляет новый node без выхода по совпадению — то есть вставка допускает дубликаты ключей с сохранением insertion order равных; gameplay поиск (gameserver VA 0x5c9370) поэтому ищет парой (строка ключа, ticket) сканом map. Типовое имя и поведение кода расходятся; по правилам свидетельств принято поведение машинного кода — оно совпадает с моделью `BTreeMap<Vec<u8>, Vec<LingBaoNodeInfo>>` в Rust, и утверждённый документацией порядок equal keys подтверждён. Изменений в модель переноса не потребовалось.

Класс расхождений с `max(0)`: outer count `DecodeFromArrayLingBao` (VA 0x5ca240) выходит только по JE — отрицательный count оригинал принял бы за огромный loop с чтением за пределами буфера; Rust трактует его как пустой вход. Уточнение записано в заголовок Shared-файла; семантических счётчиков секций это не касается, легитимный wire их отрицательными не делает. Внутренние loader-ветки повреждённого хвоста (последняя полная запись) этим проходом не дизассемблировались и остаются на прежнем основании из заголовка. Поток snapshot: subtype `0x35`, payload после CiQing (`continue_game_server_ciqing_ling_bao_configuration`) — машинная сторона диспетчера не проверялась здесь.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 38,55 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.
## Shared resources: порог honor 25 сентября 2026

Конфигурация `HonorElimilateConfig` (два i32, позиционный loader и wire-codec) перенесена из `src/setup/honorelimilateconfig.rs` в [`shared/src/resources/honorelimilateconfig.rs`](../../server/rust/shared/src/resources/honorelimilateconfig.rs). Обе роли используют одну реализацию через тонкий реэкспорт прежнего файла; `thiserror` derive заменён ручными `Display`/`Error` (Shared не зависит от `thiserror`). Установленный экземпляр и его потребители остаются у владельцев ролей.

В этом шаге формат сверен с машинным кодом пары сборок (`Nworldserver.exe` SHA-256 `f3ac454d…6ef1` ↔ `WorldServer.pdb` GUID `289F1FB3-96A0-4FF4-8B5D-1FD17B50B751` age 1, `gameserver.exe` SHA-256 `4f5c98e0…2c80e` ↔ `GameServer.pdb` GUID `5BEE6DD1-BF90-49B8-8BE9-EB25C4038D53` age 2; пары подтверждены по CodeView RSDS GUID+age): `HonorElimilateConfig::AddToByteArray` (worldserver, секция 1:0x89560) пишет два u32 подряд, `DecordFromByteArray` (gameserver, секция 1:0xf73e0) присваивает их последовательно без проверок — обрыв второго поля сохраняет первое, как и у безопасного Rust-чтения позиционным курсором; helper `0x4a3340` — 4-байтовая запись. Строки `data\HonorElimilate.ini`, `HonorElimilate`, `Load HonorElimilate.ini Config...` присутствуют в worldserver.exe; класс — singleton `GetInstance/Instance` в обеих сборках. Loader `LoadConfig` (секция 1:0x895d0) возвращает неуспех при отказе открытия до любого присваивания полей; дальнейшее — две позиционные пары istream-extraction, что соответствует документированному «присвоенное поле сохраняется». Семантика записей рейтингов honor (subtype 0x27..0x2A) этим шагом не проверялась.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 38,17 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.

## Shared resources: машинная поверка ранних переносов 25 сентября 2026

Ретроспективная поверка 13 уже перенесённых setup-/resource-таблиц (коммиты от `74cc751b1` monsterlist до `079d158d1` goodsdestructionconfig): переносы когда-то опирались на эталонные заголовки, теперь wire-форматы каждой сверены с машинным кодом обеих сборок, а имена сборок зафиксированы формально: `Nworldserver.exe` SHA-256 `f3ac454daf83e7e9c8f844c725be2c5a24efa946c27d75319cfcb68a2f466ef1`, ImageBase `0x400000`, PE timestamp `0x53fb128f`, ↔ `WorldServer.pdb` GUID `289F1FB3-96A0-4FF4-8B5D-1FD17B50B751` age 1 (совпадает с `original/server/Miracle_server/WorldServer/` по хешам; в том же комплекте есть две другие world-сборки — `worldserver.exe` и `worldserver (2).exe` с иными хешами, они для свидетельств не используются); `gameserver.exe` SHA-256 `4f5c98e0fdf6147d8aecf55f7937aaf6e2cf5e4f5a2c44491a6359228762c80e`, ImageBase `0x400000`, timestamp `0x53faff2d`, ↔ `GameServer.pdb` GUID `5BEE6DD1-BF90-49B8-8BE9-EB25C4038D53` age 2. Соответствие EXE↔PDB — по CodeView RSDS GUID+age. Символы взяты из публичного потока PDB (S_PUB32), у всех 13 классов найден `AddToByteArray` в worldserver и парный `DecordFromByteArray` в gameserver; дизассемблирование — capstone по RVA из PDB, helpers worldserver установлены по циклам записи: `0x4a3340` — u32, `0x4a33a0` — u16, `0x4a3400` — u8, `0x4a3280` — C-строка (байты по strlen + завершающий NUL), `0x4a3220` — raw N байт.

Проверенные wire-факты (обе стороны, VA из секции 1): synthesis — broadcast map, затем recipes; в recipe u16 вероятности раньше u16 типа, хотя C++ layout обратный (`0x48d560`/`0x5c31b0`); order формула-записей 8 байт; changebody — i32 count + u32 items, decoder сразу очищает vector (`0x43e610`/`0x4c5dc0`); goodsdestruction — u32 enabled из byte, u16 types, C-строки имён, decoder чистит обе lists и читает enabled как `u32 != 0` (`0x43f810`/`0x5c0e80`); regionsetup — count и 12-байтные записи, ключ map не передаётся (`0x497b90`/`0x4f01e0`); contributesetup — 11 u32 scalars затем записи `u32 lo/hi/count + name\0` (`0x492ee0`/`0x4eb490`); gmlist — два map `name → u32 info` и trailing C-строка паспорта, decoder чистит оба map (`0x4987d0`/`0x4dee90`); tradelist — trades с C-строкой имени и 8-байтными goods-записями (`0x49a860`/`0x5c07c0`); incrementshoplist — u8 key, 24-байтный prefix, две C-строки, trailing affiche (`0x43ba60`/`0x422490`); fairyexp — один VA serializer/decoder для `CBattleFairyExpConfig` и наследника `CFairyExpConf` (`0x449120`/`0x5c7800`, наследование подтверждено совпадением адресов), key u32 + signed count + u32 values; key создаётся только при первом value; newskillmonster — u32 skill ID + signed count + C-строки, временный vector группы с заменой map entry (`0x43cb00`/`0x5c46b0`); preciousbox — box key + odds vector, 17 значимых байт item из 20-байтной C++ записи (4×u32 + u8), trailing byte `!= 0` (`0x446220`/`0x5c7030`); monsterlist — 160-байтовый block, две C-строки, 6-байтовые записи списка, drop-записи C-строка + 32 байта (`0x49c760`/`0x470200`); globesetup — raw blob `0x1114` байт writer и reader (`0x433470`/`0x41e4a0`); regionrouter — 6 u32 + count + 12-байтные route-записи, decoder очищает global map (`0x4b24d0`/`0x4a5750`).

Найденный и зафиксированный класс расхождений: decoders `CBattleFairyExpConfig` и `CNewSkillMonserConf` выходят из циклов по JE/JBE, поэтому отрицательный signed count оригинал принял бы за огромный unsigned loop с чтением за пределами буфера; Rust трактует его как пустой вход (`max(0)`), что записано в заголовках соответствующих Shared-файлов. У остальных таблиц decoder выходит по signed JLE, что совпадает с семантикой `max(0)`. Это расхождение существует только на повреждённом/враждебном входе; легитимный wire World никогда не пишет отрицательные counts, и жёсткий отказ оригинала является тем же классом небезопасного чтения, что и заменяемый безразмерный `_GetStringFromByteArray`. Претензий к коду переносов по результатам поверки нет; попутно заголовки двух Shared-файлов дополнены этим уточнением. Не покрыты поверкой: позиционные лоадеры/XML-парсеры ролей World (кроме honor `LoadConfig` выше) и gameplay-потребители таблиц; их правила остаются на прежнем уровне основания из заголовков соответствующих файлов.

Проверки выполнялись локальным read-only инструментарием (PE/MSF-PDB парсер, S_PUB32, capstone) над указанными артефактами; дизассемблированные фрагменты и кэш символов сохранены локально вне Git. Серверы и клиент не запускались, автоматические тесты не создавались. Cargo-проверки для этой записи — те же, что в записи порога honor выше (одно дерево изменений).

## Shared resources: уничтожение предметов 25 сентября 2026

Правила уничтожения предметов `GoodsDestroySetup` (enabled, ordered goods types и original names, позиционный loader и wire-codec) перенесены из `src/setup/goodsdestructionconfig.rs` в [`shared/src/resources/goodsdestructionconfig.rs`](../../server/rust/shared/src/resources/goodsdestructionconfig.rs). Обе роли используют одну реализацию через тонкий реэкспорт прежнего файла; информационная trace-строка после decode удалена вместе с зависимостью Shared от `tracing`. Установленный экземпляр и его потребители остаются у владельцев ролей; `load_from_file` без живых call sites сохранён как клон оригинального `LoadFromFile`. Правила сохранены: wire пишет `u32 enabled`, signed counts, `u16` goods types и NUL-terminated original names; loader очищает оба vectors, но при ошибке открытия сохраняет enabled; после `#` идут `* label u16`, после `<end>` — независимые `+ name`; файл без `#` успешен с пустыми vectors и прежним enabled; Game decoder очищает оба списка и публикует enabled и каждый полный элемент по мере чтения.

Происхождение подтверждено заголовком: точные `worldserver.exe + worldserver.pdb` и `gameserver.exe + GameServer.pdb`, исходный owner `setup/goodsdestructionconfig.h/.cpp`. Новых свидетельств оригинального поведения в этом шаге не добавлено; доставка правил в работающий Game и загрузка INI World этим переносом не проверялись.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 38,39 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.

## Shared resources: ограничения смены тела 25 сентября 2026

Ограничения смены тела `CChangeBodyConf` (vector ограничений goods, XML-loader и wire-codec) перенесены из `src/setup/changebody.rs` в [`shared/src/resources/changebody.rs`](../../server/rust/shared/src/resources/changebody.rs). Обе роли используют одну реализацию через тонкий реэкспорт прежнего файла; `thiserror` derive заменён ручными `Display`/`Error` (Shared не зависит от `thiserror`). Установленный экземпляр и его потребители остаются у владельцев ролей. Правила сохранены: loader очищает vector до открытия XML, принимает только direct `Goods` children `RestrictionsGoodsList`, missing `index` очищает результат, diagnostics сохраняют StringTable IDs `GS1148..1151`; wire пишет signed count и `u32` items; Game decoder немедленно очищает vector и сохраняет каждый полный `u32`, safe short-buffer оставляет decoded prefix.

Происхождение подтверждено заголовком: точные `worldserver.exe + worldserver.pdb` и `gameserver.exe + GameServer.pdb`, исходный owner `setup/changebody.h/.cpp`. Новых свидетельств оригинального поведения в этом шаге не добавлено; доставка ограничений в работающий Game и загрузка XML World этим переносом не проверялись.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 37,85 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.

## Shared resources: синтез 25 сентября 2026

Таблица синтеза `CSynthesis` (ordered broadcast map, recipes/formulas, XML-loader, wire-codec и query-family `Get*`) перенесена из `src/setup/synthesis.rs` в [`shared/src/resources/synthesis.rs`](../../server/rust/shared/src/resources/synthesis.rs). Обе роли используют одну реализацию через тонкий реэкспорт прежнего файла. Правила сохранены: wire передаёт broadcast map, затем recipes; в recipe probability идёт раньше type (парный decoder ожидает именно этот порядок); loader очищает recipes, но сохраняет прежний broadcast map, decoder, напротив, очищает оба; recipe публикуется лишь после полного временного formula-vector; ошибка позднего Item не откатывает ранние Broadcast.

Происхождение подтверждено заголовком: точные `worldserver.exe + worldserver.pdb` и `gameserver.exe + GameServer.pdb`, исходный owner `setup/synthesis.cpp`. Новых свидетельств оригинального поведения в этом шаге не добавлено; доставка таблицы в работающий Game и загрузка XML World этим переносом не проверялись.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 38,87 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.

## Shared resources: precious box 25 сентября 2026

Конфигурация `PreciousBoxConf` (ordered box/odds groups, XML-loader, wire-codec и `random_item`) перенесена из `src/setup/preciousboxconf.rs` в [`shared/src/resources/preciousboxconf.rs`](../../server/rust/shared/src/resources/preciousboxconf.rs). Обе роли используют одну реализацию через тонкий реэкспорт прежнего файла. Установленный экземпляр и его потребители остаются у владельцев ролей. Правила сохранены: loader публикует records только в `_box_conf`, тогда как serializer читает `_box` (после reload subtype `0x1E` остаётся пустым), decoder очищает только `_box` и публикует box после полного разбора его временного odds-vector, 17 значимых bytes item без трёх padding, один roll `[0, 10000)` и half-open odds range с равномерным item и inclusive level.

Происхождение подтверждено заголовком: точные `worldserver.exe + worldserver.pdb` и `gameserver.exe + GameServer.pdb`, исходный owner `server/setup/preciousboxconf.h/.cpp`. Новых свидетельств оригинального поведения в этом шаге не добавлено; доставка конфигурации в работающий Game и загрузка XML World этим переносом не проверялись.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.

## Shared resources: списки монстров новых навыков 25 сентября 2026

Списки монстров новых навыков `CNewSkillMonserConf` (ordered группы skill ID → локализованные имена, XML-loader, wire-codec) перенесены из `src/setup/newskillmonsterlist.rs` в [`shared/src/resources/newskillmonsterlist.rs`](../../server/rust/shared/src/resources/newskillmonsterlist.rs). Обе роли используют одну реализацию через тонкий реэкспорт прежнего файла. Правила сохранены: duplicate skill ID заменяет всю группу, Game decoder собирает группу во временный vector и лишь затем заменяет map entry, StringTable miss даёт пустое имя, signed counts, history quoted/unquoted normalization attributes через `quick-xml` той же версии.

Происхождение подтверждено заголовком: точные `worldserver.exe + worldserver.pdb` и `gameserver.exe + GameServer.pdb`, исходный owner `setup/newskillmonsterlist.h/.cpp`. Новых свидетельств оригинального поведения в этом шаге не добавлено; доставка списков в работающий Game и загрузка XML World этим переносом не проверялись.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.

## Shared resources: таблицы опыта fairy и battle fairy 25 сентября 2026

Общие таблицы опыта `CBattleFairyExpConfig` (ordered owner-level map, XML-loader через `quick-xml`, wire-codec) и наследуемый `CFairyExpConf` перенесены из `src/setup/` в [`shared/src/resources/cbattlefairyexpconfig.rs`](../../server/rust/shared/src/resources/cbattlefairyexpconfig.rs) и [`fairyexpconf.rs`](../../server/rust/shared/src/resources/fairyexpconf.rs). Обе роли используют одну реализацию; `quick-xml` добавлен зависимостью Shared с той же версией, прежние `src/setup/*` стали тонкими реэкспортами. Правила сохранены: duplicate level или неполная группа очищает всю map, минимум `MaxLevel - 1` значений, Game decoder создаёт key только при первом exp value и дописывает повторную wire-группу, signed counts.

Происхождение подтверждено заголовками: точные `worldserver.exe + worldserver.pdb` и `gameserver.exe + GameServer.pdb`, исходные owners `setup/cbattlefairyexpconfig.*` и `setup/fairyexpconf.*`. Новых свидетельств оригинального поведения в этом шаге не добавлено; доставка таблиц в работающий Game и загрузка XML World этим переносом не проверялись. Изменение сборочной границы ограничено добавлением `quick-xml 0.39.2` в зависимости Shared; версии остальных пакетов в lockfile не менялись.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл без предупреждений; `rustfmt` новых Shared-файлов и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.

## Shared resources: ограничения регионов 25 сентября 2026

Ограничения регионов `CRegionSetup` (записи ID/уровень/вклад, текстовый loader и двоичный wire-codec) перенесены из `src/setup/regionsetup.rs` в [`shared/src/resources/regionsetup.rs`](../../server/rust/shared/src/resources/regionsetup.rs). Обе роли используют одну реализацию; прежний `src/setup/regionsetup.rs` стал тонким реэкспортом. Правила сохранены: owner очищается до чтения, при некорректной записи сохраняется уже прочитанный префикс, wire пишет signed count и 12-байтные записи, ключ BTreeMap задаёт порядок и отдельно не сериализуется.

Происхождение подтверждено заголовком: точные `worldserver.exe + worldserver.pdb` и `gameserver.exe + GameServer.pdb`, исходный owner `setup/regionsetup.cpp`; семантика результата файла загрузчика прежним владельцем отмечена как неподтверждённая. Новых свидетельств в этом шаге не добавлено; доставка записей в работающий Game и загрузка World этим переносом не проверялись.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 34,10 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.

## Shared resources: increment shop 25 сентября 2026

Общая таблица `CIncrementShopList` (записи по страницам, affiche, loader и wire-codec) перенесена из `src/setup/incrementshoplist.rs` в [`shared/src/resources/incrementshoplist.rs`](../../server/rust/shared/src/resources/incrementshoplist.rs). Обе роли используют одну реализацию; прежний `src/setup/incrementshoplist.rs` стал тонким реэкспортом. Правила сохранены: 24-байтный item prefix с обнулённым padding, равные page keys сохраняют insertion order, невалидный основной/discount goods и пустые description/name завершают загрузку, overlap меньше 1 становится 1, поздний отказ оставляет уже добавленные items, signed count.

Происхождение подтверждено заголовком: точные `worldserver.exe + worldserver.pdb` и `gameserver.exe + GameServer.pdb`, исходный owner `setup/incrementshoplist.cpp`. Новых свидетельств оригинального поведения в этом шаге не добавлено; доставка таблицы в работающий Game и загрузка World этим переносом не проверялись. Рабочий обмен Billing с таблицей описан на [странице торговли](../gameplay/trade.md#increment-shop).

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 35,96 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.

## Shared resources: country contribution 25 сентября 2026

Общая таблица `CContributeSetup` (одиннадцать scalars, позиционный loader и wire-codec) перенесена из `src/setup/contributesetup.rs` в [`shared/src/resources/contributesetup.rs`](../../server/rust/shared/src/resources/contributesetup.rs). Обе роли используют одну реализацию; прежний `src/setup/contributesetup.rs` стал тонким реэкспортом. Порядок сохранён: loader очищает только items, одиннадцать scalars при ошибке открытия сохраняются и обновляются позиционно, malformed value оставляет уже прочитанный префикс, wire пишет scalars и records `u32 lo/hi/count + name\0`.

Происхождение подтверждено заголовком: точные `worldserver.exe + worldserver.pdb` и `gameserver.exe + GameServer.pdb`, исходный owner `setup/contributesetup.cpp`. Новых свидетельств оригинального поведения в этом шаге не добавлено; доставка таблицы в работающий Game и загрузка World этим переносом не проверялись.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 35,55 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.

## Shared resources: список операторов GM 25 сентября 2026

Общие операторы `CGMList` (два map `name → GmInfo`, god passport, loader и wire-codec) перенесены из `src/setup/gmlist.rs` в [`shared/src/resources/gmlist.rs`](../../server/rust/shared/src/resources/gmlist.rs). Обе роли используют одну реализацию; прежний `src/setup/gmlist.rs` стал тонким реэкспортом. Исходный god passport `@^$^#SDFSDslfld/$dsl2a` и правила (signed count, внутренний NUL в строке блокирует append до изменения destination, byte-порядок ключей) сохранены без изменения.

Происхождение подтверждено заголовком: точные `worldserver.exe + worldserver.pdb` и `gameserver.exe + GameServer.pdb`, исходный owner `setup/gmlist.cpp`. Новых свидетельств оригинального поведения в этом шаге не добавлено; доставка списка в работающий Game и загрузка World этим переносом не проверялись. Разбор GM-допуска в игровых обработчиках (`gmmessage`) остаётся в переходном Game.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.

## Shared resources: торговые списки NPC 25 сентября 2026

Общие торговые списки `CTradeList` (записи NPC и товара, loader, wire-codec и lookup) перенесены из `src/setup/tradelist.rs` в [`shared/src/resources/tradelist.rs`](../../server/rust/shared/src/resources/tradelist.rs). Обе роли используют одну реализацию: World загружает и сериализует, Game принимает и ищет по имени NPC; прежний `src/setup/tradelist.rs` стал тонким реэкспортом. Алгоритм не менялся: очистка карты до разбора, duplicate NPC заменяет список, публикация NPC только после полного goods-list, сужение чисел до byte и byte-лексикографический порядок `BTreeMap`.

Происхождение подтверждено заголовком: точные `worldserver.exe + worldserver.pdb` и `gameserver.exe + GameServer.pdb`, исходный owner `setup/tradelist.cpp`. Новых свидетельств оригинального поведения в этом шаге не добавлено; доставка списков в работающий Game и загрузка текстов World этим переносом не проверялись. Основания — в [магазине](../gameplay/trade.md#магазин-npc-товар-цена-и-необратимый-порядок).

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.

## Shared resources: глобальный setup и маршрутизатор регионов 25 сентября 2026

`CGlobeSetup` (0x1114-байтный snapshot, typed accessors, decode/loader) и `CRegionRouter` (маршрутизация переходов) перенесены из `src/setup/` в [`shared/src/resources/globesetup.rs`](../../server/rust/shared/src/resources/globesetup.rs) и [`shared/src/resources/regionrouter.rs`](../../server/rust/shared/src/resources/regionrouter.rs). Обе роли используют одну реализацию: World загружает тексты и сериализует, Game принимает и хранит экземпляры; прежние `src/setup/*` стали тонкими реэкспортами. World JJC получил локальный adapter `GlobeSetupJjcWorldConfig` поверх нового Shared view `jjc_run_config_fields`, не зависящего от типа старого владельца. Счёт дублей router теперь возвращается из Game-декодера и журналируется обработчиком вместо скрытого trace библиотеки.

Происхождение сохранено заголовками: сериализатор/декодер World/Game EXE/PDB, исходники `server/setup/globesetup.cpp/.h` и `regionrouter.cpp/.h`. Алгоритмы не менялись: очистка router до count, first-wins обоих map-insert, очистка snapshot и typed offsets читаются из того же сырья. Новых свидетельств оригинального поведения в этом шаге не добавлено; доставка snapshot/router Game и загрузка текстов World этим переносом не проверялись.

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл без предупреждений; `rustfmt` новых Shared-файлов и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.

## Shared resources: общий формат обмена монстров 25 сентября 2026

Формат `CMonsterList` (типы, реестры, текстовый загрузчик, сериализатор/декодер и lookup) перенесён из `src/setup/monsterlist.rs` в [`shared/src/resources/monsterlist.rs`](../../server/rust/shared/src/resources/monsterlist.rs). Обе роли используют одну реализацию: World сериализует и загружает тексты, Game принимает реестр и хранит экземпляр полем; прежний `src/setup/monsterlist.rs` стал тонким реэкспортом. Это перенос собственной реализации без изменения алгоритмов: порядка записей, очистки реестров, last-write-wins, line/NUL-правила строк и кодировки полей.

Происхождение подтверждено заголовком прежнего файла и использованием парными владельцами: сериализатор World — `worldserver.exe/.pdb`, декодер Game — `gameserver.exe/.pdb`, исходник `server/setup/monsterlist.h/.cpp`. Новых свидетельств поведения в этом шаге не добавлено; runtime-доставка реестра Game и загрузка текстовых ресурсов World этим переносом не проверялись. Основания — в [реестре монстров](../gameplay/npc-ai.md#реестр-определений-монстров).

`cargo check --locked -p nebokrai-shared --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 34,33 секунды без предупреждений; `rustfmt` нового Shared-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.

## Zone content: полученный кэш runtime-свойств навыков 25 сентября 2026

Изменяемый кэш свойств навыков — `CSkillBaseProperties`, composite key `id << 16 | level & 0xffff`, двоичный кодекс `rebuild` и реестр — перенесён в [`zone/content/skills.rs`](../../server/rust/zone/src/content/skills.rs). Переходный Game делегирует каталог через тонкую обёртку `CSkillFactory`; статический реестр владельцев `SkillOwner` и их политик остаётся в Game как диспетчер исполнения, без второй копии набора. Запросы свойств, типа, имени и предикаты делегированы Zone; `supports_skill_id`/свободные константы цвета и war-soul/float вычисляются из общих Zone-функций и прежнего реестра.

По совпадающей паре Game EXE/PDB ранее подтверждён `Rebuild` RVA `0x0006CE10` (очистка до count, signed-проход, пропуск нулевой длины, malformed-record skip с публикацией разобранного префикса, last-write-wins ключей); сериализатор WorldServer — парная сторона. Этот перенос не меняет порядок: поле `skill_factory` в Game делегирует тот же алгоритм через идентичный код, вынесенный в Zone. Основания — в [навыках](../gameplay/skills.md#конфигурация-регистрация-и-команда).

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 33,49 секунды без предупреждений; `rustfmt` нового Zone-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; доставка кэша в работающий Game и запросы свойств этим шагом не проверялись.

## Zone effects: данные и записи сценарных состояний AddState 24 сентября 2026

Payload и записи семи состояний `CMoveShape::AddState` перенесены в [`zone/effects/scriptstate.rs`](../../server/rust/zone/src/effects/scriptstate.rs): пять UseGoods (`100007`–`100012`), ImproveExp (`100009`) и AutoProtect (`110000`). Переходный Game сохраняет живые Begin/restart/AI/End и GM-gate; property-формулы остаются у файлов-владельцев, диспетчер по виду — в адаптере.

По совпадающей паре Game EXE/PDB (SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`) vtable семи классов подтверждают: AutoProtect (`0x0065E00C`) — собственный End `0x005D44E0`, 8-байтная запись `0x005F51E0`; пять UseGoods — End `0x005D5B80`, getter `0x005F2CD0`, 12-байтная запись `0x005D4D10`; ImproveExp — getter `0x005D5F30`, AI `0x005D60B0`, 12-байтная запись `0x005E7330`; readers `0x004F9D80`/`0x005D6190`/`0x005EAAC0` читают clock после внешнего ID. Исправления Rust: decode читает часы после проверки ID вместо расхода до входа; дублирующие константы ID в файлах формул заменены общими Zone, неиспользуемый переходный реэкспорт `timed_client_state_time` удалён. Основания — в [описании сценарных состояний](../gameplay/attributes-and-states.md#сценарные-состояния-addstate).

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 34,65 секунды без предупреждений; `rustfmt` нового Zone-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; фактические property-эффекты не проверялись.

## Zone effects: данные и запись TeamState 24 сентября 2026

Данные и запись переменной длины `CTeamState` (`0x186A6`) перенесены в [`zone/effects/teamstate.rs`](../../server/rust/zone/src/effects/teamstate.rs). Переходный Game сохраняет живые Begin/restart/AI/End и пакеты.

По совпадающей паре Game EXE/PDB (SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`) инструкции подтверждают: конструктор `0x005BFE60` (ID `0x186A6`, stamp 0, без часов), Serialize `0x005BFA50` (ID и две C-строки, без часов), GetAdditionalData `0x005BFDD0` (бит 16 по ненулевой длине пароля, живой счёт plugs), AI `0x005BFD20` (unsigned `last + 5000 <= now`). Основания — в [описании набора](../gameplay/attributes-and-states.md#teamstate-наборное-состояние-команды). Поведение Rust при переносе не менялось.

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 36,40 секунды без предупреждений; `rustfmt` нового Zone-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; фактический цикл набора не проверялся.

## Zone effects: данные и запись восстановления от предметов 24 сентября 2026

Данные, таймер и 16-байтная запись `CRestoreHpState` (`100000`) и `CRestoreMpState` (`100001`) перенесены в [`zone/effects/consumablerestore.rs`](../../server/rust/zone/src/effects/consumablerestore.rs) вместе с enum и интервалами применения. Переходный Game сохраняет живые Begin/restart/AI/End и применение ресурса.

По совпадающей паре Game EXE/PDB (SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`) инструкции подтверждают: конструкторы `0x004F8410`/`0x004F87B0` (поля из аргументов, ID `0x186A0`/`0x186A1`, без часов), vtable `0x0065355C`/`0x006535BC` с общими Serialize `0x005F65F0`, Unserialize `0x005EEC70`, getter `0x005F2CD0` и additional `0x00601200`. Исправление Rust: decode читает часы после проверки ID вместо расхода до входа. Основания — в [описании восстановления](../gameplay/attributes-and-states.md#восстановление-от-расходуемых-предметов).

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 34,59 секунды без предупреждений; `rustfmt` нового Zone-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; фактические тики не проверялись.

## Zone effects: данные и 124-байтная запись ChangeBody 24 сентября 2026

Данные и запись `CHBYState` (`0x37`) перенесены в [`zone/effects/changebody.rs`](../../server/rust/zone/src/effects/changebody.rs); Game получил тонкую `change_body_state_from_factory`, живые Begin/restart/End, visual, навыки и hotkeys остаются в `chbystate.rs`.

По совпадающей паре Game EXE/PDB (SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`) машинный код подтверждает: конструктор `0x005DAC90` (ID без часов), vtable `0x0065E41C` с AI `0x005DAAA0`, End `0x005DA240`, getter `0x005DA030`, Serialize `0x005DA080` (остаток в живой keep, затем блок 0x78) и Unserialize `0x005DA0C0` (clock до блока, затем online=true). Исправление Rust: decode читает часы после проверки ID вместо получения снятого фабрикой значения. Основания — в [описании преображения](../gameplay/attributes-and-states.md#changebody-преображение-игрока).

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 34,08 секунды без предупреждений; `rustfmt` нового Zone-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; фактическое преображение не проверялось.

## Zone effects: данные и записи Ex/ExNew 24 сентября 2026

Данные, записи 40/52 байта и кодек `CExState` (`0x32`) / `CExStateNew` (`0x33`) перенесены в [`zone/effects/extended.rs`](../../server/rust/zone/src/effects/extended.rs); Game получил тонкую `extended_state_from_factory`, живые Add/Del/Begin/End и periodic use_item остаются у moveshape/notdisappearafterdead-путей.

По совпадающей паре Game EXE/PDB (SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`) машинный код подтверждает: конструкторы `0x005D9230`/`0x005D9990` без часов, Serialize `0x005D9510`/`0x005D9BB0` (остаток в живой keep через getter, затем блок), Unserialize `0x005D9550`/`0x005D9BF0` (clock до блока; New — в оба timestamp), AI `0x005D94E0`/`0x005D9FB0`. Различие getter-ов Original (`0x005DA030`, общий с CHBYState) и New (`0x005D6320`) вынесено в Zone `time.rs` как `change_body_client_state_time`/`guarded_client_state_time`; переходный `state.rs` делегирует. Исправление Rust: decode читает часы после проверки ID и уровня вместо расхода до входа. Основания — в [описании extended-state](../gameplay/attributes-and-states.md#exexnew-обычные-extended-state).

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 34,23 секунды без предупреждений; `rustfmt` новых Zone-файлов и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; фактические Begin/End и расход предмета не проверялись.

## Zone effects: данные и 76-байтная запись Undead 24 сентября 2026

Данные, кодек и тик-правила `CNotDisappearAfterDead` (внешний ID `0x38`) перенесены в [`zone/effects/undead.rs`](../../server/rust/zone/src/effects/undead.rs); фабрика навыков остаётся в Game через тонкую `undead_state_from_factory`, живые Begin/AI/End и visual — у `notdisappearafterdead` и moveshape.

По совпадающей паре Game EXE/PDB (SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`) машинный код подтверждает ранее описанные адреса: Serialize `0x005D64F0` (остаток getter-ом записывается в живой keep, затем 72-байтный блок), Unserialize `0x005D6530` (один clock на оба timestamp, затем блок), GetRemainedTime `0x005D6320` (без clock при нулевом сроке). Последний вариант вынесен в `zone/effects/time.rs` как `guarded_client_state_time`, общий с ExtendedState. Исправления Rust: decode читает часы после проверки ID вместо расхода до входа; обращения AI-тиков к полям заменены операциями Zone. Основания — в [описании Undead](../gameplay/attributes-and-states.md#undead-сохраняемый-набор-баффа).

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 35,86 секунды; `rustfmt` новых Zone-файлов и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; фактический бафф и тик предмета не проверялись.

## Zone effects: данные и запись ParticularState 24 сентября 2026

Данные и 8-байтная запись `CParticularState` (`0x186A5`) перенесены в [`zone/effects/particular.rs`](../../server/rust/zone/src/effects/particular.rs). Переходный Game сохраняет живые Begin/restart/AI/End и проверку товара у игрока.

По совпадающей паре Game EXE/PDB (SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`) инструкции подтверждают: конструктор VA `0x004F9440` (additional из аргумента, vtable `0x00653684`), Serialize VA `0x005E23D0` и Unserialize VA `0x00601350` (ID + additional без часов). Основания — в [описании товарного сторожа](../gameplay/attributes-and-states.md#particular-товарный-сторож). Поведение Rust при переносе не менялось.

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 35,94 секунды без предупреждений; `rustfmt` нового Zone-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; фактическая работа сторожа не проверялась.

## Zone effects: данные и кодек RideState 24 сентября 2026

Данные и wire-кодек `CRideState` (ID `100004`) перенесены в [`zone/effects/ride.rs`](../../server/rust/zone/src/effects/ride.rs). Переходный Game сохраняет живые Begin/End, visual и разрешение участников; переменная запись `ID/type/level/roleLimit/goodsName\0` и goods-check gate стали библиотечными.

По совпадающей паре Game EXE/PDB (SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`) инструкции подтверждают: Serialize VA `0x004F8F60` (четыре DWORD и C-string, без часов), safe Unserialize VA `0x004F93B0` (три DWORD и NUL в пределах стекового буфера), отсутствие обновления timestamp в AI. Основания — в [ездовом состоянии](../gameplay/movement.md#ездовое-состояние). Поведение Rust при переносе не менялось.

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 34,59 секунды без предупреждений; `rustfmt` новых Zone-файлов и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; фактическое оседлование и клиентский visual не проверялись.

Рядом с шагом RideState рабочее дерево получило форматную нормализацию `rustfmt` тринадцати ранее существовавших файлов `zone/effects/` (запущенный на `mod.rs` rustfmt обработал дочерние модули). Изменение только форматирующее, поведение не меняет; оно отложено в рабочем дереве и вошло отдельным коммитом `226f7fab4` после шага ParticularState. Обе Linux-проверки шага RideState выполнялись уже на нормализованном дереве.

## Zone effects: диспетчер PreDefense и стихийная формула Promotion 24 сентября 2026

Диспетчер вариантов щитов `DefenseShieldState` (enum, выбор источника MP, правило пропуска ID) перенесён в [`zone/effects/defenseshield.rs`](../../server/rust/zone/src/effects/defenseshield.rs); стихийная формула Promotion — в [`zone/effects/promotion.rs`](../../server/rust/zone/src/effects/promotion.rs) как `promotion_element_attack`. Переходный Game сохраняет живые Begin/restart/AI/End щитов и вызовы из `fightdefense`.

По совпадающей паре Game EXE/PDB (SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`) в `CFightDefense::PreDefense` подтверждены: диспетчер пропуска VA `0x005B0A50–0x005B0A72` (`jl 0x212`, `jg 0x221`, `jne 0x220` — пропуск ID `530..=545` кроме `544`) и стихийная ветвь VA `0x005B0C80–0x005B0CD3` (ID `0x142`, `kind == 3`, WORD `+0x3C`, `FIMUL` урона, `FMUL` f32-константы `0.001` из `0x64E9FC`, `FISTP`). **Исправлено расхождение**: ветвь игрока в Rust использовала `f64`-литерал `0.001` вместо загружаемой оригиналом `f32`-константы; теперь обе ветви делят `f64::from(0.001_f32)`. Дубль правила пропуска между `shieldstate` и `fightdefense` устранён общим предикатом Zone. Основания — в [бое](../gameplay/combat.md) и [описании Promotion](../gameplay/attributes-and-states.md#promotion-усиление-атаки-и-лечения).

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 33,63 секунды без предупреждений; `rustfmt` новых Zone-файлов и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; фактический удар со щитами не проверялся.

## Zone effects: данные и запись BossBlueFury 24 сентября 2026

Данные, срок слабой фазы и 12-байтная запись `CBossBlueFuryState` (`0x1F7`) перенесены в [`zone/effects/bossbluefury.rs`](../../server/rust/zone/src/effects/bossbluefury.rs). Переходный Game сохраняет живые restart/AI/End, запреты и пересчёт монстра.

По совпадающей паре Game EXE/PDB (SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`) машинный код подтверждает ранее описанные адреса: конструктор `0x005E8A60` (три поля без часов, ID `0x1F7`), vtable `0x0065F994` с AI `0x005E8D50`, End `0x005E8D10`, Restart `0x005FD450`, OnUpdateProperties `0x005E8DC0`, getter `0x005D5F30`, Serialize `0x005E7330`, Unserialize `0x005D6190`. Инструкции AI подтверждают две раздельные проверки часов (слабая и общая границы) и снятие запретов на каждом проходе после слабой. Исправление Rust: decode читает часы после проверки ID вместо расхода до входа. Основания — в [описании ярости](../gameplay/attributes-and-states.md#bossbluefury-ярость-синего-босса).

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 35,88 секунды; `rustfmt` нового Zone-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; фактическое усиление босса не проверялось.

## Zone effects: данные и асимметричная запись TianShenXiaFan 24 сентября 2026

Данные, кодек и формулы свойств `CTianShenXiaFanState` (`0x335`) перенесены в [`zone/effects/tianshenxiafan.rs`](../../server/rust/zone/src/effects/tianshenxiafan.rs). Переходный Game сохраняет живые Begin/restart/AI/End, visual и связь с фабрикой навыков; формулы получили примитивный `TianShenXiaFanPlayerView`.

По совпадающей паре Game EXE/PDB (SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`) машинный код подтверждает ранее описанные адреса: конструктор `0x00605780` (ID и поля без часов), vtable `0x0066202C` с Begin `0x00605B70`, AI `0x005D5BA0`, End `0x006059A0`, OnUpdateProperties `0x00605A10`, GetRemainedTime `0x00601200`, Serialize `0x006059C0`, Unserialize `0x00605C20`. Подтверждена асимметрия записи: 12 байт на записи (ID, timestamp как есть, level) против WORD+DWORD и отсутствия часов на чтении. **Исправлено расхождение**: Rust encode писал константный ноль вместо поля timestamp, тогда как native Serialize пишет поле `+0x2C` как есть; теперь загруженный WORD переживает цикл сохранения. Порядок семи запросов свойств и формулы перенесены без поколбэчной сверки (`PARTIAL`). Основания — в [описании сошествия](../gameplay/attributes-and-states.md#tianshenxiafan-асимметричная-запись-сошествия).

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 35,75 секунды; `rustfmt` нового Zone-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; фактический пересчёт свойств и клиентский visual не проверялись.

## Zone effects: атрибутные состояния боевой феи 24 сентября 2026

Данные, вид, формулы и 12-байтная запись восьми состояний Po/Yu (`0x212`–`0x219`) перенесены в [`zone/effects/battlefairy.rs`](../../server/rust/zone/src/effects/battlefairy.rs). Переходный Game сохраняет живые Begin/restart/AI/End, разрешение участников и применение к живому игроку/монстру; формулы получили примитивный `BattleFairyAttributePlayerView` вместо зависимости от `PlayerCombatProperties`.

По совпадающей паре Game EXE/PDB (SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`) найдены конструкторы всех восьми классов (адреса — в заголовке Zone-файла) и их vtable: общие Serialize `0x005E7330`, Unserialize `0x005FD660`, GetRemainedTime `0x00605E10`, AI `0x005E6E20` и End `0x005DBCE0`. Различие Po/Yu подтверждено различием вызовов участников в Begin. Формулы применения перенесены без изменения и без поколбэчной сверки, поэтому остаются `PARTIAL`; decode сохранил исходный порядок (часы после ID, до срока и значения). Основания — в [описании атрибутов боевой феи](../gameplay/attributes-and-states.md#атрибутные-состояния-боевой-феи).

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 35,87 секунды; `rustfmt` нового Zone-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; фактическое наложение и пересчёт свойств не проверялись.

## Zone effects: SpiderWeb и BossBlueQuake в blind-семействе 24 сентября 2026

Payload `CSpiderWebState` (`0x199`) и `CBossBlueQuakeState` (`0x1F8`) присоединены к общему типу семейства в [`zone/effects/blind.rs`](../../server/rust/zone/src/effects/blind.rs); переходный Game сохраняет живые Begin/restart/End, visual и обходы. По совпадающей паре Game EXE/PDB найдены конструкторы `CSpiderWebState` VA `0x005EA6E0/0x005EA750` (vtable `0x0065FBCC`) и `CBossBlueQuakeState` VA `0x005E8590/0x005E8600` (vtable `0x0065F934`): каждый записывает свой ID и срок без чтения часов, а vtable разделяют с семейством Serialize `0x005F51E0`, Unserialize `0x005EAAC0`, getter `0x005F2CD0`, AI `0x005D5BA0` и End `0x005EA9A0`. Адрес `0x005EA750` и слоты `0x005EA7D0/0x005EA8B0` совпали с ранее сохранёнными метаданными ctor/Begin. Ранее адреса обоих классов не были зафиксированы. Исправления Rust: decode обоих типов читает часы после проверки ID; ручная сериализация SpiderWeb заменена общим `encoded`; `new(0, x)` сведены к `new(x)`. Основания — в [описании blind-семейства](../gameplay/attributes-and-states.md#blind-семейство-блокировка-движения-и-боя).

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 34,76 секунды без предупреждений; `rustfmt` Zone-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; фактические запреты и клиентский visual не проверялись.

## Zone effects: данные и запись Promotion 24 сентября 2026

Данные, срок и 12-байтная запись `CPromotionState` (`0x142`) перенесены в [`zone/effects/promotion.rs`](../../server/rust/zone/src/effects/promotion.rs). Переходный Game сохраняет живой Begin/restart с visual и участниками; применение коэффициентов в бою и лечении не менялось.

По совпадающей паре Game EXE/PDB (SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`) найдены оба конструктора VA `0x005F2BC0/0x005F2C40` (второй задаёт коэффициенты `1`) и vtable `0x006605F4`: общие с семейством AI `0x005D5BA0`, End `0x005FD420`, getter `0x005F2CD0`; собственные Serialize `0x005F2E90` (ID, остаток через getter, WORD `+0x3C`, WORD `+0x3E`), Unserialize `0x005F2FB0` (часы после внешнего ID, до полей срока) и Restart `0x005FD450` (только часы). Смещение `+0x3E` подтверждено и чтением WORD в AI Heal VA `0x005EEE64`. Исправление Rust: decode читает часы после проверки ID вместо расхода до входа. Основания — в [описании Promotion](../gameplay/attributes-and-states.md#promotion-усиление-атаки-и-лечения).

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 35,96 секунды; `rustfmt` нового Zone-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; фактический бафф и его влияние на удар/лечение не проверялись.

## Zone effects: данные, тик и запись периодического лечения 24 сентября 2026

Данные, срок, правило тика и 16-байтная запись четырёх состояний лечения (`0xD3/0xE3/0xD9/0xE4`) перенесены в [`zone/effects/heal.rs`](../../server/rust/zone/src/effects/heal.rs). Переходный Game сохраняет живые Begin/restart/AI/End, чтение Promotion и публикацию HP.

По совпадающей паре Game EXE/PDB (SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`) найдены конструкторы всех четырёх классов (адреса — в заголовке Zone-файла) и их vtable: общие AI `0x005EEDF0`, End `0x005EEBA0`, GetRemainedTime `0x005F2CD0`, Serialize `0x005F65F0` и Unserialize `0x005EEC70`. Инструкции writer подтверждают порядок ID → getter остатка → частота → объём; reader читает часы до трёх оставшихся полей; AI — разрешение S, проверку смерти, WORD Promotion ×0.001 до первого clock, строгое расписание тика, FISTP-усечение, DWORD-сложение HP, повторное чтение MAX и поздние часы истечения. Ранее адреса этого семейства нигде не были зафиксированы. Исправление Rust: decode читает часы после первого поля записи вместо расхода до входа. Основания — в [описании лечения](../gameplay/attributes-and-states.md#периодическое-лечение-heal).

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 35,97 секунды; `rustfmt` нового Zone-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; фактический тик лечения и клиентский visual не проверялись.

## Zone effects: данные и запись blind-семейства 24 сентября 2026

Данные и 8-байтная запись восьми состояний blind-семейства перенесены в [`zone/effects/blind.rs`](../../server/rust/zone/src/effects/blind.rs): общий payload `BlindState<ID, BLOCKS_FIGHTING>` обслуживает Blind (`0x76`), Rush (`0x73`), Rush2 (`0x7C`), Seal (`0x138`), Strike (`0xDD`), KnightCut (`0x67`), KnockOut (`0x192`) и BoaLock (`0xD2`, с `BLOCKS_FIGHTING = false`). Переходный Game сохраняет живой lifecycle, запреты движения/боя, visual и обходы состояний; blanket-адаптер payload покрывает все типы семейства.

По совпадающей паре Game EXE/PDB (SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`) найдены и проверены конструкторы всех восьми классов (адреса — в заголовке Zone-файла): каждый записывает свой ID и срок без чтения часов. Их vtable разделяют Serialize `0x005F51E0`, Unserialize `0x005EAAC0`, GetRemainedTime `0x005F2CD0` и AI `0x005D5BA0`; у BoaLock собственный End `0x005FB800` и пустой OnAction, что закреплено константой типа Zone. Ранее отсутствовавшие адреса writer/reader/vtable для KnightCut, KnockOut и BoaLock этим закрыты. Исправления Rust: decoders трёх standalone-типов читают часы после проверки ID вместо расхода до входа; ручная сериализация KnockOut заменена общим `encoded`; `new(0, keep)` сведены к `new(keep)`. Основания — в [описании blind-семейства](../gameplay/attributes-and-states.md#blind-семейство-блокировка-движения-и-боя).

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 35,93 секунды; `rustfmt` нового Zone-файла и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; фактические Begin/End, запреты и клиентский visual не проверялись.

## Zone effects: данные и запись DaubPoison 24 сентября 2026

Данные, срок и 8-байтная сохраняемая запись `CDaubPoisonState` (`0xDF`) перенесены в [`zone/effects/daubpoison.rs`](../../server/rust/zone/src/effects/daubpoison.rs), числовое правило запроса срока — в [`zone/skills/daubpoison.rs`](../../server/rust/zone/src/skills/daubpoison.rs). Переходный Game оставляет живые Begin/restart/update/End, visual и разрешение участников; применение навыка продолжает завершать первый слот `0xDF` до запроса срока и Begin.

По совпадающей паре Game EXE/PDB (SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`) конструктор VA `0x005F17B0` записывает ID `0xDF` и срок из аргумента без чтения часов; взятая из него vtable `0x0066047C` подтверждает общие с `CCureState` writer `0x005F51E0`, reader `0x005EAAC0`, getter `0x005F2CD0`, AI `0x005D5BA0` и End `0x005FD420`. Открытый вопрос writer-а формата этим закрыт. Исправлено поведение Rust при загрузке: часы теперь читаются внутри decode после проверки ID, как в reader, а не вызывающей фабрикой до входа. Дублирующие константы `0xDF` и `0x131` в `heartlessarrow.rs` заменены общими Zone. Основания — в [описании DaubPoison](../gameplay/attributes-and-states.md#daubpoison-смазка-оружия-ядом).

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 36,71 секунды; `rustfmt --check` новых Zone-файлов и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; фактическое наложение смазки и перенос яда ударом не проверялись.

## Zone quests: общая клиентская запись задания 24 сентября 2026

Запись одного задания для клиента выделена в [`zone/quests/client.rs`](../../server/rust/zone/src/quests/client.rs); её используют и снимок входа `append_client_quest_snapshot` в Game `player.rs`, и уведомление добавления `0xBFF2C` в Game `game.rs`, ранее дублировавшие порядок полей. Владелец формата записи — Zone quests; преамбула `max_quest_count`/count, выбор получателя и момент отправки остаются у переходного Game.

По совпадающей паре Game EXE/PDB (SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`) машинные инструкции подтверждают одинаковый порядок полей обеих записей: u16 ID, пять u32 со смещений записи каталога `+0x88…+0x98`, три C-строки `+0xA0`/`+8`/`+0x24`, байт `+0x84`, четыре i32 в порядке `+0x78`, `+0x7C`, `+0x80`, `+0x74`. Участки: `AddQuestDataByteArray_ForClient` VA `0x0043E229–0x0043E339` и `CPlayer::AddQuest` VA `0x004453F4–0x004454FE`; там же подтверждены ранее описанные ветви допуска `AddQuest` (отсев состояния `0`, удаление ненулевого до поиска определения, вставка `0` перед `0xBFF2C`). Правила записаны в [клиентском списке заданий](../gameplay/quests.md#клиентский-список-и-постоянное-сохранение).

Попутно убраны устаревшие метаданные-заглушки `UNKNOWN` в Game `player.rs` для уже перенесённых и сверенных `AddQuestDataByteArray`, `AddQuestDataByteArray_ForClient` и `RunQuestCompleteScript`; поведение не менялось. Изменение байтового результата не предполагается: обе ветви записывают те же поля в том же порядке, что и прежний Rust-код; сравнение исходников это подтверждает.

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 37,06 секунды; `rustfmt --check` изменённых Zone-файлов и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались; доставка пакетов в работающем Game этим шагом не проверялась.

## Zone quests: клиентский допуск завершения и отказа 24 сентября 2026

По той же сопоставленной Game EXE/PDB исследованы машинные ветви `OnOrgasysMessage` для `0x90127/0x90128`, отдельно от аукционного вызова `RunQuestCompleteScript`. Обе требуют локального игрока и нулевого состояния задания, затем выбирают путь завершения или отказа из каталога. Допуск отказа и выбор `abandon_script` перенесены из Game в [`PlayerQuestProgress`](../../server/rust/zone/src/quests/progress.rs); Game сохраняет доступ к игроку и очередь сценариев. Основания, адреса и пределы сверки приведены в [правилах заданий](../gameplay/quests.md#принятие-завершение-и-отказ). Исходный обработчик напрямую вызывает `RunScript`; эквивалентность момента исполнения Rust-очереди и клиентский результат остаются непроверенными.

`cargo check --locked -p nebokrai-zone --lib` в Windows прошёл. Штатный Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 38,38 секунды после запуска Docker Desktop. `rustfmt --check` для изменённого Zone-файла и `git diff --check` прошли; целиком старые крупные Game-файлы `rustfmt --check` не проходят из-за существующего форматирования за пределами этого изменения. Серверы и клиент не запускались, автоматические тесты не создавались.

## Zone quests: допуск сценария завершения 24 сентября 2026

Локальный допуск `RunQuestCompleteScript` и выбор `complete_script` вынесены из Game в [`PlayerQuestProgress`](../../server/rust/zone/src/quests/progress.rs); Game по-прежнему ставит выбранный файл в сценарную очередь. Поведение Rust для отсутствующего, ненулевого и нулевого состояния не изменено. По Game EXE SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E` проверено совпадение PDB RSDS GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`. Инструкции VA `0x00458940–0x004589AA` подтверждают порядок: поиск ID в карте, проверка нулевого байта, поиск определения, передача пути исполнителю. Его прямой вызов VA `0x00487F0E` найден в аукционном обработчике; связь с исходным клиентским `0x90127` этим не установлена. Адреса и граница вывода находятся в [правилах заданий](../gameplay/quests.md#принятие-завершение-и-отказ). В этом предыдущем шаге клиентская ветвь отказа оставалась в Game без изменения и без новой заявки на `VERIFIED`.

Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 37,69 секунды; `rustfmt --check` изменённого Zone-файла и `git diff --check` прошли. Фактическая постановка сценария, его исполнение и клиентский результат после переноса не проверялись; серверы и клиент не запускались, автоматические тесты не создавались.

## Разные остатки времени задания для сценария и клиента 24 сентября 2026

По той же совпадающей Game EXE/PDB проверены исходные ветви сценарного `GetQuestTime` и клиентского запроса `0x8FA12`. Сценарий возвращает `0` при нулевом начале/лимите или отрицательной разности; клиентский `0xBF72B` получает сырую 32-битную разность без этих проверок. Rust прежде использовал сценарное ограничение для обоих путей. В Zone добавлен отдельный клиентский расчёт, а Game `playermessage.rs` переключён на него. Адреса и правила находятся в [описании заданий](../gameplay/quests.md).

Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` завершился успешно за 36,64 секунды. Клиентский обмен и сценарий в работающем сервере не запускались.

## Zone quests: время и доступность заданий 24 сентября 2026

Три поля живого игрока — начало срока, лимит и флаг доступности — выделены из Game `PlayerBaseProperties` в `Zone/quests/availability.rs`. Game сохраняет прежние смещения GameSave, берёт время и отправляет `0xBF729`, `0xBF72A`, `0xBF728` после мутаций Zone; World/DB-проекция не менялась. По совпадающей Game EXE/PDB инструкции `QuestTimeBegin`, `QuestTimeClear` и `SetQuestOn` подтвердили записи полей перед этими сообщениями. Адреса и граница проверки указаны в [правилах заданий](../gameplay/quests.md). Формула остатка перенесена без изменения, её исходные ветви остаются `PARTIAL`.

Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 36,58 секунды. Фактический отсчёт времени и сообщения клиенту после переноса не проверялись; серверы и клиент не запускались.

## Сверка локальных переходов прогресса заданий 24 сентября 2026

После переноса карты в Zone по совпадающей Game EXE/PDB проверены машинные инструкции `GetQuestState`, `GetValidQuestNum`, `AddQuest`, `CompleteQuest` и `RemoveQuest`. Обнаружено и исправлено расхождение `AddQuest`: при существующем ненулевом состоянии оригинал удаляет запись до поиска определения, поэтому отсутствие определения оставляет карту без этого ID. Прежний Rust проверял каталог раньше и сохранял старое состояние. Локальные ветви и адреса описаны в [правилах заданий](../gameplay/quests.md#принятие-завершение-и-отказ). Маршрутизация между Game и World, исполнение сценария и клиентский результат этой сверкой не установлены.

Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` завершился успешно за 38,20 секунды; `rustfmt --check` для изменённого Zone-файла и `git diff --check` прошли. Серверы и клиент не запускались.

## Zone quests: прогресс живого игрока 24 сентября 2026

Карта `u16 ID → u8 state` и операции принятия, завершения, удаления и отбора для клиента перенесены из Game `CPlayer` в `Zone/quests/progress.rs`. Экземпляр остаётся вложенным в живого игрока; Game читает и пишет GameSave, связывает прогресс с каталогом и отправляет сообщения. Алгоритмы переходов Rust не менялись. Совпадение Game EXE/PDB подтверждено по SHA-256 и RSDS GUID/age. Инструкции `AddQuestDataByteArray` вместе с целями writer-ов подтверждают упорядоченные пары `u16 + u8` после `i32 count`; вход `AddQuestDataByteArray_ForClient` подтверждает пропуск состояния `1` и ID без записи каталога. Адреса и пределы проверки находятся в [правилах заданий](../gameplay/quests.md#клиентский-список-и-постоянное-сохранение). Исходные ветви принятия, завершения и удаления повторно не сверялись, поэтому полный контракт прогресса остаётся `PARTIAL`.

Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 36,99 секунды. Сервер и клиент не запускались; GameSave и клиентский список в работающем процессе после переноса не проверены.

## Zone content: полученный каталог заданий 24 сентября 2026

Game теперь держит каталог определений через `Zone/content/quests.rs`: при селекторе `0x16` адаптер передаёт ему тело сообщения и cursor; игровое чтение использует тот же экземпляр. Кодек Shared, порядок применения полей, частичный результат, клиентские уведомления и состояние заданий игрока не менялись. Для происхождения decoder-а проверено совпадение Game EXE/PDB по RSDS и начало инструкций RVA `0x627A0`; предел этой проверки указан в [описании каталога](../gameplay/quests.md#клиентский-список-и-постоянное-сохранение).

Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 40,24 секунды. Сервер и клиент не запускались. Доставка и применение каталога в работающем Game, а также причина недоступности заданий и навыков в клиентской сессии остаются непроверенными.

## Realm content: загрузка каталога заданий 24 сентября 2026

World теперь хранит `QuestCatalog` из Realm: тот выбирает `Quest.ini`/`QuestEx.ini` и вызывает общий загрузчик Shared при старте и повторной загрузке профиля `QuestData`. World оставляет у себя StringTable, журнал и отправку сериализованного каталога Game. Второй файл теперь открывается только после разбора основного; при отсутствии основного ресурса к нему вообще нет обращения. Это соответствует раннему выходу оригинального `CQuestSystem::Load`. Прежнее безусловное сообщение `Load QuestData...OK!` сохранено, но Rust дополнительно предупреждает о неполной загрузке и сообщает причину/число применённых записей. Основание по совпадающей World EXE/PDB и адресам находится в [каталоге заданий](../gameplay/quests.md#каталог-и-состояние-персонажа).

Локальные `runtime/WorldServer/data/quest.ini` и `questex.ini` существуют; это не доказывает, что запущенный World выбрал их, разобрал полностью и передал рабочий каталог Game. Linux `cargo check --locked --workspace --lib --bins` через `deploy/check-rust.ps1` прошёл за 38,03 секунды. Сервер и клиент не запускались; причина недоступности заданий и навыков в наблюдавшейся сессии остаётся неизвестной.

## Проверки переноса навыков 24 сентября 2026

После переноса допуска `ResetSkill` в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Game EXE/PDB VA `0x00501599–0x00501645` подтверждают проверку ненулевого флага до доступа к головному предмету, отказ при отсутствии предмета и требование значения `1` у свойства `172`/индекс `1`; при выключенном флаге выбирается `ZHGS0008`. Чтение живого предмета и отправка уведомления остаются у Game. Фактический клиентский отказ не запускался.

После сверки виртуального поиска предметов `ResetSkill` с Game EXE/PDB удалена ошибочная Rust-проверка фабричного ID `0`: native lookup неизвестного имени возвращает `0`, а listener всё равно сравнивает его с ID предметов. Конструктор контейнера VA `0x004DE0A0–0x004DE0CD` устанавливает vtable, её слот `+0x28` ведёт в обход VA `0x004FC680–0x004FC6AC`, listener сравнивает ID в VA `0x004E2240–0x004E2284`. Два запроса `ZHJNS01`, `ZHJNS02` до выбора позиции подтверждены VA `0x00501663–0x0050178A`. Linux `cargo check --locked --workspace --lib --bins` завершился успешно; сценарий с ID `0` и клиентский результат не запускались.

После переноса правила расхода одного предмета сброса навыка в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Game EXE/PDB VA `0x005016BD–0x0050178A` выбирают группу предметов по позиции; вызываемая функция игрока VA `0x004315E9–0x00431702` при количестве `0` или `1` идёт к удалению, при `>1` уменьшает количество. Rust прежде пропускал удаление нулевой стопки; живой рюкзак продолжает менять Game. Фактический сценарий с нулевой стопкой и клиентские сообщения не проверялись.

После исправления стоимости MP в уведомлении о выбранном навыке `ResetSkill` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Game EXE/PDB VA `0x00501B6B–0x00501B8D` подтверждают знаковое `fild dword` перед double-масштабированием; Rust прежде масштабировал DWORD как беззнаковый. Ветвь без найденного навыка VA `0x00501AD4–0x00501AE2` оставляет предшествующие изменения без завершающих сообщений. Фактический пакет и клиентский сценарий не проверялись.

После переноса чтения четырёх ID и выбора слота `ResetSkill` в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Исходные getter-вызовы для ключей `181–184` перед проверкой позиции и таблица переходов `3–6` сверены по Game EXE/PDB VA `0x005017B4–0x00501806`; живой getter и числовые ключи остаются у Game. Фактический расход предмета и клиентский сценарий не проверялись.

После переноса порядка четырёх записей свойства головного предмета при `ResetSkill` в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Для ключей `181–184` записи уровня/ID `0, 0, 1, выбранный ID` и отсутствие проверки результата отдельных setter-ов сверены по Game EXE/PDB VA `0x00501892–0x00501AC7`; сам предмет меняет Game. Фактическое изменение предмета в запущенном сервере и клиентский сценарий не проверялись.

После переноса всего цикла выбора нового ID при `ResetSkill` в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Три ветви `random(13) + 530` и общий слот `random(3) + 543`, проверки конфликтов, раннее принятие нуля и повторные вызовы того же RNG сверены по Game EXE/PDB VA `0x00501815–0x00501A8D`. Изменение предмета остаётся у игрока в Game; фактическая случайная последовательность и клиентский сценарий не запускались.

После переноса проверки кандидата сброса боевого духа в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Три ветви `random(13) + 530`, общий слот `random(3) + 543`, сравнения текущих и парных ID и раннее принятие нуля сверены по Game EXE/PDB VA `0x00501815–0x00501A8D`; в Rust исправлен отказ нулевому кандидату при нулевом старом ID. Случайная последовательность и полный игровой/клиентский сценарий фактически не запускались.

После переноса таблицы восьми несовместимых пар навыков боевого духа в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Пары `530↔534` … `533↔537` сверены по вставкам конструктора совпадающей Game EXE/PDB VA `0x00504052–0x00504149`. Цикл случайного выбора и клиентский сценарий этим шагом не проверялись.

## Проверки переноса навыков 23 сентября 2026

После замены предварительного снимка девяти навыков боевого духа последовательным обходом слотов Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Прямые вызовы в Game EXE/PDB VA `0x00430610–0x00430921` показывают чтение свойств перед снятием/установкой каждого навыка; Rust теперь сохраняет это чередование в шести путях Game. Доставка накопленных уведомлений и клиентский сценарий не проверялись.

После разделения проекций снятия и установки девяти навыков боевого духа в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Снятие читает только ID; установка первых семи читает уровень перед ID, последних двух — только ID с уровнем `1`. Эти чтения и порядок вызовов сверены по совпадающей Game EXE/PDB VA `0x00430610–0x00430921`; исправлены лишние чтения уровней и обратный порядок ID/уровня в Rust. Применение к живому списку и клиентский сценарий не запускались.

После переноса выбора уровня навыка боевого духа в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Нулевой offset, порядок чтений свойств и fallback на `0x222`/`0x223` сверены по Game EXE/PDB VA `0x0042E7A0–0x0042E81C`; чтение живого предмета остаётся в Game. Также уточнены адреса трёх ветвей команды навыка. Фактическое применение навыка и клиентский сценарий не запускались.

После выделения общей классификации цели `SkillTarget::form` в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Условия выбора объектной, координатной и собственной цели сверены по трём ветвям совпадающей Game EXE/PDB; проверки живого региона и объекта остались в Game. Фактическая постановка навыка и клиентский сценарий не запускались.

После переноса полей двух запросов навыка и вычисленных Game признаков доступности цели в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Очистка старшего бита ID до чтения остальных полей команды сверена по обеим ветвям совпадающей Game EXE/PDB. Вычисление признаков по живому региону, постановка навыка и клиентский сценарий этим переносом не запускались.

После переноса форм команд `PlayerSkillDispatch`/`BattleFairySkillDispatch` и ключа ожидающего запроса в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Проверка цели и сравнения object/point сверены по совпадающей Game EXE/PDB; конкретная постановка в очередь и клиентский сценарий не запускались.

После переноса правила reuse и числового представления стоимости MP боевого духа в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Сложение DWORD и unsigned-сравнение сверены по `CSkill::IsRestored`, а double-масштаб и усечение QWORD — по ветви ошибки `CWangsheng::AI` совпадающей Game EXE/PDB. Конкретные вызовы и клиентская диагностика не запускались.

После переноса идентичности фигуры в `zone/regions`, а `SkillLifecycle`, стадий и kernel в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Базовые Begin/End и конструктор сверены по совпадающей Game EXE/PDB; Rust-стадии и диагностическая причина завершения остаются внутренней моделью. Конкретные каталоги исполнений и вызовы Game не переносились; клиентский сценарий не запускался.

После переноса DWORD-правила лечения Wangsheng в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. ID, связь vtable с AI и участок GetHP → свойство `31` → сложение → SetHP/OnChangeStates сверены по совпадающей Game EXE/PDB. Живое лечение и клиентский сценарий не запускались.

После переноса создания состояния Hearten в `zone/skills` и исправления момента чтения часов в `zone/effects` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Порядок двух запросов и конструктора сверен по `CHearten::AI`, вызов часов после ID — по reader совпадающей Game EXE/PDB. Полный lifecycle и клиентский сценарий не запускались.

После переноса расчёта прибавок GodBless/GodBless2 в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Порядок шести запросов свойств, два округления до `f32` для каждой прибавки, поздний запрос срока и усечение трёх значений сверены по `CGodBless::AI` и `CGodBless2::AI` совпадающей Game EXE/PDB. Живое наложение и клиентский сценарий не запускались.

После переноса данных и числовых правил GodBless/GodBless2 в `zone/effects` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Конструкторы, общие writer/reader/getter, AI и property callback сверены по совпадающей Game EXE/PDB; исправлен момент чтения часов при загрузке и записи. Живое наложение, пересчёт и клиентский сценарий не запускались.

После переноса расчёта параметров Pillar в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Порядок запросов, unsigned преобразование и float-коэффициент сверены по `CPillar::AI` совпадающей Game EXE/PDB. Живое переключение и клиентский сценарий не запускались.

После переноса данных и срока Pillar в `zone/effects` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Конструктор, общий writer, reader, getter и AI сверены по совпадающей Game EXE/PDB; исправлен момент чтения часов при записи. Живое переключение, расчёт защиты и клиентский сценарий не запускались.

После переноса границ области Roar в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Границы, включительный обход и порядок осей сверены по `CRoar::AI` совпадающей Game EXE/PDB. Живые цели и клиентский сценарий не запускались.

После переноса данных и числового правила подавления атаки Roar в `zone/effects` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Конструктор, writer/reader, AI и три сравнения для игрока и монстра сверены по совпадающей Game EXE/PDB; исправлен момент чтения часов при записи. Живой пересчёт, visual и клиентский сценарий не запускались.

После переноса сохранённого `WangshengState` в `zone/effects` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Конструктор, общие writer/reader/getter, AI и отдельный property callback сверены по совпадающей Game EXE/PDB; при загрузке исправлен момент чтения часов, при записи убрано временное выделение памяти. Загрузка legacy-записи, изменение HP и клиентский сценарий не запускались.

После переноса списка конфликтов Fury в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Девять ID и порядок обхода/End проверены по `CFury::AI` совпадающей Game EXE/PDB; фактическое снятие состояний и клиентский результат не запускались.

После переноса общего payload Fury/RageBreak и формулы усиления атаки в `zone/effects` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Инструкции конструкторов, общего writer/reader, срока и property callback сверены по совпадающей Game EXE/PDB; исправлен момент чтения часов при загрузке. Живой Begin/End, числовой результат в процессе и клиентский сценарий не запускались.

После переноса порога и списка ID очищения Cure в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Инструкции `CCure::CastCure` и вызов из `CCure::AI` подтверждают формулу, порядок трёх запросов свойств, 12 ID и расход RNG лишь для подходящих слотов. Живой End, фактическое снятие состояний и клиентский результат не запускались.

После переноса `CureState` в `zone/effects` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Конструктор, writer, reader, AI и общий getter срока сверены по совпадающей паре Game EXE/PDB; исправлен порядок чтения часов при записи, а при загрузке часы читаются после ID. Живые Begin/End и клиентский visual фактически не запускались.

После переноса данных и числовой ветви LifeShield в `zone/effects` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Конструктор, writer/reader, AI, общий getter срока и ветвь `CFightDefense::PreDefense` ID `0x220` сверены по совпадающей паре Game EXE/PDB. Создание Cure осталось у живого End в Game. Фактическое поглощение, клиентский сценарий и возможное значение двух отдельных чтений MP боевого духа не проверены.

После переноса мана-щита и объединения числового хвоста с машинным щитом в `zone/effects` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Конструктор, запись/чтение, общие часы AI/getter и ветвь `CFightDefense::PreDefense` ID `0x141` сверены по совпадающей паре Game EXE/PDB. Клиентский сценарий, фактическое поглощение урона и предельные значения x87 в Rust не проверены.

После переноса данных и числового поглощения урона машинного щита в `zone/effects` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Конструктор, запись/чтение, AI и ветвь `CFightDefense::PreDefense` сверены по совпадающей паре Game EXE/PDB; фактическое применение щита и клиентский сценарий не запускались.

После переноса числовых правил области ослабления и расчёта срока призыва в `zone/skills` выполнен `pwsh -NoProfile -File deploy/check-rust.ps1`: Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Проверка подтверждает компиляцию всех библиотек и бинарных входов, но не линковку, работу Game или клиентский сценарий с Weak.

После выделения данных области PoisonFog в `zone/skills` тот же `cargo check --locked --workspace --lib --bins` повторно завершился успешно. Фактическое наложение тумана и клиентский выход этим не проверены.

После переноса данных и маски SpiderMist в `zone/skills` тот же `cargo check --locked --workspace --lib --bins` завершился успешно. Региональное наложение SpiderPoison и клиентский сценарий не запускались.

После переноса массива и часов SnowStorm в `zone/skills` тот же `cargo check --locked --workspace --lib --bins` завершился успешно. Вызовы RNG, атака по целям и клиентский снимок фактически не запускались.

После сверки исходного `Initialize` и формулы элемента SnowStorm арифметика урона выделена в ту же модель Zone. Linux `cargo check --locked --workspace --lib --bins` повторно завершился успешно. Статическая сверка Game EXE/PDB и компиляция не подтверждают фактическую атаку или клиентский сценарий.

После переноса сборки `AttackInformation` SnowStorm в Zone тот же Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Проверены машинные инструкции создания и заполнения атаки; запуск попадания и клиентский сценарий не выполнялись.

После переноса полей клиентского снимка SnowStorm в Zone тот же `cargo check --locked --workspace --lib --bins` завершился успешно. Порядок полей и чтений часов сверены с Game EXE/PDB; отправка пакета клиенту не проверялась.

После переноса ключей и порядка запросов `CSnowStorm::Summon` в Zone тот же `cargo check --locked --workspace --lib --bins` завершился успешно. Сверены прямые вызовы в Game EXE/PDB, фактический призыв навыка не запускался.

После выделения срока и маски FireWall в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно, включая путь замены соседних областей. Формула и таблицы масок сверены с Game EXE/PDB; игровой и клиентский сценарии стены не запускались.

После переноса остальных параметров призыва FireWall и порядка живых чтений в Zone тот же Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Порядок запросов сверён с инструкциями `CFireWall::Summon`; фактический призыв и клиентский эффект этим не проверялись.

После переноса хранимой маски и числового правила пересечения FireWall/YinYang в Zone тот же `cargo check --locked --workspace --lib --bins` завершился успешно. Машинные ветви `ReplaceAffectRegion` сверены статически; игровые переходы и клиентский результат не запускались.

После переноса часов и режима удара этих областей в Zone тот же `cargo check --locked --workspace --lib --bins` завершился успешно. Сравнения AI FireWall/YinYang сверены с Game EXE/PDB; фактический проход AI и End не запускались.

После переноса масок и параметров призыва YinYang/YinYang2 в Zone тот же `cargo check --locked --workspace --lib --bins` завершился успешно. Конструкторы и вызовы запросов сверены с Game EXE/PDB; фактический призыв, попадания и клиентский выход не запускались.

После переноса снимка и числового расчёта элементальных областей в `zone/skills` Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Прямые инструкции шести `CalculateAttackPower` и `Attack` FireWall сверены с Game EXE/PDB; фактическое попадание, применение урона и клиентский результат не запускались.

После переноса окон, маски и полей снимка GodThunder в `zone/skills` тот же Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Конструкторы, Initialize, writer и часы AI сверены с Game EXE/PDB; призыв, попадания и клиентский результат не запускались.

После переноса порядка чтения параметров призыва GodThunder/GodThunder2 в Zone тот же Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Прямые вызовы обоих Summon сверены с Game EXE/PDB; выбор исходного варианта больше не зависит от позднего повторного чтения ID регистрации. Фактический призыв и клиентский результат не запускались.

После переноса пути, часов и полей снимка ChaosSphere в Zone тот же Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Конструктор, writer и порядок ветвей AI сверены с Game EXE/PDB; движение по живому региону, попадания и клиентский снимок фактически не запускались.

После выделения двух фаз чтения параметров `CChaosSphere::Summon` в Zone тот же Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Ветви длины пути, порядок поздних запросов и числовое масштабирование элемента сверены с инструкциями Game EXE/PDB; призыв и клиентский результат фактически не запускались.

После переноса маски и параметров пустой клетки SoulMirror в Zone тот же Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Все 664 клетки трёх таблиц направлений сопоставлены с формулой; GetLength/GetHeight и порядок чтений AI сверены с Game EXE/PDB. Живой призыв, попадания и клиентский результат не запускались.

После переноса обхода столбцов и строк SoulMirror в Zone тот же Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Порядок повторных чтений уровня и направления, переход к следующей клетке после callback и проверка границ сверены с инструкциями AI Game EXE/PDB. Живой обход целей, призыв и попадания не запускались.

После переноса фаз расчёта прямых элементальных попаданий в Zone тот же Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Инструкции восьми `CalculateAttackPower` подтверждают порядок свойств, RNG, усечение элемента и различие критического и усиленного душами хвостов; фактический удар и клиентский результат не запускались.

После переноса данных, предела и записи SoulCollect в Zone effects тот же Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Конструктор, AddSoul, Serialize/Unserialize и порядок End→Calculate трёх снарядов сверены с Game EXE/PDB; фактический расход душ, visual и попадание не запускались.

После переноса данных, беззнакового предела и записи EnergyHolding в Zone effects тот же Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Конструкторы, AddEnergy и общий с SoulCollect Serialize/Unserialize сверены с Game EXE/PDB; фактические пополнение, расход и восстановление состояния не запускались.

После переноса множителя InverseChopped к EnergyHolding в Zone тот же Linux `cargo check --locked --workspace --lib --bins` завершился успешно. Порядок чтения первого слота ID `0x89`, вычисления множителя, вызова End и уничтожения остатка перед RNG сверены с Game EXE/PDB; фактический удар не запускался.

## Проверка публичного дерева 21 сентября 2026

На коммите `714175bb5` повторно выполнен `cargo check --locked --offline --lib --bins` для библиотеки и шести серверных входов. Источники получены через `git archive`, без `.git`, исследовательских материалов, runtime, конфигов и БД. Использован Linux x86_64, Rust/Cargo 1.97.1 и образ `rust:1.97.1-bookworm` с digest `sha256:0e2bcaef56d041a486784e54104a81aebe0da44bd03019bd70bc0401e42e4a97`. Все 233 внешних архива зависимостей сверены по SHA-256 из Cargo.lock; target-кэш изначально пуст. Сеть контейнера отключена. Проверка завершилась успешно за 1 минуту 23 секунды.

Это проверка типов и заимствований после очистки исходников и удаления встроенных параметров доступа Auth. Линковка, повторный запуск служб и клиентский сценарий этим запуском не проверялись. Ниже сохранены результаты прежних сборок и runtime-наблюдений.

## Наблюдения серверного стенда

Состояние на 21 сентября 2026 года, ветка `main`. Это оценка **активного Rust-кода и отслеживаемого развёртывания**, а не доля перенесённых строк. Проверка `cargo check --locked --lib --bins` и сборка `cargo build --locked --bins` успешно пройдены на Linux x86_64 с Rust 1.97.1 через локальный [check-rust.ps1](../../deploy/check-rust.ps1). Получены и совместно запущены шесть Rust-бинарников в dev-профиле; отдельно собран release-образ текущего Game. Game открыл клиентский порт после получения конфигурации World. Владелец исходным клиентом прошёл список персонажей, загрузку, вход в мир и смог перемещать персонажа; движение при этом было заметно задержано. После этого Game AI был отдельно исправлен и проверен на release, но новый клиентский проход ещё не выполнен. Наблюдённые ошибки и границы проверки перечислены ниже. «Не проверено» ниже означает отсутствие подтверждения, а не доказанный отказ.

Для общих переменных, переходов между регионами и торгового обмена Billing нет подтверждения работы через реальные соединения и исходный клиент.

## Как читать матрицу

- **Исходный факт** — конкретный EXE/PDB, owner или данные, на которые опирается реконструкция. Верхний комментарий указанного файла содержит детали; уровень знания определяется по [правилам свидетельств](../reconstruction/evidence-and-contracts.md).
- **Код** — есть ли исполняемая реализация, а не только имя файла или RAW-псевдокод.
- **Полнота** — покрыты ли заявленные ветви и варианты. «Частично» не означает оценку доли работ.
- **В процессе** — достигнут ли код из объявленного бинарного входа и игрового lifecycle. Это не эквивалент успешной работе.
- **Сборка / runtime / клиент** — отдельные уровни проверки текущего состояния кода. Колонка «Сборка» означает получение исполняемых серверов; успешный общий `cargo check`, указанный выше, её не заменяет.

| Подсистема | Исходный факт | Код | Полнота | В процессе | Сборка | Runtime | Исходный клиент |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Auth | EXE/PDB и owner в [`cgame.rs`](../../server/rust/src/authserver/src/cgame.rs) | `Init → MainLoop → Release`, сеть, очередь, DB workers | Частично: не подтверждена ANSI-карта регистра учётных записей | Да, [`process/authserver.rs`](../../server/rust/src/process/authserver.rs) | Да, Linux dev | Запущен, слушает 7100 | Не проверено |
| Login | EXE/PDB и owner в [`game.rs`](../../server/rust/src/loginserver/loginserver/game.rs) | Вход, Auth/GAS, CD-key, выбор мира, World-маршрут | Частично: исходный default `verification_sign_upper` и высокобитная ANSI-ветвь неизвестны | Да, [`process/loginserver.rs`](../../server/rust/src/process/loginserver.rs) | Да, Linux dev | Запущен, слушает 2345/2346 | Вход до списка персонажей подтверждён владельцем |
| Billing | EXE/PDB и owner в [`game.rs`](../../server/rust/src/billingserver/billingserver/game.rs) | Сетевой цикл, покупка/обмен/баланс, DB workers | Полная эквивалентность SQL-процедур и внешних эффектов не проверена | Да, [`process/billingserver.rs`](../../server/rust/src/process/billingserver.rs) | Да, Linux dev | Запущен, слушает 8188; внешний worker отключён | Не проверено |
| Misc/аукцион | EXE/PDB и owner в [`game.rs`](../../server/rust/src/miscserver/miscserver/game.rs) | World-соединение, очередь, reconnect, аукционные операции | Частично: ряд повреждённых или неподтверждённых goods-ответов останавливается на безопасной границе | Да, [`process/miscserver.rs`](../../server/rust/src/process/miscserver.rs) | Да, Linux dev | Зарегистрирован в World; операции не проверены | Не проверено |
| Сеть и общий wire | Шесть вариантов EXE/PDB перечислены в [`basemessage.rs`](../../server/rust/src/nets/basemessage.rs) | Заголовок, очереди, кадры, TCP-владельцы | Частично: `GetEx`, повреждённый RLE и отдельные ошибки длины остаются неизвестными | Да, через `nets` и `transport` | Да, Linux dev | Начальный World → Game обмен; есть неподдержанные сообщения | Не проверено |
| База данных | MSSQL/ADO-вызовы и owner-ы в [`dbaccess/`](../../server/rust/src/dbaccess/) | Tiberius-адаптеры Auth/Login/Billing/World | Частично: исходная `userinfo` отсутствует в найденных backup; все схемы и эффекты не подтверждены | Подключены конкретные owner-ы; охват всех сценариев не доказан | Да, Linux dev | Подключения и часть загрузки; есть SQL 208 | Не проверено |
| WorldServer lifecycle | EXE/PDB и owner в [`worldserver/game.rs`](../../server/rust/src/worldserver/worldserver/game.rs) | `Init`, основной цикл, `Release`, сеть, таймеры, workers | Частично: есть явные блокировки на неизвестных состояниях и конфигурации | Да, [`process/worldserver.rs`](../../server/rust/src/process/worldserver.rs) | Да, Linux dev | Init, регистрация Game/Misc, устойчивый основной цикл и естественный save-cycle | Не проверено |
| Загрузка/сохранение игрока | World DB owners и [`savedb.rs`](../../server/rust/src/worldserver/worldserver/savedb.rs) | Очереди, TDS-адаптер, фазы сохранения | Полнота данных и фактическое восстановление персонажа не доказаны | Да, через World lifecycle | Да, Linux dev | Не проверено | Не проверено |
| GameServer lifecycle и сообщения | EXE/PDB и owner в [`gameserver/game.rs`](../../server/rust/src/gameserver/gameserver/game.rs) | `Init`, цикл, dispatch, регионы, объекты | Частично: неизвестные сообщения и недостающие конкретные owners не исполняются | Да, отдельный Rust-бинарник объявлен | Да, Linux dev/release | На свежем release: ID 1, 549 регионов, healthy LISTEN 2347; устойчивого >1 s lag-warning после setup не наблюдалось | Вход в мир подтверждён владельцем; стабильная сессия не доказана |
| Движение | EXE/PDB в [`shapemessage.rs`](../../server/rust/src/gameserver/appserver/message/shapemessage.rs) и owner-ах игрока | Команда шага, очередь AI, проверка клеток и изменение позиции | Частично: другие варианты перемещения и синхронизация клиента не доказаны | Да, диспетчер → AI → регион | Да, Linux dev/release | Игровой путь достигнут; серверный AI после отдельного исправления укладывается примерно в tick budget на release | Движение наблюдалось, но с заметной задержкой; текущий release ещё не перепроверен клиентом |
| Бой и базовая атака | EXE/PDB в [`baseattack.rs`](../../server/rust/src/gameserver/appserver/skills/baseattack.rs) | Запрос, проверка цели, исполнение базовой атаки, приём попадания | Частично: PvP, смерть, loot и все ветви эффектов не подтверждены сквозным сценарием | Да, через Game AI и конкретного исполнителя | Да, Linux dev | Не проверено | Не проверено |
| ИИ монстров и NPC | EXE/PDB в [`aifactory.rs`](../../server/rust/src/gameserver/appserver/ai/aifactory.rs) и производных owner-ах | Выбор типа, поиск, преследование и атака части монстров | Частично: производные AI-ветви и поведение обычных NPC не закрыты | Да, через региональный AI-проход | Да, Linux dev | Не проверено | Не проверено |
| Навыки | EXE/PDB и [фабрика](../../server/rust/src/gameserver/appserver/skills/skillfactory.rs) | Реестр ID и часть исполняемых навыков | Неполно: соответствие 209 ID не означает 209 полных Begin/AI/End | Частично: dispatch отклоняет ID/формы без конкретного owner-а | Да, Linux dev | Не проверено | Не проверено |
| Предметы и контейнеры | EXE/PDB в [предметном owner-е](../../server/rust/src/gameserver/appserver/message/goodsmessage.rs) и [переносе](../../server/rust/src/gameserver/appserver/message/containermessage.rs) | Модель экземпляра, контейнеры, переносы, часть улучшений и [wire-маршрутов](../gameplay/items.md) | Частично: вся матрица контейнеров, улучшений, отказов и сохранения не доказана | Достигнутые обработчики Game; охват всех операций не доказан | Да, Linux dev | Не проверено | Не проверено |
| Задания и сценарии | EXE/PDB в [каталоге](../../server/rust/shared/src/resources/quest_wire.rs), [обработчике Game](../../server/rust/src/gameserver/appserver/message/organsysmessage.rs) и [интерпретаторе](../../server/rust/src/gameserver/appserver/script/script.rs) | Каталог, операции заданий, очередь/интерпретатор, сохранение quest map и персональных переменных, отправка общих присваиваний World | Частично: охват языка и наград; отказ доставки не откатывает копию Game, массивы World не сохраняются обратимо. Подробности — [сценарии](../gameplay/scripting.md) | Связаны конкретные Game/World/DB-пути; полный сценарий не проверен | Да, Linux dev | Не проверено | Не проверено |
| Торговля и Billing-эффекты | EXE/PDB в [магазине](../../server/rust/src/gameserver/appserver/message/shopmessage.rs) и [обмене](../../server/rust/src/gameserver/appserver/session/ctrader.rs) | NPC-цены, обмен, часть [личной и премиальной торговли](../gameplay/trade.md) | Частично: полный commit при сетевых сбоях, аукцион и финансовые ответы не закрыты | Достигнутые ветви Game/Billing; платежи не проверены | Да, Linux dev | Не проверено | Не проверено |
| Мировые организации и войны | EXE/PDB в [организационном владельце](../../server/rust/src/worldserver/appworld/organizingsystem/organizingctrl.rs) и [войнах стран](../../server/rust/src/worldserver/appworld/country/countrywarsys.rs) | Регионы, организации, календарные фазы и часть [военных сценариев](../gameplay/world-wars.md) | Частично: парные wire-layouts, все права/исходы и поведение таймеров под нагрузкой неизвестны | Частично: связаны с World lifecycle, полный путь не доказан | Да, Linux dev | Не проверено | Не проверено |
| ServerUpdate | Сохранённые owner-файлы в [`updatesys/`](../../server/rust/src/updatesys/) | Только RAW-псевдокод | Нет | Нет отдельного бинарника или модуля в [`lib.rs`](../../server/rust/src/lib.rs) | Неприменимо | Не проверено | Не проверено |

## Совместный запуск 20 сентября 2026

`VERIFIED`: локальный Docker Desktop `desktop-linux`, Linux x86_64, Rust 1.97.1, dev без debug-информации и incremental; команды — [Rust-стенд](../operations/rust-runtime.md). Работают MSSQL и шесть процессов Rust. `database_init` завершён с кодом 0, пять прикладных БД находятся ONLINE. Существующие базы не восстанавливались поверх данных. NAS в этом запуске не использовался. Сборочный кэш после сборки — около 1203 МиБ в одном томе с порогом 2048 МиБ.

`VERIFIED` по журналам совместного запуска и состоянию сокетов: World зарегистрировал Game с ID 1 и Misc с ID 5; Game показал `server_id=1`, `regions=541` и открыл `2347` в состоянии `LISTEN`. Login слушает `2345` и клиентский `2346`, Auth — `7100`, Billing — `8188`, World — `1747`. Это подтверждает старт и начальный обмен конфигурацией, но не работу всех игровых подсистем из матрицы. Этот начальный запуск предшествовал клиентской попытке, описанной ниже.

При первом запуске Game не получал конфигурацию: World за 58 секунд ставил 6025 сообщений с 359566258 байтами payload, а сетевой цикл стоял вместе с доменным. Затем отправка закрывала сокет: `167293902 + 1054318 > 167772160` байт. `VERIFIED` по диагностике [CServer](../../server/rust/src/nets/servers.rs) и [World](../../server/rust/src/worldserver/worldserver/game.rs). Сетевой worker теперь обслуживает очередь независимо; лимит не увеличен. Общая запись TCP дописывает частичный Linux write, а Game вычитывает несколько готовых шагов за такт. Устройство и причина — [сетевой runtime](../server/network-runtime.md) и [ADR-0002](../decisions/0002-semantic-boundary.md).

`PARTIAL`: запуск выявил незакрытые участки, которые доступный порт не подтверждает:

- Для восьми начальных регионов типов `4/5` найдено исходное расхождение World/Game. `VERIFIED` статически: World создаёт их как обычный `CWorldRegion` и не сериализует три war-counter, а Game создаёт производный `CServerWarRegion` и inherited decoder читает эти 12 байт за logical end `CMessage`. Receive-copy не переносит туда CRC или следующий frame; свободная capacity `std::vector` не является wire-контрактом. Rust startup теперь читает для subtype `4/5` только доказанный `CServerRegion` prefix и не меняет World wire. Решение и адреса — [ADR-0008](../decisions/0008-region-startup-war-tail.md). Повторный совместный запуск после исправления ещё не выполнялся, поэтому runtime-статус остаётся `PARTIAL`.
- Ложные runtime-warning для `0x7F80D` в Game и `0x80403`/`0x7F80D`/`0x7F801` в Misc сняты прямым сопоставлением с оригиналами. `VERIFIED`: jump-table Game `OnServerMessage @ 0x0049D300` направляет `0x7F80C/0x7F80D` в общий no-op return `0x0049EFD0`; Misc `OnOtherMsg @ 0x00403CC0` обрабатывает только `0x7F809/0x7F80B`, а остальные типы безусловно игнорирует. Оригинальный World при этом действительно строит `0x7F80D`: `CGame::SendGlobeVariableToGS @ 0x004017E0` добавляет 16 байт и отправляет пакет. Rust теперь отличает эти исходные no-op от реально неподдержанных ветвей.
- SQL 208 для `AuctionLog`/`Auctionlog` снят как кодовый блокер: поставочный `LogDB.bak` не содержит auction-таблиц, а сохранённый запуск оригинального World показывает ту же ошибку и затем успешный старт listener-а и цикл сохранения. Точный DDL остаётся `UNKNOWN`, поэтому локальный стенд не создаёт фиктивную схему; граница описана в [документации БД](../architecture/database.md#схема-и-локальный-стенд). Обработчик внешних пополнений Billing отключён через `PlayerFillCheckSrvSwitch 0`, поскольку нет `TBL_NeedUpdate`; объяснение — [локальный режим Billing](../operations/rust-runtime.md#внешние-пополнения-billing).
- После загрузки регионов ранняя dev-сборка сообщала задержки порядка 1,4–1,7 секунды при целевом интервале 80 ms. Встроенное временное профилирование локализовало основную стоимость в `CGame::ai`: monster-проход обходил все зарегистрированные фигуры, включая sleeping monsters, а resolver очистки повторно сканировал регион даже без staged cleanup. Сопоставление с `CArea::GetActivedShapes`/`WakeUpMonsters` показало, что sleeping storage в этот AI-проход не входит. После перехода на active/pet/carriage monster storage и условного построения resolver release-профиль дал около 63 AI-вызовов за 5,04 s и `ai_ms` около 4,13–4,18 s, то есть примерно 65–66 ms на AI-вызов. Затем profiler удалён, release пересобран из чистого дерева; свежий Game дошёл до `server_id=1`, `regions=549`, healthcheck `healthy`, а за наблюдаемое steady-state окно warning об отставании цикла более чем на секунду не появился. Это серверная проверка темпа; качество движения клиента на этой сборке ещё не проверено.
- В World раннее steady-state отставание имело отдельную причину: `main_loop_resource_snapshot` трижды за один проход глубоко копировал реестр из 14 630 goods и индекс исходных имён. Временное dev-профилирование давало примерно 35–55 ms на один такой snapshot при целевом шаге World 40 ms. Горячие snapshot-ы переведены на `Arc`, а пути reload/release используют copy-on-write через `Arc::make_mut`, поэтому уже выданная версия остаётся неизменной для её потребителя. После чистой пересборки и перезапуска World 30-секундное steady-state окно прошло без `warning!!! 1 second not call AI()`; это runtime-подтверждение устранения именно наблюдавшегося hot-loop bottleneck, а не общей производительности всех подсистем.
- Natural save-cycle World после этого перезапуска также прошёл штатно: World видел `Connects = 1/1`, отправил `SaveNotify`, получил terminal save-data от `GameServer1`, запустил `SaveThread`, выполнил `Save Region Data` и завершил `Save Data end` / `SaveThread end`. Прежний SQL 8115 (`Arithmetic overflow ... int`) в этом цикле не повторился. Исправление сохраняет legacy DWORD-биты `today_total_tax`/`total_tax` при записи в SQL `int` через `u32 ↔ i32`; контракт описан в [регионах и организациях](../gameplay/regions-and-organizations.md). Этот цикл был без игроков (`player:0`), поэтому он подтверждает региональное сохранение и межсерверный save-handshake, но не полный player persistence/relogin.
- Ранний разрыв World до создания клиентского listener больше не считается доказанным пробелом реконструкции. `VERIFIED` по matching Game EXE/PDB: `CMyNetClient::HandleClose @ 0x0041A590` ставит `0x6F901` в собственную FIFO World-клиента, `CGame::ProcessMessage @ 0x00405830` обрабатывает её и без `s_pNetServer`, а reconnect-worker повторяет попытку каждые 8000 ms. При этом успешный `CGame::ReConnectWorldServer @ 0x0040B7B0` сам без null-check публикует `0x6F902` через `s_pNetServer->field_0xdc`. Следовательно, отдельного исходного pre-listener recovery-пути не найдено; Rust не материализует его догадкой. Наблюдённый в первом неудачном запуске последующий `NotConnected` остаётся фактом того запуска, а не основанием объявлять отсутствующей совместимую механику. Подробности — [жизненный цикл соединений](../protocol/connection-lifecycle.md#исходящие-соединения-между-серверами).

## Выбор персонажа в локальном клиенте

20 сентября владелец вошёл локальной учётной записью и увидел список персонажей; при выборе любого из трёх клиент сообщал, что не удалось подключиться к серверу. `VERIFIED` по World `log/debug-DB_2026-09-20.txt`: в 19:37:10, 19:37:19 и 19:37:24 UTC загрузка трёх локальных персонажей (A, B и C) завершилась `Read Player DB Error`. В `TemptLoadDataLog` есть соответствующие начала и завершения попыток. Это отказ до выдачи назначения Game, а не подтверждённая ошибка TCP к `2347`.

Сопоставление реальной локальной схемы с чтением Tiberius выявило расхождения регистра (`depotpassword`, `dwLt60Stamp`, `JJcLevel`/`JJcScore`) и типов: координаты — SQL `float(53)`, `goodsIndex` — `bigint`, `place`/`position` — `tinyint`. Загрузчики ожидали буквальное совпадение имён, `f32` и `i32` соответственно. Исправлены общие чтение имени/целого и явное преобразование координат; добавлена диагностика этапа отказа без содержимого DB-полей. Контракт — [чтение БД](../architecture/database.md#чтение-имён-и-чисел-через-tiberius), журналы — [диагностика выбора](../operations/diagnostics.md#отказ-после-выбора-персонажа).

`cargo build --locked --bins` после изменений прошёл; кэш — 1204 МиБ при лимите 2048 МиБ. На `1234a57fc` связанные службы перезапущены. В 19:56:33 UTC повторный выбор персонажа B дошёл до `World завершил загрузку персонажа`, регион 13001; далее Game зарегистрировал `connections=1, players=1`. Это подтверждает загрузку и передачу игрока Game по реальному клиентскому соединению. По ответу владельца клиент завис, показал сообщение примерно о нехватке предметов поверх окна и затем закрылся; вход в управляемый мир не подтверждён. Автоматические тесты не создавались и не запускались.

На этой повторной попытке Billing в 19:56:41 UTC получил SQL 156 (`Incorrect syntax near CONVERT`) при `GetUserPoint`; Game в 19:56:44 записал `Receive MSG_B2S_BILLING_AUTHEN : (RES)-2`. Во всех четырёх вызовах процедур `rsplayeraccount` преобразования перенесены из аргументов EXEC в локальные переменные, как описано в [контракте БД](../architecture/database.md#параметры-вызовов-процедур-billing). Исправленная SQL-форма `GetUserPoint` вручную выполнена для того же account в локальной BillingDB: результат `0`, значение баланса получено. Эта процедура только читает баланс; покупки и cash-log для проверки не выполнялись. Сборка всех бинарников прошла, Billing перезапущен и Game подключился к нему заново. Ошибка `-2` в обработчике Game только журналируется, не отправляет клиенту отказ и не закрывает сокет: причинная связь с завершением клиента не установлена. Windows Application Error 1000 в 22:56:48 местного времени подтверждает аварийное завершение локального `Miracle game.exe` с `0xc0000005` по RVA `0x8F703`; разбор клиентского пакета остаётся открытым вопросом.

### Сдвиг пакета входа перед заданиями

Локальный клиент `Miracle game.exe`: SHA-256 `5b41ebfcea8c40ab8756e3fb676c8e03ec1f45fba527f33bc163b084cf520971`, размер 2068480 байт, база PE `0x00400000`. Клиент и его ресурсы при диагностике не изменялись. Повторный вылет 20 сентября в 23:06:13 местного времени записан Windows как `0xc0000005`, RVA `0x8F742` — тот же цикл чтения дополнительных свойств предмета, что и `0x8F703` в предыдущем дампе.

Захват обоих направлений TCP сохранил отправленный Rust Game пакет `0xBF401` длиной 2392 байта после RLE-декодирования. `VERIFIED` для этой записи: предмет экипировки `1010012` содержит 11 обычных свойств и нулевой счётчик дополнительных; этот участок согласован с клиентским чтением. Первое найденное расхождение перед заданиями — запись `city_war_died_state` четырьмя байтами вместо одного. [Контракт и адреса отправителя/получателя](../protocol/game-login.md#флаги-перед-заданиями) фиксируют исправленную ширину.

При чтении старой записи клиент берёт максимум заданий с offset `0x4E7` вместо `0x4EA` и получает 1677721600 вместо 100. Позднее он читает счётчик боевых фей с `0x4F9` вместо `0x4FC`: **39 вместо 0**. Эти 39 взяты из соседнего `loan_time_limit=10080`, а не из БД предметов. Дамп первого вылета содержит возврат `0x00451AEC`: именно вызов декодирования предмета этого контейнера; далее клиент читает чужие поля как запись предмета и выходит за границы пакета. Сообщение клиента о предметах поэтому не свидетельствует об их нехватке.

Исправлена ширина флага в Rust. Независимая сверка сообщений перед `0xBF401` также выявила неверный opcode результата CiQing, пару `(id, id)` вместо `(type, id)` и три расширенных поля в `0xBF721`; исправленные форматы и свидетельства собраны в [каталоге opcode](../protocol/opcode-catalog.md#уведомления-свойств-игрока). Сборка всех шести бинарников прошла за 59,89 секунды, кэш остался 1204 МиБ при лимите 2048 МиБ. Game перезапущен в 20:25 UTC; в 20:26 UTC снова открыл `2347`, зарегистрировал server ID 1 и 541 регион. Результат повторного входа описан ниже. Локальные pcap, декодированный пакет и дамп остаются вне Git; автоматические тесты для этого разбора не создавались.

## Проверка всей последовательности загрузки

После `89b89fa35` владелец повторил вход: прежнее сообщение о предметах сменилось окном `regions/512\config` на экране загрузки. В новой записи `0xBF401` занимает 2373 байта; Game также отправил исправленные `0xC010E` и 124-байтовый `0xBF721`, затем ответил на запрос конфигурации `0xC010D` (5400 байт). Это следующий достигнутый этап, а не подтверждённый вход в мир.

Сопоставление полного пути загрузки с клиентом и оригинальными EXE/PDB выявило дополнительные расхождения:

| Участок | Найденное расхождение и исправление |
| --- | --- |
| Ресурс региона в `0xBF401` | Вместо `resource_id=11001` передавалась ширина 512, вместо `exp_scale=1.0` — биты целой высоты 512. Имя дополнительно перекодировалось в UTF-8. Исправлена передача существующих полей региона и исходных байтов имени; [контракт](../protocol/game-login.md#поля-региона-после-player-snapshot). В `Pack2.pak` локального клиента действительно есть `regions/11001/config` и 22 записи ресурсов этого региона; пакеты клиента не изменялись. |
| Непустые контейнеры в снимке | Пропускался четырёхбайтовый префикс каждой записи; восстановлены признак экипировки, количество и позиция, общий порядок обхода и setup-флаг DaKong. Разделение обычных/DaKong-свойств снова проверяет наличие типа в каталоге, а не пустоту значений; [формат](../protocol/game-login.md#записи-контейнеров-и-предметов). В обеих записанных попытках эти контейнеры пусты, поэтому это отдельный найденный дефект. |
| Подтверждение загрузки карты `0x8F801` | Клиент передаёт `CString + i32`, Rust требовал ровно 4 байта. Добавлен явный разбор подтверждённого клиентского варианта с сохранением оригинального `i32`; `Some(Err)` теперь журналируется как ошибка, а не успех. Значение строки уточнено до `INFERRED: account`: клиент повторно использует тот же global `std::string` первым полем `0x2FD09`; прямое matching Login EXE-подтверждение этой семантики ещё не закрыто. [Парный контракт и предел знания строки](../protocol/opcode-catalog.md#подтверждение-входа-в-регион). |
| Конфигурация CiQing | В `0xC010D` совпадала общая длина, но перепутывались материал B, вероятность, кристаллы, деньги и объявленное число результатов. Исправлены обе стороны общего World/Game codec; [порядок полей](../protocol/opcode-catalog.md#конфигурация-ciqing-0x8fc30--0xc010d). |
| Локальная смена региона `0xBF505` | Перепутывались аргумент перехода и ID назначения; вместо имени передавался путь, а числовой хвост имел неверные поля и размер. Отправитель согласован с оригинальным Game и клиентским reader; [формат](../protocol/opcode-catalog.md#локальный-переход-в-регион-0xbf505). Обычный вход этот пакет не использует, но его может вызвать сценарий переноса. |

Статически прослежено завершение загрузки: `0x8F801` → размещение игрока → `0xBF501` → погода и окружающие фигуры. Клиент переключается из состояния загрузки 5 в игровое состояние 6 по `0xBF501`; отдельного завершающего пакета после соседей нет. Оболочки `0xBF502/0xBF507`, обычные NPC, монстры и короткие снимки игроков сопоставлены с клиентскими декодерами. Редкие постройки, ненулевые состояния, призванные фигуры и дополнительные ветви WarSoul не прошли фактический клиентский сценарий.

Сборка всех шести бинарников с полным набором исправлений прошла за 56,83 секунды; кэш — 1204 МиБ при лимите 2048 МиБ. В 20:52 UTC перезапущены Login, World, Misc и Game. Хеши исполняемых процессов World/Game совпадают с новыми артефактами сборки; в 20:53 UTC Game получил server ID 1, 541 регион и состояние healthcheck `healthy` по listener `2347`. Auth и Billing продолжают работать. Это подтверждает совместный запуск обновлённых служб; вход клиента в управляемый мир после этого набора пока не проверен.

## Статическая реконструкция движения 21 сентября

`VERIFIED` по matching `gameserver.exe + GameServer.pdb`: `CBaseAI::MoveTo @ 0x004C7FF0` выполняет одноклеточный `Slip` по восьми направлениям `_slip_order` и проверяет весь `s_listMoveCheckCell[figure][direction]`. `CPlayerAI::MoveTo @ 0x00508F10` повторяет этот `Slip` один раз для ходьбы, два для бега и три при `RideState 0x186A4`; каждый повтор получает исходное желаемое направление. У игрока используемый virtual figure возвращает `0`. Rust-путь игрока приведён к тому же общему helper, который теперь разделяется с монстрами. Задержка вычисляется по направлению от исходной до окончательной клетки после обхода и не умножается на число Slip.

`VERIFIED` по машинному коду `CPlayerAI::OnSchedule @ 0x005098D0`: когда новая skill-команда отсутствует, очередь назначения движения проверяется раньше уже сохранённой текущей цели. Front назначения снимается только после возврата `MoveTo`, независимо от его успеха. Rust восстановил этот порядок и больше не удерживает queued movement только из-за прежнего `current_player_skill`.

`VERIFIED` по `CMoveShape::OnMove @ 0x004CD490`: направление меняется до `0xBF605`, пакет рассылается до virtual `SetTileXY`, а результат `SetTileXY` не проверяется; после возврата `CPlayerAI::MoveTo` ставит `ASA_MOVE`. Rust сохраняет этот частичный эффект и при собственной ошибке spatial-мутации после уже начатого `OnMove`. Обе формы `0xBF605` дополнительно сопоставлены с локальным reader `FUN_00546c80` исходного клиента. После изменений `cargo check --locked --lib --bins` через `deploy/check-rust.ps1` завершился успешно. На момент этой статической проверки runtime-вход с движением ещё не выполнялся; последующий клиентский проход описан ниже.

## Runtime-проверка Game AI 21/22 сентября

`PARTIAL`: после описанной выше реконструкции движения владелец исходным клиентом вошёл в игровой мир и смог перемещать персонажа. Первый проход имел заметные остановки: после короткого нормального участка персонаж бежал на месте, затем снова продолжал движение. Это подтвердило сам клиентский путь, но не корректную синхронизацию.

Отдельная серверная диагностика сначала обнаружила сильное отставание общего Game-loop: dev-профиль тратил на `CGame::ai` примерно 1,5–1,6 s за проход. После исправления active monster traversal и отказа от лишнего полного resolver-scan release-профиль держал AI примерно на 65–66 ms при базовом интервале 80 ms; предупреждения об отставании больше секунды в проверенном steady-state окне не возникали.

Повторный клиентский проход на release показал отдельную причину оставшихся остановок. `0x8F903` сначала приходил сериями, а затем примерно раз в три секунды, хотя сервер обрабатывал каждый принятый шаг за десятки миллисекунд: очередь назначения не росла, runtime speed был `2400`, spatial mutation возвращала `Ok`. В `run_player_ai_destination` игрок на время `move_step` извлекался из `CGame::players`, а around-runtime без detached-player snapshot не мог разрешить его как получателя; собственный длинный `0xBF605` тем самым пропускался. После передачи snapshot ID/team/shape через существующую границу `GameServerAroundRuntime::with_player` короткий повторный smoke дал последовательные клиентские шаги примерно каждые 0,4–0,5 s, а владелец подтвердил, что прежние остановки исчезли. Это подтверждает текущий обычный сценарий перемещения в проверенной области, но не длительную игровую сессию, все режимы движения или переходы между регионами.

## Первый участок Shared: общий бинарный кодек

22 сентября 2026 года, база `bc07465ed` и рабочие изменения ветки `codex/extract-shared-codec`: выделен пакет `nebokrai-shared`, существующий серверный пакет подключает его как локальную зависимость. `LegacyReader/LegacyWriter` и ошибки границ перенесены в `shared/src/protocol/`; 126 файлов-потребителей переключены на единый публичный путь. Старый модуль Game удалён. Сравнение исходников подтвердило сохранение тел методов с изменением видимости для границы crate; изменения потребителей ограничены заменой путей. Это проверка переноса собственной реализации, а не новое свидетельство о поведении оригинала.

`pwsh -NoProfile -File deploy/check-rust.ps1` выполнил `cargo check --locked --workspace --lib --bins` под Linux в Docker Desktop с Rust 1.97.1. Проверены обе библиотеки и шесть серверных входов; команда завершилась успешно за 1 минуту 23 секунды. `Cargo.lock` добавил только локальный Shared, версии внешних пакетов не изменены. CI переведён на проверку workspace, Dockerfile дополнен копированием Shared. Линковка серверов, сборка игрового образа и клиентские запуски в этом шаге не выполнялись. Автоматические тесты не создавались и не запускались.

## Второй участок Shared: GUID

22 сентября 2026 года, база `bc07465ed` и рабочие изменения ветки `codex/extract-shared-codec`: `CGuid` и `NULL_GUID` выделены в `shared/src/values/`, 132 файла-потребителя переключены на `nebokrai_shared::values`. Старый `public/guid.rs` и его устаревшее подавление `dead_code` удалены. Версии `getrandom` и `uuid` заданы в workspace для обоих пакетов; версии внешних зависимостей в lockfile не изменились.

Сравнение исполняемого текста до и после переноса не выявило изменений, кроме публичной видимости. Для всех 132 потребителей проверена только замена путей, с учётом уже выполненного переноса кодека. Происхождение, локальное неизвестное разбора строки и долг обработки отказа генерации у предметных операций описаны в [GUID](../architecture/values-and-compatibility.md#guid). Свидетельства оригинального поведения в этом шаге не перепроверялись и не дополнялись.

`pwsh -NoProfile -File deploy/check-rust.ps1` успешно выполнил `cargo check --locked --workspace --lib --bins` в Linux/Docker с Rust 1.97.1 за 1 минуту 24 секунды. Проверены обе библиотеки и шесть бинарников; `git diff --check` не выявил ошибок пробелов, битых локальных ссылок в изменённой документации не найдено. Серверы и клиент не запускались, линковка и сборка игрового образа не выполнялись; автоматические тесты не создавались и не запускались.

## Исправление потребителей GUID при рефакторинге

22 сентября 2026 года, та же база `bc07465ed` и рабочая ветка `codex/extract-shared-codec`: разбор продолжен от представления GUID до создания предмета и отказа его потребителя. В Game устранены 28 подстановок `GUID_INVALID` вместо системной ошибки: 15 подготовок копии для разделения, 12 фабричных callbacks и повторная генерация сценарного аукциона. Добавлен один адаптер предметной идентичности с записью причины отказа. У временного пересчёта свойств и проверки камней больше нет фиктивного вызова генерации GUID. Решение и границы эффектов описаны в [ADR-0011](../decisions/0011-goods-guid-failure.md) и [предметах](../gameplay/items.md#создание-идентификатора-предмета).

Статически прослежены: отказ до уменьшения исходной стопки в `take_goods_by_ex_id`; отказ фабрики до выдачи и списания цены в покупке NPC; прекращение сценарного аукциона до замены текущего узла. При пакетном создании отклоняется весь неопубликованный набор, чтобы неполная выдача не прошла по полной цене. Сравнение исходников подтвердило сохранение тела генерации свойств и порядка игровых бросков. Поиск по Game оставляет единственный прямой вызов `CGuid::create` в адаптере, без подстановки нуля.

После последней правки `pwsh -NoProfile -File deploy/check-rust.ps1` успешно проверил обе библиотеки и шесть бинарников через `cargo check --locked --workspace --lib --bins` в Linux/Docker, Rust 1.97.1, за 34,96 секунды. `git diff --check` прошёл. Отказ системного RNG в работающем сервере не воспроизводился; статический разбор ветвей не является runtime-проверкой. Автоматические тесты не создавались и не запускались, серверы и клиент не запускались. Внешние платежи, уже совершённые эффекты других предметных операций и их компенсация этим результатом не проверены.

## Shared resources: чтение пакетов и устранение копирований

22 сентября 2026 года, база `bc07465ed` и рабочая ветка `codex/extract-shared-codec`: `PackageArchive`, `PackageFileIndex` и `PackageReadError` выделены в `shared/src/resources/`. `ClientResource` переключён на библиотеку; старый `public/package.rs` и его устаревшее подавление предупреждений удалены. `flate2`/`lzo` перенесены в зависимости Shared без изменения версий внешних пакетов. Исследовательские привязки используемого пути сокращены до [основной страницы ресурсов](../architecture/resources-and-configuration.md#снимок-пакета); новые свидетельства поведения оригинала не устанавливались.

Архив разделяет неизменяемые байты и индекс через `Arc`, а `extract_compressed` заимствует данные вместо копирования блока и записи индекса. Выходной буфер резервируется с возвратом ошибки выделения. В `rf_open` сохранён возврат `None` без fallback на диск, но вместо потери причины добавлены сообщения об отсутствующей записи и ошибке чтения/распаковки с корнем и путём ресурса.

Статическое сравнение подтвердило сохранение тела разбора заголовков/индекса, кроме упаковки результата в общий снимок. Прослежены границы среза, несжатая/LZO/zlib ветви и путь ошибки до `rf_open`. Последний `pwsh -NoProfile -File deploy/check-rust.ps1` успешно выполнил `cargo check --locked --workspace --lib --bins` под Linux/Docker с Rust 1.97.1 за 34,54 секунды. `git diff --check` и проверка локальных ссылок затронутых страниц прошли. Автоматические тесты не создавались и не запускались; серверы и клиент не запускались. Фактическая загрузка пакетов, отказ аллокатора и изменение CPU/RSS в работающем сервере в этом шаге не измерялись; вывод относится к устранённым копированиям и компиляции, а не к установленной причине игровых проблем с ресурсами.

## Shared resources: курсор, передача буфера и дисковые пути

22 сентября 2026 года, база `bc07465ed` и рабочая ветка `codex/extract-shared-codec`: `CRFile` выделен в `shared/src/resources/rfile.rs`, нормализация путей — в `path.rs`. Серверный `public/rfile.rs` сохраняет только связь контекста с выбором источника. Удалены повторная реализация курсора, неиспользуемый `rf_close`, устаревшее подавление предупреждений и три дубликата нормализатора в `ClientResource`. `read_resource` передаёт память пакета через `into_bytes` без второй аллокации/копирования. Ошибки открытия и полного чтения теперь сохраняют причину; память больше `u32` отклоняется через `Result`, а не `expect`.

В выбранной цепочке установлена несогласованность: обход skill-cache использовал `SKILLS`, возвращал lowercase-путь, а его открытие на Linux шло по имени с учётом регистра. Общий `resolve_resource_path` подключён к обычным ресурсам, `.ril`, `.pak` и корню skill-cache по [ADR-0012](../decisions/0012-resource-path-case.md). Статически разобраны точное совпадение, разрешение каждого компонента, неоднозначность и передача I/O-ошибки; пакетная ветвь не заменяется дисковой при отказе.

Сравнение исходников подтвердило сохранение тела `read_to_stream` и C-string вставки. Проверены границы и изменение логической позиции `read_data/read_exact`, полный перенос memory-буфера и чтение объявленного размера файла. `pwsh -NoProfile -File deploy/check-rust.ps1` успешно выполнил `cargo check --locked --workspace --lib --bins` в Linux/Docker с Rust 1.97.1 за 34,64 секунды после семантических правок. `git diff --check` прошёл. Автоматические тесты не создавались и не запускались. Реальные варианты регистра ресурсных файлов, short read, ошибки доступа/аллокации и клиентская загрузка в работающем сервере не воспроизводились; изменение CPU/RSS не измерялось. Исправленная статическая цепочка не доказывает причину ранее наблюдавшейся недоступности навыков и заданий.

## Shared resources: индекс FilesInfo и ошибки разбора

22 сентября 2026 года, база `bc07465ed` и рабочая ветка `codex/extract-shared-codec`: `FilesInfo`, его записи и ошибки перенесены в `shared/src/resources/filesinfo.rs`. `ClientResource` переключён на Shared; прежний файл и устаревшее подавление предупреждений удалены. Индекс хранится в неизменяемом `Arc`-снимке, а lookup нормализованного сегмента использует заимствованный ключ без выделения `Vec`. При ошибке разбора `.ril` сохраняются путь и байтовое смещение; замена контекста выдаёт диагностику недоступности индекса без изменения прежней дисковой ветви.

Сравнение исходников подтвердило сохранение логики записей/дубликатов `read_folder`, обхода `find_file_list` и сегментов `file_info_by_text`. Статически проверены продвижение токенов и смещения: начало ошибочного числа, конец данных при EOF, учёт whitespace; преобразования чисел остаются прежними. После семантических правок `pwsh -NoProfile -File deploy/check-rust.ps1` успешно выполнил `cargo check --locked --workspace --lib --bins` в Linux/Docker с Rust 1.97.1 за 35,26 секунды. `git diff --check` прошёл. Автоматические тесты не создавались и не запускались. Реальные `.ril`, ошибки их загрузки и клиентская сессия в работающем сервере в этом шаге не проверялись; производительность и память не измерялись. Привязки оригинала перенесены в [описание индекса](../architecture/resources-and-configuration.md#индекс-filesinfo), новые свидетельства не устанавливались.

## Shared resources: каталог, открытие источника и диагностика загрузки

22 сентября 2026 года, база `bc07465ed` и рабочая ветка `codex/extract-shared-codec`: связка индекса с пакетами выделена в `shared/src/resources/catalog.rs`, выбор и открытие источника — в `source.rs`. Серверный `DefaultClientResourceOwner` сохраняет корень, установку/освобождение контекста, адаптацию к загрузчикам и журналирование. Удалены `public/rfile.rs`, промежуточная оболочка `DefaultClientResourceRef`, неиспользуемые методы и устаревшее подавление предупреждений. Открытие через контекст выполняет одну нормализацию и один поиск записи индекса вместо повторных проходов. Границы зафиксированы [в описании каталога](../architecture/resources-and-configuration.md#каталог-и-его-установка).

В начальной установке `WorldProcessResources::install_default_client_resource` отчёт пакетов отбрасывался. Теперь владелец при каждой установке пишет ошибки открытия, чтения и разбора пакетов с ID, путём и причиной, а также числа загруженных, пропущенных и отказавших пакетов. Ошибки обхода skill-cache тоже перестали пропускаться молча; продолжение обхода и накопленный результат сохранены. `open_resource` возвращает различимые `ResourceOpenError`; существующие загрузчики получают прежнее `None` после журналирования ошибки владельцем.

Статическое сравнение подтвердило сохранение алгоритма загрузки каталога после переименований, выбора последнего имени повторного ID, порядка открытия, режима `r+b`, построения путей, `is_file_exist`, индексного списка и порядка замены контекста, кроме добавленной диагностики. Прослежены ветви отсутствующего контекста/индекса/записи, дискового флага, недоступного пакета, отсутствующей записи пакета и отказа распаковки: ошибка выбранного пакета не приводит к дисковому fallback. Shared не импортирует серверные модули, журналирование или выбор cwd.

После изменений `pwsh -NoProfile -File deploy/check-rust.ps1` успешно выполнил `cargo check --locked --workspace --lib --bins` в Linux/Docker, Rust 1.97.1, за 35,41 секунды без предупреждений. `git diff --check` и проверка локальных ссылок затронутых страниц прошли. Автоматические тесты не создавались и не запускались; серверы и клиент не запускались. Загрузка реальных ресурсов, вывод новой диагностики в работающем процессе и изменение CPU/RSS не проверялись. Причина недоступности игровых действий этим шагом не установлена; новых свидетельств по оригинальным бинарникам не добавлено.

## Shared resources: таблица текстов и передача World → Game

22 сентября 2026 года, база `bc07465ed` и рабочая ветка `codex/extract-shared-codec`: `StringTable` и `MyStringTable` перенесены в плоские файлы `shared/src/resources/stringtable.rs` и `stringtable_wire.rs`. World, Game и обработчик конфигурационных сообщений используют публичный интерфейс Shared; прежние файлы `public/stringtable.rs` и `public/mystringtable.rs` удалены. Жизненный цикл экземпляров остаётся у серверных владельцев. Диагностические счётчики декодера возвращаются в результате, а trace-сообщение пишет Game.

Текстовый parser возвращает `StringTableParseError` вместо одного bool: вид отказа, ID, байтовые смещения начала записи и конца доступного входа. World пишет эти поля вместе с путём и размером оставшейся таблицы. Прежние тексты `last_error`, итоговый bool в отчёте загрузчика и уже применённые записи сохранены. В [документации](../architecture/resources-and-configuration.md#таблица-текстов) устранена неоднозначность очистки: декодер сам её не выполняет, но Game `create_string_table` очищает таблицу перед декодированием; при обрыве остаётся принятый префикс, а внешний курсор сообщения не продвигается.

Сравнение исходников подтвердило сохранение текстового алгоритма после исключения новых диагностических полей/`Result`, неизменность encoder-а и чтения/записи C-строк, а также сохранение decoder-а кроме переноса trace и возврата счётчиков. Статически прослежены пустой ввод, комментарий до EOF, обе ветви отсутствующих кавычек, пустой ID, дубликаты, неположительное wire-количество и поздний обрыв пары. Shared не зависит от серверных владельцев или `tracing`.

`pwsh -NoProfile -File deploy/check-rust.ps1` успешно выполнил `cargo check --locked --workspace --lib --bins` в Linux/Docker, Rust 1.97.1, за 38,40 секунды без предупреждений. `git diff --check` и проверка локальных ссылок затронутых страниц прошли. Автоматические тесты не создавались и не запускались; серверы и клиент не запускались. Реальные языковые файлы, сообщения новой диагностики и доставка таблицы в работающий Game в этом шаге не проверялись. Изменение CPU/RSS не измерялось; причина недоступности игровых действий не установлена. Исследовательские привязки перенесены из прежних файлов, новые свидетельства по оригинальным бинарникам не добавлялись.

## Shared resources: определения заданий и частичная загрузка

22 сентября 2026 года, база `bc07465ed` и рабочая ветка `codex/extract-shared-codec`: `CQuestSystem`/`QuestEntry`, текстовый загрузчик и кодек выделены в `shared/src/resources/quest.rs`, `quest_text.rs`, `quest_wire.rs`. World, Game, клиентский снимок персонажа и обработчики конфигурационных сообщений переключены на Shared. Прежний `setup/questsystem.rs` и устаревшее подавление предупреждений удалены. Загрузка файлов, разрешение текстов, журналирование, прогресс персонажа и исполнение сценариев остаются у серверных владельцев. Счётчики сетевого декодирования возвращаются Game, Shared не пишет журнал.

Исправлен отчёт, который после отказа чтения записи мог возвращать `Loaded`: теперь он содержит независимые ошибки основного списка и расширения и возвращает `Partial`, если оба ресурса доступны, но запись не дочитана. World пишет путь, поле, вид ошибки, байтовые смещения и сообщение о частичной загрузке вместо двух `OK`; отсутствие расширения также отмечается. Применённые данные сохраняются, расширение после отказа основной записи продолжает обрабатываться. Это исправление результата Rust-загрузчика и диагностики, а не введение отката или нового барьера инициализации. Правила — в [каталоге заданий](../gameplay/quests.md#каталог-и-состояние-персонажа).

Статическое сравнение исходников подтвердило сохранение порядка полей заголовка и обеих записей, их вставки/замены, нормализации путей, token/line/marker-разбора, encoder-а и wire-reader-ов. Decoder сохраняет порядок изменения общих полей, очистки карты, чтения количества и публикации записей; изменён только возврат диагностических счётчиков вместо trace внутри библиотеки. Прослежены отсутствие ресурсов, отказ заголовка, ошибочное число/EOF внутри записи, отсутствие маркеров текста и независимые отказы обоих списков. Числовые преобразования и разрешение отсутствующего текста остаются прежними.

После семантических изменений `pwsh -NoProfile -File deploy/check-rust.ps1` успешно выполнил `cargo check --locked --workspace --lib --bins` в Linux/Docker, Rust 1.97.1, за 37,76 секунды без предупреждений. `git diff --check` и проверка локальных ссылок затронутых страниц прошли. Автоматические тесты не создавались и не запускались; серверы и клиент не запускались. Реальные Quest.ini/QuestEx.ini, сообщения новой диагностики и принятие задания клиентом в этом шаге не проверялись. Производительность не измерялась; причина недоступности игровых действий не установлена. Новые свидетельства по оригинальным бинарникам не добавлялись.

## Realm content: поиск файлов сценариев

22 сентября 2026 года, база `bc07465ed` и рабочая ветка `codex/extract-shared-codec`: создан пакет-библиотека `nebokrai-realm`, первый компонент расположен в `realm/src/content/scriptfiles.rs`. Из большого World `game.rs` удалён дисковый обход и его вспомогательные функции; оставлен вызов публичного интерфейса Realm, передача корня и журналирование результата. `walkdir` принадлежит Realm; библиотека зависит от Shared, но не от старого серверного пакета. Workspace и Dockerfile подключают новый пакет. Отдельного бинарника Realm пока нет; шесть прежних входов остаются переходной композицией. Правило переноса сразу в целевые Shared/Realm/Zone уточнено в [архитектуре](../architecture/realm-and-zone.md#1-организация-проекта-и-сборки).

Исправлена зависимость дискового поиска сценариев от отдельного cwd: он получает корень установленного `DefaultClientResourceOwner`, через который затем читаются найденные ресурсы. При отсутствии контекста сохраняется прямой путь относительно `.`. Корень поиска разрешается через общий механизм ASCII-регистра по ADR-0012, а выдаваемые ключи не получают физический префикс runtime. Ошибки разрешения и обхода сохраняются в отчёте и журналируются; успешно найденные файлы продолжают загружаться. Выбор literal `scripts/*.*`, фильтр расширения, сортировка, операции загрузки/рассылки и отсутствие нового барьера запуска сохранены. Основное описание — [путь сценарного ресурса](../gameplay/scripting.md#от-файла-до-ресурса-game).

После подключения Realm `pwsh -NoProfile -File deploy/check-rust.ps1` успешно выполнил `cargo check --locked --workspace --lib --bins` в Linux/Docker, Rust 1.97.1, за 1 минуту 30 секунд. Проверены три библиотеки workspace и шесть бинарных входов, без предупреждений. Проверка после изменения Cargo.lock пересоздала прежний кэш; версии внешних зависимостей не менялись. `git diff --check` и локальные ссылки затронутых страниц проверены. Автоматические тесты не создавались и не запускались; серверы и клиент не запускались. Обход реального дерева с другим cwd, регистром и отказами доступа, доставка сценариев и игровой эффект в этом шаге не проверялись. Отсутствие сценариев как причина проблем игры не установлено; новых свидетельств по оригинальному бинарнику не добавлено.

## Realm content: владелец сценариев и индексная загрузка

2026-09-22, рабочее дерево на базе `bc07465ed`, продолжение переноса в Shared/Realm/Zone. В `realm/content/scripts.rs` выделен `ScriptResources`: тексты функций и переменных, карта сценариев, загрузка/замена/очистка и выбор списка. Старый World подключает его через адаптер, сохраняет уведомления и рассылку. Удалены самостоятельные поля и реализация загрузчика из World `CGame`; повторная нормализация ключа сведена к одной функции Realm.

Перед переносом проверены SHA-256 и соответствие CodeView EXE/PDB по GUID/age. По инструкциям `Nworldserver.exe` установлено пропущенное в Rust обращение к индексному списку сценариев. Исправлено: индексный корень имеет приоритет, пустой список не вызывает дискового fallback, отсутствие корня разрешает обход диска. Отказы отдельных файлов сохраняют продолжение загрузки, но теперь попадают в сводку с источником и числом отказов. Проверенные ветви, адреса, идентификатор сборки и оставшиеся различия дискового обхода зафиксированы на [странице сценариев](../gameplay/scripting.md#основание-загрузки-и-границы-проверки); локальные дампы остаются вне Git.

Дополнительно исправлено установленное расхождение Shared `FilesInfo::find_file_list`: нижний ASCII-регистр применяется и к расширению. Прежнее утверждение об обработке только корня опиралось на неполные границы функции Ghidra; второй `_strlwr` найден в машинных инструкциях после `_free`. Правило и основание — в [описании индекса](../architecture/resources-and-configuration.md#индекс-filesinfo).

После последней правки `pwsh -NoProfile -File deploy/check-rust.ps1` успешно выполнил `cargo check --locked --workspace --lib --bins` в Linux/Docker, Rust 1.97.1, за 37,78 секунды без предупреждений. `git diff --check` и локальные ссылки затронутых страниц проверены. Автоматические тесты не создавались и не запускались; серверы и клиент не запускались. Проверка машинного кода подтверждает конкретные расхождения, но не устанавливает их связь с наблюдавшимися отказами навыков, ударов или заданий. Доставка сценариев и исправление игрового случая пока не подтверждены запуском. `AGENTS.md` остаётся локальным и исключён через `.git/info/exclude`.

## Zone content: полученные ресурсы сценариев

2026-09-22, рабочее дерево на базе `bc07465ed`, следующий участок реорганизации Shared/Realm/Zone. Создана библиотека `nebokrai-zone`, первый компонент — `content/scripts.rs`. Ему переданы буферы функций/переменных и карта текстов сценариев, установка, lookup и очистка. Game использует одного владельца через переходные методы; декодирование/регистрация персонажа и применение общих переменных читают его данные. Интерпретатор, реестр функций и активные экземпляры остаются в прежнем Game. Zone не зависит от прежнего пакета или внешних библиотек; workspace, локальные зависимости Cargo.lock и Dockerfile подключены к новому пакету.

Проверены идентичность Game EXE/PDB и машинные инструкции setters, lookup и обновления реестра. Исправлена ошибка повторной публикации: Rust освобождал старый FunctionList/VariableList и отбрасывал полученный буфер; оригинал после delete продолжает установку. Теперь повторный FunctionList также перестраивает реестр. Переход за delete был пропущен в границах функций Ghidra и подтверждён непосредственным чтением PE. Для ключа сценария убрано лишнее копирование уже переданного Vec. Основания и пределы проверки — в [установке ресурсов Zone](../gameplay/scripting.md#установка-ресурсов-в-zone).

`pwsh -NoProfile -File deploy/check-rust.ps1` выполнил `cargo check --locked --workspace --lib --bins` в Linux/Docker, Rust 1.97.1, за 1 минуту 26 секунд без предупреждений. Проверены четыре библиотеки и шесть бинарных входов. Изменение lock-файла пересоздало кэш; версии внешних зависимостей не менялись. Автоматические тесты не создавались и не запускались; серверы и клиент не запускались. Исправление ветвей подтверждено статически, но повторная доставка ресурсов, работа игровых действий после reload и связь с прежними проблемами игрока не проверены запуском.

## Shared scripting и Zone content: FunctionList и реестр команд

2026-09-22, рабочее дерево на базе `bc07465ed`: реестр имён команд перенесён из прежнего Game `script.rs` в `zone/content/functions.rs`. Формат читает новый `shared/scripting/functionlist.rs`; Zone зависит от Shared. Game связывает публикацию текста с загрузкой реестра, журналирует отчёт и передаёт ошибку обработчику сообщения. Исполнитель и активные экземпляры сценариев остаются в прежнем Game.

По машинным инструкциям идентифицированной пары Game EXE/PDB сверены LoadFunction, CIni и преобразование ID через CRT. Исправлены различия заголовков секции, границ блока, сохранения пробелов в именах и переполнения ID. Удалена безусловная регистрация `AddJingJieBuff = 11131`, отсутствующая в исследованном LoadFunction и способная перезаписать объявление из файла. Числовой обработчик сохранён; соответствие другой сборке не объявляется проверенным. Основания и безопасные отказы для небезопасных ветвей оригинала описаны в [правилах FunctionList](../gameplay/scripting.md#разбор-functionlist-и-реестр-команд). Ошибка теперь содержит смещение и не сопровождается `FunctionList...OK!`; уже установленный текст и разобранный префикс реестра сохраняются, новый барьер запуска не введён.

`pwsh -NoProfile -File deploy/check-rust.ps1` успешно выполнил `cargo check --locked --workspace --lib --bins` в Linux/Docker, Rust 1.97.1, за 2 минуты 1 секунду без предупреждений. Проверены четыре библиотеки и шесть бинарных входов. Автоматические тесты не создавались и не запускались; серверы и клиент не запускались. Проверка машинного кода подтверждает перечисленные правила загрузки, но доступность игровых команд, связь с отказами навыков/заданий и изменение нагрузки этим запуском не проверены.

## Shared scripting: объявления VariableList

2026-09-22, рабочее дерево на базе `bc07465ed`: разбор объявлений Game выделен в `shared/scripting/variablelist.rs`. Общий проход CIni вынесен из FunctionList в соседний `ini.rs`; оба формата используют его, сохраняя отдельные правила преобразования. Старые `section_records`, trim, разбор суффикса массива и числовой parser удалены из Game `variablelist.rs`. Shared выдаёт заимствованные объявления; Game материализует значения у игрока или в своей копии общих переменных. Владельцы живого состояния, World GeneralVariableList и wire-кодек пока остаются в прежних компонентах.

Повторно проверены идентичность Game EXE/PDB и байты инструкций LoadVarList, GetArrayNum, CIni, числовой ветви интерпретатора и начала декодирования снимка. Исправлены обрезание пробелов, объединение повторов имён, определение строки по префиксу `#`, насыщение чисел и обрезание имени при нулевом размере массива. Сохранён установленный результат ReadInt для массива без разделителя. Основания и ограничения — в [объявлениях VariableList](../gameplay/scripting.md#объявления-variablelist).

В текущем шаге сверено ветвление `LoadVarList`: отрицательный результат `GetArrayNum`, как и нулевой, выбирает scalar/string-ветвь; прежний Rust-отказ удалён. В Shared добавлено контекстно-свободное арифметическое подмножество для десятичных размеров с `+`, `-`, `*`, `/`, `%` и скобками. Остальные выражения по-прежнему дают `UnsupportedArrayExpression`, поскольку перевод их обработки не доказан и не реализован. Ошибки синтаксиса и безопасных границ содержат смещение; отказ выделения массива возвращается вызывающему. Декодирование снимка останавливается до его полей, сохраняя уже загруженные объявления. Дополнительная инициализация при регистрации журналирует отказ, не меняя прежнюю политику регистрации. Это не полное восстановление выражений или сохранения переменных; детали — в [объявлениях VariableList](../gameplay/scripting.md#объявления-variablelist).

Для предыдущего среза `pwsh -NoProfile -File deploy/check-rust.ps1` успешно выполнил `cargo check --locked --workspace --lib --bins` в Linux/Docker, Rust 1.97.1, за 38,40 секунды без предупреждений. В текущем срезе `cargo check --locked -p nebokrai-shared --lib` завершился успешно, и `rustfmt --check` прошёл по изменённым Rust-файлам. Полный workspace-чек через документированный Linux/Docker-скрипт не завершён: пустой Docker-кэш скачивал зависимости более девяти минут без перехода к компиляции; запуск остановлен. Предыдущий успешный workspace-чек относится к коду до этого изменения. Тесты, сборка исполняемых файлов, серверы и клиент не запускались. Доступность навыков и заданий и влияние на нагрузку остаются неподтверждёнными. `AGENTS.md` остаётся локальным и исключён из Git.

## Zone scripts: список переменных Game

22 сентября 2026 года, база `a79bf33f2`: реализация `CVariableList`, типы значений/результатов/ошибок и кодек снимка перенесены из `server/rust/src/gameserver/appserver/script/variablelist.rs` в [Zone scripts](../../server/rust/zone/src/scripts/variables.rs). Старый файл и его модуль удалены; Game-потребители используют публичный интерфейс `nebokrai-zone`. Персональный список по-прежнему хранится полем игрока, а общая копия — полем игрового агрегата: эти внешние агрегаты пока находятся в переходном хосте. World/Realm `CVariableList` не затронут. Сохранены привязки к исходному `GameServer` EXE/PDB и прежние правила списка; новые выводы о поведении оригинала в этом переносе не заявляются.

`cargo check --offline --locked -p nebokrai-zone --lib` прошёл; `rustfmt --edition 2024 --check` для перенесённого модуля и `git diff --check` прошли. Импорты потребителей и рабочие ссылки документации ведут на Zone, старый модуль больше не объявлен. Полная Linux/Docker-проверка не завершена: после изменения lock-файла скрипт начал заново получать crates.io index при почти пустом кэше и был остановлен. Проверка Game-бинарника на Windows также не прошла из-за неподдерживаемой платформы `procfs` и сборки нативной зависимости; README указывает, что нативная Windows-сборка не поддерживается. Поэтому потребители старого хоста не подтверждены компилятором в этом шаге. Тесты и игровой runtime не запускались.

При повторной проверке коммитов `a79bf33f2`–`4c1d0dd53` найден унаследованный от старого Game-модуля дефект: положительный tag массива из снимка сразу передавался в `Vec::with_capacity`, даже если в сообщении не было стольких значений. Zone теперь сначала сверяет размер с остатком входа и использует отказоустойчивое выделение. Для этого изменения проверена та же пара Game EXE/PDB; новых выводов о поведении оригинала нет. `cargo check --offline --locked -p nebokrai-shared -p nebokrai-zone --lib`, `rustfmt --check` затронутых Rust-файлов и `git diff --check` прошли. Полный Game-бинарник и сценарий с клиентом не проверялись; сохранена указанная ниже граница оригинального `ReadCmd`.

## Shared scripting: парсер команд Game

22 сентября 2026 года, база `2a01d9d7a`: чистый байтовый парсер перенесён из старого Game-модуля в `shared/src/scripting/parser.rs`; Zone `CScript` теперь потребляет Shared API. Единственная прямая правка поведения — набор разделителей имени в `command_name` приведён к инструкциям `GetFunctionName`. Проверенный артефакт: `gameserver.exe` SHA-256 `4F5C98E0FDF6147D8AECF55F7937AAF6E2CF5E4F5A2C44491A6359228762C80E`, `GameServer.pdb` SHA-256 `B17BB9B7D69A9CC43E314C0E35C517830BB42CAA89416E173380AB17D2D66016`; EXE CodeView и PDB stream совпали по GUID `5bee6dd1-bf90-49b8-8be9-eb25c4038d53`, age `2`. Дизассемблирование `RunStep` (`0x00428D80`) подтвердило вызов `ReadCmd` перед `GetFunctionName` и размер буфера `0x4000`; инструкции `GetFunctionName` (`0x00425000`) подтвердили `(`, пробел, TAB, LF, CR и `;` как завершители имени.

В этом шаге `ReadCmd` напрямую не разбирался, поэтому соответствие оригиналу для границ команд, пропуска комментариев и остальных ветвей парсера оставалось `PARTIAL`/`UNKNOWN`; перенос не объявлял их совпадающими. `nom` стал прямой зависимостью Shared и удалён из переходного серверного пакета. `cargo check --offline --locked -p nebokrai-shared --lib`, `rustfmt --edition 2024 --check` для перенесённого файла и `git diff --check` прошли. Полный Game-чек на Windows не запускался: README указывает, что нативная Windows-сборка не поддерживается; клиент и runtime не запускались. Тесты не создавались и не запускались.

Продолжение на базе `860775319`: из переходного Game `script.rs` в тот же Shared parser перенесены четыре чистые функции поиска присваивания, оператора верхнего уровня и индекса переменной. Вызовы Game переключены на Shared; алгоритм этих функций не менялся. Теперь отдельно разобран машинный код Game `ReadCmd` (`0x00424CB0`): ветвь `0x00424E4F–0x00424E7C` сохраняет CR/LF внутри кавычек и пропускает следующие TAB. `next_command` исправлен для этой ветви без выделения дополнительного буфера в обычной команде. Это подтверждает только указанное правило; конец файла, выдача `;`, курсор, комментарии и полный язык остаются частично либо совсем не сверенными. Целевые `cargo check --offline --locked -p nebokrai-shared -p nebokrai-zone --lib`, `rustfmt --check` для Shared parser и `git diff --check` прошли. В большом старом `script.rs` проверка rustfmt по-прежнему показывает прежние отличия форматирования вне затронутых строк. Полная компиляция Game, runtime и клиент в этом шаге не проверялись.

## Zone sessions: проверочная последовательность входа

На базе `04c30d055` компонент `CSequenceRegistry`/`CSequenceString` перенесён из старого Game `message/sequencestring.rs` в [Zone sessions](../../server/rust/zone/src/sessions/sequence.rs). Старый Game держит экземпляры сессий и общий RNG, выбирает момент отправки `0xBF403`; нового RNG или сетевого владельца в библиотеке нет. Повторно проверены SHA-256 той же пары Game EXE/PDB и инструкции конструктора `0x0042A390`, `Initialize` `0x0042A6D0–0x0042A755`, `Serialize` `0x0042A3D0–0x0042A473`, а также условие входа `0x0049F6CC–0x0049F6F9`. Установленный формат записан в [игровом входе](../protocol/game-login.md#условия-отправки); перенос не изменяет его. `cargo check --offline --locked -p nebokrai-zone --lib`, `rustfmt --check` для файлов Zone sessions и `git diff --check` прошли. Полная Linux-сборка переходного Game и клиентский вход после переноса не проверялись; семантика последующей проверки ответа последовательности этим шагом не установлена.

## Сквозная компиляция после переносов Shared и Zone

После `2bc524fd1` предусмотренный README `pwsh -NoProfile -File deploy/check-rust.ps1` впервые проверил полный workspace в Linux/Docker. Первый проход выявил ошибку `E0624`: переходный Game вызывал `CVariableList::load_definitions`, оставшийся `pub(crate)` после переноса списка в Zone (`2a01d9d7a`). В Zone добавлена публичная операция `initialize_if_empty`, которая сохраняет прежнюю проверку пустого списка и вызывает закрытый загрузчик; Game использует её. Повторный `cargo check --locked --workspace --lib --bins` под Rust 1.97.1 прошёл для всех библиотек и шести бинарных входов. Это подтверждает компиляцию, но не выполнение входа, работу сценариев или ответы клиента. Новых контрактов оригинала это исправление не вводит; автоматические тесты и серверы не запускались.

## Zone sessions: состояние проверки входа

После `294f01497` три поля переходного `CGame` — реестр проверочной последовательности, выбранные последовательности по player ID и отметки времени — сведены в [Zone `LoginValidationState`](../../server/rust/zone/src/sessions/validation.rs). Game по-прежнему выбирает момент отправки `0xBF402/0xBF403`, владеет настройкой и общим RNG; `Zone sessions` готовит последовательность и очищает состояние игрока. Порядок получения RNG, вставки owner-а перед отправкой, удаления при отказе и очистки при завершении сохранён. По инструкциям той же пары Game EXE/PDB сверены `AppendSequenceMap` VA `0x00409340–0x004093C7` (дубликат удаляется до отказа), `AppendValidateTime` VA `0x004093D0–0x004093F9` (вставка без замены) и `OnLogMessage` VA `0x0049F63F–0x0049F6F9` (сначала `0xBF402`, затем условие `0xBF403`). Форматы и границы записаны в [игровом входе](../protocol/game-login.md#условия-отправки). Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker с Rust 1.97.1 прошёл; клиентский вход и ответная проверка не запускались, автоматические тесты не создавались.

Следующая сверка `OnLogMessage` VA `0x0049F63F–0x0049F6C7` показала, что настройка `CGame+0x134` в ветви отправки используется только как ненулевой переключатель: в `0xBF402` добавляются ID и два показания часов. `CPlayer::OnLost` VA `0x004417A0–0x004417C7` удаляет сначала sequence, затем отметку. В Rust удалён неиспользуемый тип `PlayerLoginValidateTime` с полем `timeout_ms`, которое ошибочно намекало на уже работающий контроль срока; существующее хранение отметки и порядок отправки не менялись. Ответная проверка и значение настройки вне этой ветви остаются неизвестными. Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 38,19 секунды; проверка клиентом для этого среза не проводилась. `cargo fmt --check` для всего workspace выявил ранее существовавшие массовые расхождения форматирования; изменённые строки дополнительно проверены через `git diff --check`.

## Shared resources: таблица эмоций

23 сентября 2026 года общий `CEmotion` перенесён из `src/setup/emotion.rs` в [Shared resources](../../server/rust/shared/src/resources/emotion.rs). World и Game используют один тип и отдельные экземпляры, а старые загрузчики получают вынесенный поиск маркера через прежний адаптер. Неиспользуемое чтение прямого дискового пути удалено: Shared принимает байты, а World выбирает источник. По идентифицированным парам World EXE/PDB и Game EXE/PDB сверены загрузка `LoadSetup` VA `0x004A0CA0`, запись `Serialize` VA `0x004A03F0`, чтение `Unserialize` VA `0x004D80C0` и поиск `IsEmotionRepeated` VA `0x004D7870`; область утверждений и безопасные границы — в [таблице эмоций](../architecture/resources-and-configuration.md#таблица-эмоций). Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 36,73 секунды после финального изменения. Изменённые файлы Shared прошли `rustfmt --check`; клиент и серверы не запускались, автоматические тесты не создавались. Появление типа в Shared само по себе не проверяет загрузку `data/Emotions.ini` в живом процессе.

## Shared resources: таблица HitLevel

23 сентября 2026 года `CHitLevelSetup` перенесён из `src/setup/hitlevelsetup.rs` в [Shared resources](../../server/rust/shared/src/resources/hitlevel.rs). World выбирает `data/hitlevel.ini` и отправляет снимок, Game хранит принятую таблицу и использует её в серии убийств. Неиспользуемый прямой дисковый загрузчик удалён; общий формат принимает уже выбранные World байты. В Game устранено копирование всего каталога при каждом обновлении серии: игрок получает заимствованный срез без изменения алгоритма поиска. По совпадающим World и Game EXE/PDB проверены очистка перед открытием/декодированием и 12-байтные записи со счётчиком; адреса и безопасные отличия на повреждённых данных — в [контракте HitLevel](../architecture/resources-and-configuration.md#таблица-hitlevel). Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 39,82 секунды, файл Shared прошёл `rustfmt --check`. Запуск серверов и клиента не выполнялся, автоматические тесты не создавались.

## Shared resources: шаблоны персонажа

23 сентября 2026 года общий `CPlayerList` перенесён из `src/setup/playerlist.rs` в [Shared resources](../../server/rust/shared/src/resources/playerlist.rs). Удалены три неиспользуемых метода прямого чтения файлов: выбор источника и последовательность чтения остаются у World. Типы World и Game используют один формат; `origin_equipment` остаётся мировой таблицей и не входит в передаваемый снимок. Машинный код совпадающих World и Game EXE/PDB подтвердил 0x58-байтную запись свойств, порядок блоков снимка и очистку только основных таблиц перед приёмом; три карты улучшений накладываются по уровню. Подробности и граница полного совпадения — в [шаблонах персонажа](../architecture/resources-and-configuration.md#шаблоны-персонажа). Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 38,40 секунды; серверы и клиент не запускались. Автоматические тесты не создавались.

## Zone combat: модель рассчитанной атаки

23 сентября 2026 года `MasterInfo` и `AttackInformation` вместе с компонентами и конечной арифметикой перенесены из переходного Game в [`zone/combat`](../../server/rust/zone/src/combat/mod.rs). Старые пути теперь только экспортируют типы; единый `UNKNOWN_SKILL_ID` задаёт Zone. Исходное поведение сверено по совпадающей паре Game EXE/PDB: конструктор и присваивание `tagMasterInfo` VA `0x0050A610/0x0050A640`, `tagAttackInformation::Clear` и конструктор VA `0x005D3CA0/0x005D3D70`, `CMoveShape::ApplyFinalDamage` VA `0x004D0DA0`. Последний подтверждает последовательное DWORD-вычитание, насыщение нулём, ограничение текущим максимумом и запись только положительной знаковой разницы. Поэтому приведение отрицательного компонента к DWORD оставлено без «исправления». Применение к живому игроку, защита, смерть и callbacks остаются в переходном Game; этот шаг не проверял их целиком и не подтверждает клиентский удар. Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 38,81 секунды. Автоматические тесты и серверы не запускались.

## Zone effects: автоматическое восстановление

23 сентября 2026 года четыре состояния автоматического восстановления выделены в [`zone/effects`](../../server/rust/zone/src/effects/automaticrestore.rs). Старый Game оставляет адаптер живого игрока и Begin/restart; `AutomaticRestoreProperties` передаёт только нужные поля вместо зависимости библиотеки от всего `CPlayer`. Прямо сверены ID конструкторов и HP peace `AI` по паре Game EXE/PDB, включая проверку состояния до часов, строгое сравнение DWORD и повторный вызов времени. При первом разборе 12-байтная запись оставалась `PARTIAL`: PDB-имена адресов writer/reader указывали на другие состояния. Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 39,24 секунды. Клиентская работа восстановления не проверялась; автоматические тесты и серверы не запускались.

Позднее в той же сборке проверены vtable всех четырёх состояний и вызов reader из фабрики. Они ведут к общим телам writer/reader несмотря на чужие PDB-имена. Инструкции подтверждают три DWORD записи (ID, частота, объём), чтение двух последних после ID и инициализацию часов при чтении. Формат записи теперь `VERIFIED`; прямые адреса и граница вывода — в [описании состояний](../gameplay/attributes-and-states.md#автоматическое-восстановление-hp-и-mp).

## Zone effects: отображаемый остаток срока

23 сентября 2026 года общий для Rust-состояний расчёт отображаемого остатка перенесён из переходного Game в [`zone/effects`](../../server/rust/zone/src/effects/time.rs); старый путь оставлен переэкспортом. В Game EXE/PDB функция `CHealState::GetRemainedTime` VA `0x005F2CD0` подтверждает DWORD-срок, ноль при первом `now >= deadline` и отдельное второе чтение часов для положительного остатка. Прежний комментарий ошибочно называл тот же адрес `CBlindState`. Другие классы, использующие Rust helper, в этом шаге не сверялись. Граница исходного правила — в [описании состояний](../gameplay/attributes-and-states.md#что-делает-экземпляр-состояния). Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 37,75 секунды; `rustfmt --check` и `git diff --check` тоже прошли. Серверы и автоматические тесты не запускались.

## Zone effects: периодический урон

23 сентября 2026 года `PeriodicAttackCore` и общий кодек записи перенесены из старого Game в [`zone/effects`](../../server/rust/zone/src/effects/periodicattack.rs). Поля времени и счётчик теперь закрыты операциями Zone; форматный `PeriodicAttackRecord` не зависит от `AppliedState`. Game по-прежнему разрешает участников, рассылает visual и применяет атаку. По сопоставленной паре Game EXE/PDB проверены writer/reader PoisonArrow, reader Kerosene, writer/reader BloodLoss, writer LeafCut и reader LeafCut2, а также порядок срока/частоты в AI PoisonArrow. Установленная область формата, адреса и оставшиеся границы — в [описании периодического урона](../gameplay/attributes-and-states.md#периодический-урон). Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 37,20 секунды. Работа с клиентом и полные AI-варианты не проверялись; автоматические тесты и серверы не запускались.

Следующим шагом общий payload четырёх ядовитых состояний вынесен в [`zone/effects/poison.rs`](../../server/rust/zone/src/effects/poison.rs). Старый Game оставляет лишь адаптер `AppliedState` и применение атаки. Конструкторы и vtable всех четырёх классов в той же Game-сборке подтвердили общие writer/getter, а также общий reader для PoisonArrow, SpiderPoison и SpriteBurn и отдельный порядок времени у Kerosene. `CSpriteBurnState::AI` подтвердил позднюю проверку срока после попытки удара; иные ветви расчёта урона не заявлены проверенными. Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 36,82 секунды. Серверы и автоматические тесты не запускались.

Затем payload BloodLoss и его seed перенесены в [`zone/effects/bloodloss.rs`](../../server/rust/zone/src/effects/bloodloss.rs). Значение ID `0x21D` теперь одно для записи состояния и переходного навыка; игровой RNG и расчёт по текущей цели остались у Game. По той же Game EXE/PDB сверены конструктор, vtable, writer/reader, последовательность бросков в `CalculateAttackPower` и ветви `GetCCH` игрока и монстра; другие виды цели остаются неразобранными. Точные адреса и область вывода — в [описании периодического урона](../gameplay/attributes-and-states.md#периодический-урон). Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 36,70 секунды. Клиент и серверы не запускались, автоматические тесты не создавались.

Три payload LeafCut перенесены в [`zone/effects/leafcut.rs`](../../server/rust/zone/src/effects/leafcut.rs). ID `0x6B/0x80/0x8F` теперь задаются в Zone и используются переходными навыками; расчёт урона с RNG и применение к цели остались у Game. По той же Game EXE/PDB сверены конструкторы состояний и навыков, все три vtable, общий writer/reader и getter срока. Для первого `CalculateAttackPower` сверены порядок бросков и критического расчёта; у второго и третьего — ключевые операции, но не полный граф ветвей. Адреса и предел проверки — в [описании периодического урона](../gameplay/attributes-and-states.md#периодический-урон). Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 37,69 секунды; `rustfmt --check` и `git diff --check` тоже прошли. Автоматические тесты и серверы не запускались.

## Zone effects: запас метеорных стрел

23 сентября 2026 года payload `CMeteorArrowState`, его 12-байтная запись и операция добавления перенесены в [`zone/effects/meteorarrow.rs`](../../server/rust/zone/src/effects/meteorarrow.rs). Старый Game продолжает выбирать живой экземпляр, проверять S, публиковать visual и удалять запас при расходе. По совпадающей паре Game EXE/PDB сверены конструкторы состояния и навыка, vtable, writer/reader и `AddMeteorArrow`: сохранены signed-сравнение, DWORD-переполнение, верхний предел и обновление visual при нулевом добавлении ниже предела. Точные адреса и область вывода — в [описании запаса](../gameplay/attributes-and-states.md#запас-метеорных-стрел). Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 37,16 секунды; `rustfmt --check` нового файла и `git diff --check` прошли. Полный путь накопления/расхода с клиентом не проверен; автоматические тесты и серверы не запускались.

## Zone effects: ядовитый туман

23 сентября 2026 года данные, кодек и расчёт потерь `PoisonFogState` перенесены в [`zone/effects/poisonfog.rs`](../../server/rust/zone/src/effects/poisonfog.rs); Game оставляет поиск живой фигуры, visual и запись результата. Оба общих числовых преобразования `FISTP DWORD` и `__ftol2` перенесены из переходного Game в [`zone/combat/rounding.rs`](../../server/rust/zone/src/combat/rounding.rs) со старыми путями-переэкспортами. По сопоставленной Game EXE/PDB сверены ID конструкторов навыка и состояния, vtable, девять DWORD записи, момент чтения часов reader, строгая проверка срока и две ветви property callback. Исправлено установленное расхождение: потеря защиты/сопротивления игрока теперь берёт младшее WORD 64-битного `__ftol2`, а не 32-битного FISTP. Точные адреса и граница проверки — в [описании ядовитого тумана](../gameplay/attributes-and-states.md#ядовитый-туман). Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 37,26 секунды; `rustfmt --check` новых Zone-файлов и `git diff --check` прошли. Клиентский сценарий и полный lifecycle не проверены; автоматические тесты и серверы не запускались.

## Zone effects: прибавки максимума HP и MP

23 сентября 2026 года два постоянных payload максимума HP/MP объединены в типизированную модель [`zone/effects/maxresource.rs`](../../server/rust/zone/src/effects/maxresource.rs). Старый Game оставляет поиск живого игрока, выбор изменяемого свойства и Begin/End; значения ID для переходных навыков теперь приходят из Zone. По совпадающей паре Game EXE/PDB отдельно сверены конструкторы навыков и состояний, обе vtable с общими writer/reader и формулы property callbacks. Два DWORD записи и unsigned-ограничение суммы `INT_MAX` описаны в [сценарии постоянной прибавки](../gameplay/attributes-and-states.md#сценарий-постоянное-увеличение-максимумов-hp-и-mp). Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 36,34 секунды; `rustfmt --check` и `git diff --check` прошли. Работа с клиентом и полный путь применения навыков не проверялись; автоматические тесты и серверы не запускались.

## Zone effects: полное уклонение EnlargeFullMiss

23 сентября 2026 года данные, восьмибайтная запись и WORD-формула `EnlargeFullMissState` перенесены в [`zone/effects/fullmiss.rs`](../../server/rust/zone/src/effects/fullmiss.rs). Game оставляет участников, Begin/End и изменение свойства живого игрока. По совпадающей паре Game EXE/PDB сверены конструктор, vtable, writer/reader и property callback; адреса и область проверки приведены в [описании состояний](../gameplay/attributes-and-states.md#сценарий-постоянное-увеличение-максимумов-hp-и-mp). Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 39,45 секунды; `rustfmt --check` нового Zone-файла и `git diff --check` прошли. Клиентский сценарий и серверы не запускались, автоматические тесты не создавались.

## Zone effects: TaiJi и Origin

23 сентября 2026 года общий формат двух постоянных состояний и разные формулы для игрока перенесены в [`zone/effects/element.rs`](../../server/rust/zone/src/effects/element.rs). По совпадающей Game EXE/PDB сверены конструкторы и ID, vtable с общими writer/reader, а также callbacks игрока и передача полной прибавки монстру. Адреса и граница вывода приведены в [описании состояний](../gameplay/attributes-and-states.md#сценарий-постоянное-увеличение-максимумов-hp-и-mp). Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 38,42 секунды; `rustfmt --check` и `git diff --check` прошли. Клиентский сценарий и серверы не запускались, автоматические тесты не создавались.

## Zone effects: постоянные прибавки Swordship

23 сентября 2026 года данные, 12-байтная запись и формулы игрока четырёх вариантов Swordship перенесены в [`zone/effects/swordship.rs`](../../server/rust/zone/src/effects/swordship.rs). Game оставляет доступ к живым фигурам и Begin/End. По совпадающей Game EXE/PDB сверены ID, четыре vtable, общий writer/reader, property callback и порядок MIN → MAX; адреса и граница проверки приведены в [описании состояний](../gameplay/attributes-and-states.md#сценарий-постоянное-увеличение-максимумов-hp-и-mp). Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 37,83 секунды; `rustfmt --check` и `git diff --check` прошли. Клиентский сценарий и серверы не запускались, автоматические тесты не создавались.

## Исследование записи WuXing

23 сентября 2026 года по сопоставленной паре Game EXE/PDB проверены ID и vtable пяти WuXing-состояний, общий writer/reader и сырая длина записи 96 байт. [Описание границы](../gameplay/attributes-and-states.md#wuxing-граница-сохраняемой-записи) отделяет подтверждённый размер блока от ещё не проверенных полей и формул текущего Rust-кода. Реализация в Game этим шагом не менялась; компиляция, серверы и клиент не запускались, автоматические тесты не создавались.

## Zone effects: данные WuXing

23 сентября 2026 года типизированное состояние, 96-байтный кодек и соответствие пяти ID перенесены в [`zone/effects/wuxing.rs`](../../server/rust/zone/src/effects/wuxing.rs). Доступ к живому игроку и прежняя численная формула остаются в Game. Два оригинальных property callbacks подтвердили смещения полей, использованных при переносе; границы знания, включая промежуточные байты и ещё не проверенные вычисления, указаны в [описании WuXing](../gameplay/attributes-and-states.md#wuxing-граница-сохраняемой-записи). После замены доступа к полям тело формулы Rust совпало с прежним при нормализации форматирования. Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 36,36 секунды; `rustfmt --check` и `git diff --check` прошли. Серверы и клиент не запускались, автоматические тесты не создавались.

## Zone effects: временная прибавка Hearten

23 сентября 2026 года данные, срок и 12-байтная запись Hearten перенесены в [`zone/effects/hearten.rs`](../../server/rust/zone/src/effects/hearten.rs). Формула прибавки максимума HP использует общий с постоянными Enlarge механизм Zone; Game сохраняет участников, visual, применение к живому игроку и End. По совпадающей паре Game EXE/PDB сверены конструкторы навыка и состояния, vtable, общий writer/reader, getter срока, строгая проверка истечения и property callback. Адреса и пределы вывода указаны в [описании Hearten](../gameplay/attributes-and-states.md#hearten-временная-прибавка-максимума-hp). Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 38,49 секунды; `rustfmt --check` и `git diff --check` прошли. Клиентский результат и полный lifecycle не проверялись; автоматические тесты и серверы не запускались.

## Zone visual: общая база и ресурс навыка

23 сентября 2026 года `CVisualEffect` перенесён в [Zone effects](../../server/rust/zone/src/effects/visualeffect.rs), а `SkillVisualEffect` с внутренним выбором вида — в [Zone skills](../../server/rust/zone/src/skills/visualeffect.rs). Старый `states/visualeffect.rs` оставлен как переходный экспорт для существующих вызовов Game. По сопоставленной паре Game EXE/PDB непосредственно сверены `BeginVisualEffect` VA `0x005DC1B0`, End `0x005DC1D0`, две перегрузки Update `0x005DC1E0/0x005DC1F0`, конструктор `0x005DC200` и деструктор `0x005DC220`: ненулевое значение `loop` подавляет базовый End, а деструктор выставляет `ended` и обнуляет `loop`. Виды `SkillVisualEffectKind` остаются внутренним Rust-dispatch; полные производные publishers и их клиентские пакеты этим шагом не сверялись. Полный `cargo check --locked --workspace --lib --bins` в Linux/Docker прошёл за 38,91 секунды. Автоматические тесты и серверы не запускались.

## Пробелы, влияющие на путь к игре

1. **Сборка, совместный запуск, вход в мир и короткий обычный сценарий движения подтверждены, но полный lifecycle ещё нет.** [Rust-дополнение Compose](../../deploy/hybrid/compose.rust.yaml) запускает все шесть Rust-служб; [Rust Dockerfile](../../deploy/hybrid/Dockerfile.rust) успешно собрал release-бинарники. На текущем Game release клиент вошёл в мир и после исправления self-delivery `0xBF605` непрерывно перемещался в коротком smoke. Ещё не подтверждены длительная сессия, logout/save/relogin, региональный переход и устойчивость остальных gameplay-подсистем.
2. **Знание протокола не равно устойчивой работе с клиентом.** В отслеживаемом дереве нет проекта клиента или автоматического сквозного испытания. Наличие EXE/PDB, статических контрактов и даже одного достигнутого движения не подтверждает длительную игровую сессию, корректность всех вариантов движения или синхронизацию остальных подсистем.
3. **Модель БД зависит от локальных материалов.** `runtime/` целиком исключён из Git. [Локальный скрипт БД](../../deploy/hybrid/database-init.sh) создаёт проекцию `LoginDB.dbo.userinfo` поверх BillingDB, потому что в найденных backup исходной таблицы нет. Это поддерживает локальный сценарий, но не доказывает точную исходную схему и все её побочные эффекты.
4. **World и Game сохраняют явные границы реконструкции.** [`process/worldserver.rs`](../../server/rust/src/process/worldserver.rs) различает блокировку `Init`, цикла и `Release`; в GameServer dispatcher имеются сообщения без обработчика, а фабрика навыков знает больше ID, чем доказанно исполняемых реализаций. Эти факты ограничивают вывод о готовности к длительной игре даже после успешной компиляции.
5. **Сохранённый C++-проход не закрывает эти пробелы автоматически.** Из 716 `.cpp` файлов 535 пустые; [CMake](../../server/cpp/CMakeLists.txt) исключает пустые единицы трансляции и объявляет пять приложений без GameServer. Его код остаётся полезным источником, но не подтверждённой полностью работающей альтернативой.

## Что считать следующими доказательствами

Для каждого игрового сценария нужны: точная версия оригинального клиента, текущий commit сервера, необходимые локальные данные и конфигурация, проверка сборки всех нужных бинарников, наблюдённый вход/переход между процессами, результат действия игрока и, если оно сохраняется, проверка чтения после повторного входа. Результат записывается как проверенный сценарий с исходом и границами, а не как общий процент готовности. Текущий честный итог: **вход в мир и короткое непрерывное движение на текущем release подтверждены; стабильная длительная играбельность, сохранение состояния и переходы между регионами ещё не доказаны**.
