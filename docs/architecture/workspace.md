# Карта проекта

Основная разработка идёт в [server/rust/](../../server/rust/). Каталоги разделены по назначению, чтобы код, данные запуска и старые реализации не зависели друг от друга.

Эта страница показывает существующее дерево. Для рефакторинга принята [структура Realm, Zone и Shared](realm-and-zone.md#1-организация-проекта-и-сборки): три целевых пакета, компоненты с именами по обязанности и плоские файлы внутри компонента. Уже выделены Shared и первые компоненты Realm и Zone. Остальные обязанности пока находятся в старом серверном пакете; его шесть бинарников используют выделенные библиотеки на время переноса.

| Путь | Назначение |
| --- | --- |
| [server/rust/](../../server/rust/) | Основной Rust-сервер; пакет и шесть бинарников объявлены в [Cargo.toml](../../server/rust/Cargo.toml). |
| [server/rust/realm/](../../server/rust/realm/) | Библиотека `nebokrai-realm`: `src/activities/` владеет мировыми городской, деревенской, четырёхстранной и фракционной войнами (`attackcitysys.rs`, `villagewarsys.rs`, `fournationwarsys.rs`, `factionwarsys.rs`) и DB-адаптером Gods Battle (`rsgodsbattle.rs`); `src/auction/` владеет межсерверным узлом аукционного товара и поиском (`auctionnode.rs`, `auctionroom.rs`) и журнальной записью аукциона с исходным `#[repr(C)]` layout 0x150 байт и `SYSTEMTIME` (`auctionlog.rs`) и комнатами аукциона Misc/Game с исходным порядком opt/del/success/back очередей (`aucitionroom.rs`); `src/characters/` владеет FIFO запросов загрузки игроков и снимком обновления exploit (`playerloadqueue.rs`, `playerexploit.rs`); `src/billing/` владеет DB-операциями счетов и increment-log (`rsplayerfillmgr.rs`, `dbincrementlog.rs`); `src/persistence/` — DB-инициализацией World и SQL row-помощниками (`rssetup.rs`, `row.rs`), DB-адаптером общих переменных `CSL_GENVAR` (`rsgenvar.rs`) и write-log конвейером World: payload-типами FIFO журнала (`writelog.rs`), самой FIFO (`writelogqueue.rs`) и worker-ом с исходными SQL-вставками и reconnect-loop (`writelogworker.rs`); `src/regions/` — DB регионов (`rsregion.rs`); `src/organizations/` — DB enemy factions, параметрам, country-identity и отчётом отказа мутации (`rsenemyfactions.rs`, `organizingparam.rs`, `countryidentity.rs`, `factionenemyblock.rs`); `src/access/` владеет генератором validcode (`validcode.rs`), GAS HTTP (`mywininet.rs`), login DB (`rscdkey.rs`), DB queue-формами (`dbqueue.rs`), IPv4-фильтром (`kl_ipfilter.rs`), очередью (`kl_multi_list.rs`), DB контекстом и network-конфигурацией Auth (`dbcontext.rs`, `networkconfig.rs`), ini-reader (`configreader.rs`), обработчиками Auth-сообщений (`message_func.rs`) и auth DB worker (`authproc.rs`), FIFO service log (`servlogqueue.rs`) и FIFO журнала аккаунтов Login с typed-записями (`acclogqueue.rs`, `acclog.rs`); `src/content/` владеет ресурсами сценариев, выбирает индексный или дисковый список и выполняет загрузку, событийными календарными таблицами (`timetoreturn.rs` — `TimeToReturn.ini` с weekly/absolute событиями), wire-сериализатором базовых свойств товара (`goods.rs`), записью навыка и skill cache World→Game (`skill.rs`, `skillfactory.rs`), общими wire-структурами организаций (`organizing.rs`) конфигурацией объединения battle fairy (`battlefairyproperty.rs`), параметрами стран (`countryparam.rs`) и списком общих скриптовых переменных мира (`variablelist.rs`); `src/app/` владеет всем краем направления Login/GameServer ↔ WorldServer: сообщением и диспетчером (`world_message.rs`), принятым сервером Game-соединений (`world_server.rs`), состоянием принятого Game-клиента (`world_server_client.rs`) и исходящим Login-клиентом (`world_client.rs`), Misc-направлением к World и его ответными ветвями (`misc_message.rs`, `misc_client.rs`, `othermessage.rs`), конфигурацией роли Misc (`setup.rs`) и полным runtime MiscServer: доменным `CGame` с AI/ProcessMessage/reconnect-порядком (`misc_game.rs`), auction-handler (`miscservermessage.rs`) и служебными ветвями Billing (`onbillserver.rs`), Billing-направлением к Game (`billing_message.rs`, `billing_server.rs`, `billing_server_client.rs`), Auth-направлением к Login (`auth_message.rs`, `auth_server.rs`, `auth_server_client.rs`), сообщением трёх направлений Login (`login_message.rs`) и полным краем Login: сообщением (`login_message.rs`), listener игровых клиентов (`login_server.rs`) и World-соединений (`login_world_server.rs`), принятыми состояниями (`login_server_client.rs`, `login_world_server_client.rs`) и исходящим Auth-клиентом (`login_auth_client.rs`). `nets/netlogin/` содержит только переходные реэкспорты. Зависит от Shared, `parking_lot`, `tokio` и `walkdir`, не импортирует прежний серверный пакет. Собственного бинарного входа пока нет. |
| [server/rust/zone/](../../server/rust/zone/) | Библиотека `nebokrai-zone`: `src/activities/` владеет локальным исполнением арен и войн: ареной (`jjcsystem.rs`), войной стран (`countrywarsys.rs`) и системами городских, деревенских и четырёхстранных войн (`attackcitysys.rs`, `villagewarsys.rs`, `fournationwarsys.rs`); `src/app/` владеет краем направления GameServer: сообщением (`game_message.rs`), listener игровых клиентов (`game_server.rs`), состоянием принятого клиента (`game_server_client.rs`) и исходящим краем к World/Billing (`game_client.rs`); `src/replication/` владеет spatial recipient-snapshot принятых игроков Game region (`recipients.rs`) и around-runtime view разрешения игроков и sessions (`around.rs`); `src/content/` владеет принятыми текстами, реестром команд, базовыми свойствами товара со startup-decoder (`goods.rs`), параметрами стран (`countryparam.rs`) и startup-snapshot почётных списков (`honorranks.rs`), `src/regions/` — идентичностью фигуры, базовым объектом пространственной идентичности (`baseobject.rs`) и базовым региональным cell-storage/codec (`region.rs`), а также достигнутой spatial/membership частью фигуры (`shape.rs`), storage-частью области (`area.rs`), жизненным циклом призванного монстра (`summonedcreature.rs`) и proxy-проекцией удалённого региона с wire-проекцией `tagRegionParam` (`proxyserverregion.rs`, `regionparam.rs`); `src/sessions/` — последовательностями входа, базовыми session/plug примитивами (`csession.rs`, `cplug.rs`), командным сеансом с разъёмом (`cteam.rs`, `cteamate.rs`) и buyer plug личной лавки (`cpersonalshopbuyer.rs`), `src/skills/` — базой навыков, фабричным реестром владельцев и политик (`skillfactory.rs`) и выделенными правилами, `src/effects/` — данными наложенных эффектов, `src/quests/` — прогрессом, временем и флагом доступности заданий живого игрока, `src/items/` — базовым lifecycle контейнера (`ccontainer.rs`). Зависит от Shared, `parking_lot` и `tokio`, не импортирует прежний серверный пакет; собственного бинарного входа нет. |
| [server/rust/shared/](../../server/rust/shared/) | Самостоятельная библиотека `nebokrai-shared`: кодек в `src/protocol/`, GUID в `src/values/`, снимок `.pak` и распаковка в `src/resources/`. Использует `bytes`, `getrandom`, `uuid`, `flate2`, `lzo`; не импортирует серверных владельцев. |
| [server/cpp/](../../server/cpp/) | Отдельная C++-реализация с [CMake](../../server/cpp/CMakeLists.txt) и [vcpkg](../../server/cpp/vcpkg.json). В Rust-пакет и гибридный стенд не входит. |
| [deploy/hybrid/](../../deploy/hybrid/) | Образы, Compose и подготовка локального стенда. |
| [deploy/check-rust.ps1](../../deploy/check-rust.ps1) | Проверка типов Rust и отдельный режим сборки бинарников в локальном Docker Desktop с отдельным ограничиваемым кэшем. |
| [docs/](../README.md) | Руководство разработчика и справочные страницы. |
| `runtime/` | Локальные настройки служб, игровые ресурсы, backup БД и GameServer для стенда. |
| `original/server/` | Оригинальный Windows-комплект сервера. Для сборки основного кода не требуется. |
| `.exe/`, `ghidra/`, `reports/` | Оригинальные бинарники, результаты анализа и рабочие отчёты. |
| `related/Luvinia/` | Код другой игры на том же движке; нужен только для отдельных исследований. |

Локальные материалы исключены из Git и Docker build context. Оригинальный комплект сохраняется без внутренних правок; его состав и применение описаны [отдельно](../reconstruction/sources.md).

## Внутри Rust

[src/lib.rs](../../server/rust/src/lib.rs) собирает общий граф модулей и экспортирует функции запуска. Большинство модулей остаются внутренними для пакета.

Существующий пакет `nebokrai-server` зависит от `nebokrai-shared`, `nebokrai-realm` и `nebokrai-zone`. В [shared/src/protocol/](../../server/rust/shared/src/protocol/) находятся `reader.rs`, `writer.rs` и `errors.rs`; публичные типы доступны через `nebokrai_shared::protocol`. В [shared/src/values/](../../server/rust/shared/src/values/) находится `guid.rs`; `CGuid` и `NULL_GUID` доступны через `nebokrai_shared::values`. Все прежние потребители переключены на Shared, модули `gameserver/appserver/legacycodec.rs` и `public/guid.rs` удалены. Контракт кодека — в [общих механизмах](shared-mechanisms.md), представление и границы GUID — в [значениях](values-and-compatibility.md#guid).

Первые компоненты ролей — [realm/src/content/](../../server/rust/realm/src/content/) и [zone/src/content/](../../server/rust/zone/src/content/). Realm выбирает источник, загружает и хранит тексты для передачи. Zone устанавливает полученные тексты и владеет реестром функций в `content/functions.rs`. Формат FunctionList разбирает [shared/src/scripting/functionlist.rs](../../server/rust/shared/src/scripting/functionlist.rs): он возвращает объявления или ошибку со смещением, не хранит реестр и не пишет журнал. Интерпретатор пока остаётся в прежнем Game. World/Game содержат переходные адаптеры, без второй копии перенесённых буферов, карты сценариев и реестра функций. Правила — в [сценариях](../gameplay/scripting.md#от-файла-до-ресурса-game).

В `shared/src/scripting/` общий проход `ini.rs` обслуживает FunctionList и [variablelist.rs](../../server/rust/shared/src/scripting/variablelist.rs). Последний возвращает объявления VariableList без выделения массивов и доступа к состоянию персонажа. Старый Game `CVariableList` применяет их к своему списку; его wire-кодек и мутации пока остаются на месте. Поддержанные размеры и явная граница выражений описаны в [объявлениях переменных](../gameplay/scripting.md#объявления-variablelist).

В [shared/src/resources/](../../server/rust/shared/src/resources/) находятся `catalog.rs` (связь индекса с пакетами и отчёт загрузки), `source.rs` (выбор и открытие источника), `filesinfo.rs` (индекс `.ril`), `package.rs` (снимок пакета и распаковка), `rfile.rs` (курсор/передача буфера) и `path.rs` (нормализация и разрешение регистра на диске). Прежние `public/filesinfo.rs`, `public/package.rs` и `public/rfile.rs` удалены. Серверный `public/clientresource.rs` хранит установленный корень и каталог, управляет их заменой и пишет диагностику; порядок reload и игровые таблицы остаются у серверных владельцев. Границы и причины — в [ресурсах](resources-and-configuration.md#каталог-и-его-установка).

В том же компоненте `resources/` находятся `stringtable.rs` (таблица байтовых текстов и текстовый parser) и `stringtable_wire.rs` (передача World → Game). Обе стороны используют Shared напрямую; `public/stringtable.rs` и `public/mystringtable.rs` удалены. Момент очистки, применение, рассылка и журналирование принадлежат серверным владельцам; [правила и частичные эффекты](resources-and-configuration.md#таблица-текстов) описаны отдельно.

`resources/emotion.rs` хранит общий формат и таблицу эмоций, а `marker.rs` — поиск маркера в текстовых токенах. Прежний `setup/emotion.rs` удалён; старый `public/readwrite.rs` пока переэкспортирует поиск маркера для остальных загрузчиков. World/Realm выбирает ресурс и отправляет снимок, Game/Zone принимает его и применяет таблицу. [Контракт эмоций](resources-and-configuration.md#таблица-эмоций) отделяет формат от этих стадий.

`resources/hitlevel.rs` хранит таблицу HitLevel и её передачу World → Game. Прежний `setup/hitlevelsetup.rs` удалён; выбор источника и отправка остаются в World, а применение бонуса — в Game. [Формат и порядок очистки](resources-and-configuration.md#таблица-hitlevel) записаны в ресурсной странице.

`resources/playerlist.rs` хранит общие шаблоны создания и развития персонажа и их снимок World → Game. Прежний `setup/playerlist.rs` удалён; файловый доступ и порядок reload остаются у World, а применение полученных определений — у игровых владельцев. [Различие очистки основных таблиц и наложения улучшений](resources-and-configuration.md#шаблоны-персонажа) важно для повторного снимка.

Формат определений заданий выделен в Shared: `resources/quest.rs` задаёт каталог, `quest_text.rs` разбирает два текстовых ресурса, `quest_wire.rs` обслуживает передачу World → Game. Экземпляр каталога World и порядок чтения ресурсов находятся в Realm `content/quests.rs`; полученный экземпляр Game и его установка — в Zone `content/quests.rs`. Прежний `setup/questsystem.rs` удалён; World, Game и клиентский снимок персонажа используют общий тип. Прогресс и игровые операции остаются у серверов; [разделение и отказы](../gameplay/quests.md#каталог-и-состояние-персонажа) описаны на странице заданий.

| Каталог в `server/rust/src/` | Где проходит граница |
| --- | --- |
| `bin/` | Тонкие точки входа. |
| `process/` | Tokio runtime, каталог ресурсов, сигналы ОС и результат процесса. |
| `authserver/`, `loginserver/`, `billingserver/`, `miscserver/`, `worldserver/`, `gameserver/` | Состояние и поведение соответствующей службы. |
| `nets/` | Соединения, очереди, сообщения и правила отдельных сетевых направлений. |
| `transport/` | Общие операции TCP через Tokio. |
| `dbaccess/` | Операции MSSQL, разделённые на Auth, Login, Billing и World. |
| `setup/` | Общие игровые настройки и таблицы. Конфиги отдельных служб читаются также внутри их модулей. |
| `public/` | Вспомогательные типы и механизмы; часть файлов содержит обязанности конкретных служб. Границы проверяются по [каталогу механизмов](shared-mechanisms.md), а не по имени `public`. |

В Game доменные подсистемы находятся в `appserver/`, в World — в `appworld/`. Сборку подсистем и основной цикл выполняет соответствующий `game.rs`, а связь с ОС, ресурсами и фоновыми задачами — процессный runtime. Разбор этого разделения — в [модели процессов](../server/process-model.md).

Полный декомпилят хранится только локально. В публичном дереве остаются собственная реализация, технические выводы и метаданные происхождения. Файлы без исполняемого кода не означают реализованную подсистему; проверяйте подключение в `lib.rs` и фактические вызовы. См. [границу исследовательского корпуса](../reconstruction/overview.md).

Практическая карта «задача → модуль» находится в [руководстве разработчика](../development.md).

Каталог ресурса не определяет способ чтения: World может брать байты из пакета, собирать таблицу и передавать её Game. Этот путь описан в [конфигурации и ресурсах](resources-and-configuration.md).
